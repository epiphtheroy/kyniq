# Connect (film-history import) — server env for the WEB deploy (Vercel).
# All optional: a missing provider just shows its tile as "Coming soon" in-app.
# Set these in the Vercel project's Environment Variables (not committed).
# See LAUNCH-RUNBOOK.md §0 and HANDOFF-커넥트-기록이관.md §7.2.

# REQUIRED to enable ANY OAuth connector — the token-encryption key.
# Generate: openssl rand -hex 32
CONNECT_TOKEN_KEY=

# Trakt (trakt.tv/oauth/applications — free, instant)
# Redirect URIs to register there: metatake://connect-callback
#   and dev: exp://<your-mac-LAN-IP>:8081/--/connect-callback
TRAKT_CLIENT_ID=
TRAKT_CLIENT_SECRET=

# Simkl (simkl.com/settings/developer — free)
# Same redirect URIs as Trakt.
SIMKL_CLIENT_ID=
SIMKL_CLIENT_SECRET=

# TMDB — already set as TMDB_READ_TOKEN (used for matching + v4 user auth).
# Just allow the two redirect URIs above in the TMDB v4 app settings.

# Optional: protect the daily sync cron (shared with the push cron).
# CRON_SECRET=
