# HANDOFF — The Navigator: 시네필 턴바이턴 (메타테이크 제1기능 기획서)

> **오너 선언(2026-07-26): 마이룸의 핵심이자 서비스 전체의 가장 중요한 기능. 우리의 모든 지적 기획의 축약버전.**
> 구글맵 모바일 내비게이션을 메타포로, 시네필의 여정을 목적지→경로→턴바이턴으로 안내한다.
> 이 문서는 **기획 정본**이다 — 구현 전 오너 승인 게이트(§12) 통과 필수.
> 연구 근거: Google Navigation SDK 공식 문서의 UI 해부(§2에 반영).

---

## §0. 오너의 말, 정돈된 언어화

오너가 말한 것을 한 페이지로 정리하면:

> **"내비게이션은 복잡한 지도 속에서 당신이 해야 할 것만 추려서 그때그때 보여주는 기능이다.
> 복잡한 연산은 앱이 하고, 당신은 지금 필요한 것만 본다."**

이를 시네필의 삶에 옮기면:

1. **지도는 이미 있다.** 시네마 전체(수천 편)가 시대×취향의 평면에 놓여 있고(오디세이),
   그 위에 길들(사조·정전·계보·감독)이 나 있으며, 내가 이미 걸어온 길(본 영화)이 표시된다.
2. **목적지를 적는다.** "TIFF Top 10 완주", "홍상수 정복", "1960년대 에센셜" — 수백 개가 준비되어 있다.
3. **누르면 경로가 펼쳐진다.** 경유할 영화들이 구글맵의 경로처럼 지도 위에 이어지고,
   단거리/최소시간 같은 **경로 선택지**가 나란히 제시된다.
4. **주행이 시작되면**, 도로 위에 **내 위치**가 있고, 상단에는 **다음 신호(다음 영화)**가,
   하단에는 **남은 거리와 시간**이 뜬다. 그때그때 필요한 것만.
5. **경로 요약 이미지**로 여정 전체를 한 장에 본다(공유 가능).

**왜 이것이 모든 기획의 축약인가:** 메타테이크는 지금까지 지도(오디세이)·점수(TakeScore)·
진행(커버리지)·추천(스크리너)·가용성(왓투와치)·기록(원장)을 각각 만들었다. 내비게이터는 이
여섯을 **한 화면의 문법**으로 묶는다 — 지도 위에서, 점수로 고른, 내 구독으로 볼 수 있는,
다음 한 편을, 내 진행 위에서, 안내한다. "그걸로 뭘 하느냐"(사업전략 §6.5)의 최종 답이다.

---

## §1. 메타포 사전 (구글 내비 요소 → 메타테이크 등가물)

연구로 확보한 구글 내비의 정식 해부 전체를 1:1 번역한다. **이 표가 기획의 심장이다.**

| 구글 내비 (공식 요소) | 메타테이크 등가물 | 데이터 소스 (이미 있음) |
|---|---|---|
| **지도 레이어** | 시네마 평면 — 시대(x) × 취향(y) | odyssey `map.v1.json` (1,959편 좌표) |
| **도로망** | 사조·계보·정전 라인 35+ / 감독 필모 길 | odyssey lines · lineage · 감독 필모 |
| **경로 폴리라인** | 목적지까지 이어지는 영화 체인 | 경로 엔진(§4) 산출 |
| **셰브런(내 위치)** | 여정에서 내가 선 자리 = 마지막 본 영화 뒤 | `user_movies`(**항상 파생, 저장 금지** §10-1) |
| **지나온 길(회색 처리)** | 이미 본 구간 | `me_coverage`·원장 |
| **턴 가이던스 카드** (기동 아이콘·도로명·턴까지 거리) | **다음 영화 카드**: 포스터·제목·"왜 이 턴인가" 사유 칩 | `me_recommend_wwi` 사유·계보 순번 |
| **차선 안내 (lane guidance)** | **어느 서비스 차선으로**: Netflix·Criterion… 사용 가능 차선 하이라이트 | `film_availability`·내 구독 |
| **트립 진행바** | 여정 진행률 (7/12편) | 커버리지 계산 |
| **ETA · 남은 시간 · 남은 거리** | **남은 편수 · 남은 러닝타임 합 · 도착 예정일**(내 페이스 기반) | runtime 합 · 최근 90일 시청 페이스 |
| **교통 레이어 (정체·통제)** | **가용성 기상**: 스트리밍 이탈 임박=통제 예고 · 대여만=유료도로 | availability + leaving-soon |
| **대안 경로 (라이트 라인 + ETA 버블)** | 경로 3종: 최단·최속·무료도로 (§4.2) | 경로 엔진 |
| **경로 옵션 (톨비 회피 등)** | "대여 없이"(내 구독만) · "걸작 우선" | 내 구독 필터 |
| **재탐색 (rerouting)** | 경로 밖 영화를 봤을 때/턴 스킵 시 재계산 — *"경로를 재탐색합니다"* | 원장 변화 감지 |
| **리센터 버튼** | 내 위치(다음 영화)로 복귀 | UI |
| **오버뷰 버튼** | 전체 경로 보기(지도 위 폴리라인) | 오디세이 평면 재사용 |
| **음소거** | 안내 알림 끄기(주간 넛지 옵트아웃) | 설정 |
| **도착 경험** | **도착 화면 + 경로 요약 이미지**(공유 카드) | §5.5 |
| **커스텀 마커** | 랜드마크 = 정전 대작들(지도 위 상시 비치) | TakeScore 상위 |
| **야간 모드** | (v2) 다크 주행 뷰 | — |

---

## §2. 개념 모델 — 다섯 명사

용어 헌장(1명사=1실체) 준수. 신규 명사는 둘뿐이다.

- **지도(Map)** — 시네마 평면. 기존 실체(오디세이) 재사용, 새 이름 없음.
- **목적지(Destination)** — 완주 가능한 영화 집합 + 완주 조건. 수백 개 카탈로그(§3).
- **경로(Route)** — 목적지까지의 **순서 있는** 영화 체인. 항상 결정론적으로 도출(LLM-0).
- **여정(Drive)** — 사용자가 "안내 시작"을 누른 순간 생기는 진행 중 상태. 한 번에 1개(v1).
- **턴(Turn)** — 여정의 다음 한 편. 항상 정확히 하나만 제시된다. **이것이 제품의 심장.**

> ⚠️ "Journey"는 이미 Metatake Deck(/journey)의 이름 — 충돌 회피를 위해 진행 상태는 **Drive**로
> 명명(한국어 UI는 "여정"으로 표기 가능, 코드·DB는 drive). 제품명은 **The Navigator / 내비게이터**.

---

## §3. 목적지 카탈로그 (6과, 수백 개)

오너 명시 + 기존 자산 직결. **전부 이미 데이터가 있다** — 카탈로그는 조립이지 생산이 아니다.

| 과(科) | 예시 | 소스 | 완주 조건 |
|---|---|---|---|
| **정전(Canon)** | "TIFF All-Time Top 10" "Sight & Sound 100" | lineage(facet=canon·critics·festival) — **me_coverage가 이미 진행률 보유** | 전편 seen |
| **감독(Director)** | "홍상수 정복" "타르콥스키 전작" | auteur conquest | 핵심 필모 seen |
| **TakeScore 티어** | "APEX 100 완주" | cinecodex_ranked | 티어 내 전편 |
| **연도별 TakeScore** | "1960년대 에센셜 20" "2010년대 상위 30" | ranked × decade | 부분집합 seen |
| **내 구독 우선순위** | "내 서비스의 최고작 15 — 떠나기 전에" | availability × TS × leaving-soon | 리스트 소진 |
| **사조/장르 (도로 완주)** | "누벨바그 고속도로" "느와르 국도" | odyssey 35 lines | 라인 핵심역 seen |

- 목적지 크기 규율: **8~40편**(작으면 성취감 없음, 크면 도착 불가) — 대형 정전(100+)은
  "1구간/2구간" 분할 목적지로 제공.
- 목적지 카드에 상시 표기: 총 편수 · 총 러닝타임 · 내 진행률(이미 본 편수) · 내 구독 커버리지 %.

## §4. 경로 엔진 (결정론, LLM-0)

### 4.1 순서 원칙 (경로는 "정렬"이다)
목적지 집합이 주어지면 경로는 다음 우선순위로 **결정론적 정렬**:
1. 이미 본 영화 → 뒤로(지나온 길, 회색 구간).
2. 목적지 고유 순서가 있으면 존중(계보 순위·연대기·감독 연대기).
3. 동순위 내: **지금 볼 수 있는 것 먼저**(내 구독 on > 대여 > 미가용), 다음 leaving-soon 급한 것.
4. 최종 동률: 취향 근접(t-SNE 거리, journey deal 엔진 재사용) — 다음 턴이 낯설지 않게.

### 4.2 경로 3종 (대안 경로)
| 경로 | 구글 등가 | 정의 |
|---|---|---|
| **최단** | Fewest turns | 완주 조건을 만족하는 최소 편수(핵심 코어) |
| **최속** | Fastest | 총 러닝타임 최소 정렬(같은 집합, 짧은 것 우선) |
| **무료도로** | Avoid tolls | **내 구독만으로** — 대여·미가용 제외(완주 불가 시 "n편은 유료도로" 정직 표기) |

선택 화면은 구글처럼: 지도 위 3개 라이트 라인 + 각각 "12편 · 22h 40m · 예상 도착 10월".

### 4.3 ETA (정직성 규율)
- **페이스 = 최근 90일 seen/주** (원장 파생). 페이스 0이면 ETA 숨김, 러닝타임 합만.
- 표기: "이 페이스면 **10월 말 도착**" — 절대 단정 금지, "이 페이스면" 접두 고정.

### 4.4 재탐색
- 경로 내 영화를 봄 → 셰브런 전진(자동 — 위치는 원장 파생이므로 웹/앱/임포트 어디서 봐도 즉시).
- 경로 **밖** 영화를 봄 → 위치 불변(벌점 없음). 목적지 집합에 속하면 편입 처리.
- "이 턴 건너뛰기" → 해당 편을 경로 후미로(삭제 아님) + *"경로를 재탐색합니다"* 마이크로 모먼트.
- 가용성 변화(이탈 임박) → 통제 예고 배지 + 해당 편 앞당김 제안.

---

## §5. 화면 기획 (모바일 퍼스트, 5화면)

### 5.1 목적지 탐색 (Where to?)
구글의 검색바+최근 목적지 문법. 상단 "어디로 갈까요?" 검색 + 6과 브라우즈 행(가로 스크롤 카드:
진행률 링 오버레이). 이미 시작한 여정이 있으면 최상단 "주행 재개" 카드.

### 5.2 경로 선택
```
┌──────────────────────────────┐
│  [지도: 3개 라이트 라인+버블]   │
│   ● 최단 12편 · 21h · ~10월    │
│   ○ 최속 12편 · 19h · ~10월    │
│   ○ 무료도로 14편 · 24h · ~11월 │
│  ⚙ 대여 없이 · 걸작 우선        │
│  [ 안내 시작 ]                 │
└──────────────────────────────┘
```

### 5.3 ★ 주행 뷰 (THE screen — 제품의 얼굴)
```
┌──────────────────────────────┐
│ ▲ 다음 턴                     │ ← 턴 카드(초록 유리): 포스터 썸네일
│ [poster] Persona (1966)       │    + "왜": '베리만 코어 3/5 · 취향 근접'
│ 107min · ▸Criterion ▸(rent)   │    + 차선: 가능한 서비스 하이라이트
├──────────────────────────────┤
│                              │
│    [도로 리본 뷰]              │ ← 스타일라이즈드 도로: 지나온 정거장
│  ●──●──●──▣──○──○           │    (회색)·현재(셰브런)·다음 2~3 정거장
│         ▲나                   │    포스터가 랜드마크처럼 도로변에
│                              │
├──────────────────────────────┤
│ ⏱ 5편 남음 · 9h 12m · ~10월末 │ ← 트립 푸터 + 진행바
│ [진행바 ▓▓▓▓▓▓▓░░░░ 7/12]    │
│ (오버뷰) (스킵) (본 걸로)      │
└──────────────────────────────┘
```
**핵심 규율: 이 화면에는 위 요소 외 아무것도 없다.** 연산은 뒤에서, 화면엔 지금 필요한 것만 —
오너의 정의 그대로. 도로 리본은 v1에서 순수 SVG/CSS(실지도 불필요 — 구글도 주행 뷰에선
세계 전체가 아니라 확대된 도로만 보여준다).

### 5.4 전체 경로 (오버뷰)
오디세이 평면 위에 경로 폴리라인 + 정거장 포스터 마커 + 지나온 구간 회색. **기존
OdysseyGalaxy에 경로 오버레이 모드 추가**로 구현(재사용, 신규 지도 없음).

### 5.5 도착 + 경로 요약 이미지
"🏁 도착했습니다 — TIFF Top 10 · 3월 2일 출발 · 7월 26일 도착 · 12편 · 22h 41m."
**요약 이미지**: 지도 축소판 + 경로 + 정거장 포스터 스트립 + 날짜 — OG 카드로 렌더(공유·저장
시스템 재사용). 다음 목적지 제안 2개로 즉시 재출발 루프.

---

## §6. 표면 배치

| 표면 | 역할 |
|---|---|
| **모바일 앱** | **제1표면.** Tonight 탭이 여정 활성 시 주행 뷰가 된다 — 앱 정체성("판단 내비게이터")의 문자적 완성 |
| **/room** | Tonight 섹션 최상단에 활성 여정 미니 주행 카드(다음 턴+진행바) · 전용 /room/navigator |
| **/journey (Deck)** | 존속 — "목적지 없이 한 판" = 즉흥 드라이브. 딜 엔진은 §4.1-4 동률 정렬에 재사용 |
| 공개 마케팅 페이지 | v2 — /navigator 소개(색인 가능). v1은 로그인 표면만 |

## §7. 기존 자산 배선 (신규 생산 최소화 증명)

지도=`map.v1.json`(있음) · 도로=odyssey lines+lineage(있음) · 진행=me_coverage(있음) ·
사유칩=me_recommend_wwi(있음) · 가용성=film_availability+wtw(있음) · 러닝타임=films(있음) ·
페이스=원장(있음) · 취향거리=deal.ts(있음) · 공유카드=OG 시스템(있음) · 도착 통계=원장(있음).
**신규는 셋뿐**: 목적지 카탈로그 테이블 · 경로 엔진(순수 함수) · 주행 뷰 UI.

## §8. 데이터 모델 (v1 최소)

```sql
nav_destinations (id, family, key, label, film_slugs[], ordered bool, size, total_runtime)  -- 조립 산출물, 재생성 가능
nav_drives (id, user_id, dest_id, route_pref, started_at, arrived_at, skipped[] )           -- 여정 상태
```
**불변식: 셰브런 위치·진행률은 저장하지 않는다** — 항상 `user_movies ∩ destination` 파생.
웹·앱·임포트 어디서 봐도 자동 전진(한 계정=한 기록 원칙의 완성). RLS deny-all + me_* RPC 패턴.

## §9. 단계

- **P0 기획 승인** — 이 문서, 오너 결정 §12.
- **P1 종이 프로토타입 ✅ SHIPPED (2026-07-26)** — `public/navigator-preview.html` → **metatake.net/navigator-preview.html**(noindex, JS-0 CSP-proof, 실 큐브릭 데이터). 3화면 CSS-only 전환(어디로/경로/주행). 주행 뷰=구글맵 형태(초록 다음-턴 배너·파란 경로·빨간 위치 화살표·포스터 표지판·하단 ETA/진행바). 자가검수 스크린샷 2회. **오너 감각 검증 대기 → OK면 P2.**
- **P2 목적지 카탈로그+경로 엔진** — 6과 중 **정전·감독**부터(진행률 데이터 최상) · 순수 함수+테스트.
- **P3 웹 주행 뷰** — /room/navigator + 룸 Tonight 미니카드.
- **P4 앱 이식** — Tonight 탭 통합(JS-only·OTA).
- **P5 오버뷰(오디세이 오버레이)·도착 카드·공유.**

## §10. 불변식 (처음부터 새기는 것)

1. **위치는 파생, 저장 금지** — 원장이 유일 진실.
2. **경로는 결정론(LLM-0)** — 같은 입력=같은 경로. 사유 칩도 서버 계산값만(가짜 사유 금지).
3. **ETA는 "이 페이스면"** — 단정 금지, 페이스 없으면 숨김.
4. **주행 뷰 순결** — 턴 카드·도로 리본·트립 푸터 외 무엇도 추가 금지(광고성 모듈 영구 금지).
5. **서버 HTML 비개인화** — 여정 상태는 클라 하이드레이션(기존 철칙).
6. **기존 지도 재사용** — 새 지도 실체 금지(용어 헌장·오디세이가 지도다).
7. **스킵=후미 이동** — 삭제·벌점 없음. 내비는 심판이 아니라 안내자.

## §11. 리스크와 정직한 미지수

- **목적지 크기 편차**: 100+ 정전의 구간 분할 편집 노동 필요(P2에서 상위 20개만 수작업 검수).
- **페이스 프라이버시 감각**: "당신 페이스면 11월"이 압박으로 읽힐 수 있음 — 톤을 안내로(넛지 알림은 기본 off).
- **runtime 결측**: Tier-2 일부 — 결측 시 편수만 표기(가짜 합계 금지).
- **주행 뷰의 참신함 = 학습 비용**: P1 종이 프로토타입에서 오너+지인 3인 반응 필수.

## §12. 오너 결정 (P0 게이트)

- [ ] **D1. 제품명**: The Navigator / 내비게이터 (대안: 로드무비·시네나비 — 추천은 Navigator)
- [ ] **D2. 진행 명사**: Drive("여정") 승인 (Journey는 Deck 소유라 회피)
- [ ] **D3. v1 목적지 2과**: 정전+감독부터 (추천안)
- [ ] **D4. 동시 여정 수**: 1개(추천 — 내비는 한 번에 한 목적지) vs 복수
- [ ] **D5. P1 종이 프로토타입 먼저** 승인 (바로 P2~3 직행 대안)
- [ ] **D6. 앱 우선인가 웹 우선인가**: 웹 먼저(P3→P4 순, 추천 — 검증 빠름) vs 앱 먼저

## §13. 개정 로그
- 2026-07-26 v1 — 최초 기획. 구글 Navigation SDK 문서 연구 기반 메타포 사전(§1) + 5화면 + 경로 엔진 + 불변식.
