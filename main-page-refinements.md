# 메인페이지 완성도 향상안 (디테일 폴리시) — metatake.net `/`

> **읽는 대상: 메인페이지를 손보는 다른 AI.**
> **전면 개편이 아니다.** 현재 메인은 v6 "The Pair" 설계가 실데이터로 잘 구현돼 있다(히어로 페어 회전,
> 10-페어 갤러리, 개념 사슬, 카운터, 살아있는 별자리, 4개 진입문, 매니페스토 — 모두 동작). 아래는 **그 위에
> 얹는 디테일 완성도(polish)** 항목이다. 레이아웃·구조·정체성(빨강 하나 · retrieved, not generated)은 그대로 둔다.
>
> 동봉 이미지: `main-page-refinements-visual.svg` (번호 ①~⑩이 이 문서의 R-항목과 일치).

## 0. 현재 상태 (2026-06 라이브 확인)
- 데이터가 실제로 커졌다: **935 meta-takes · 46,503 readings** (시안의 자리표시자 152/13,241을 대체). 규모 자체가 강점.
- 히어로 페어 = Dogville ⟷ Kill Bill: Vol. 1 *(via The Alienation Effect)* 처럼 **진짜 unlikely pair**가 회전 중.
- 카운터("THE MACHINE UNDERNEATH")가 스냅샷에서 0으로 잡힘 → **카운트업이 큰 수(46,503)로 안정 동작하는지 확인 필요**.

---

## P1 — 먼저 (체감 큰 폴리시)

### R1 · 10-페어 갤러리: 영화·메타테이크 중복 제거 (큐레이션)
- **현재:** 갤러리 10쌍에 **The Favourite가 3회**(⟷Loveless, ⟷Invisible Life, ⟷Dark Habits), Loveless 2회, The Grand Budapest Hotel 2회, **메타테이크 "The Emotional Landscape"가 2회** 등장.
- **문제:** "끊임없이 다양하게 많다"는 인상이 핵심인데 같은 영화/같은 메타테이크가 반복돼 다양성이 깎인다.
- **제안:** 10쌍 선정 시 **영화 중복 0, 메타테이크 중복 0**을 보장. 가능하면 (a) 서로 멀리 떨어진 장르/연대, (b) 인지도 높은 작품, (c) surprise(표면 거리)가 큰 쌍을 가중.
- **구현:** 페어 추출 쿼리에서 `DISTINCT` 보장 — 한 번 쓴 film_id·meta_take_id를 이후 후보에서 제외하며 10개를 채운다(greedy). 히어로 현재 페어와 갤러리도 중복 회피.

### R2 · "THE MACHINE UNDERNEATH" 카운터: 큰 수 안정 카운트업 + 천단위 구분
- **현재:** 스냅샷에서 6개 게이지가 0. 실제 타깃은 큰 수(46,503 등).
- **문제:** JS 전/스크롤 전에는 0으로 보일 수 있고, 6자리 수의 카운트업이 끊기거나 콤마가 없으면 가독성 저하.
- **제안:** ① 스크롤 진입 시 0→실제값 ease-out 카운트업, ② **천단위 콤마**(46,503), ③ 진입 전에도 0이 길게 노출되지 않도록(뷰포트 밖이면 정적, 들어오면 즉시 시작), ④ Takes/Nodes에 가끔 +1 라이브 틱으로 "살아있음" 유지.
- **구현:** `toLocaleString("en-US")` 포맷, `IntersectionObserver(threshold .4)`로 1회 발화, `data-target`는 서버 실카운트. **Nodes 값이 실제로 채워지는지 확인**(0이면 노출 말 것).

### R3 · 히어로 "VIA [figure]" 긴 텍스트 처리 + 빨강 선 정렬
- **현재:** via 문구가 길다(예: "The minimalist stage set (chalk lines, minimal props).").
- **문제:** 카드 폭을 넘기면 줄바꿈/넘침으로 좌우 카드 높이가 어긋나고, 가운데 **빨강 선이 메타테이크 중심과 어긋날 수** 있다.
- **제안:** via는 **최대 2줄로 클램프**(말줄임), 좌우 카드 상단 정렬 고정. 빨강 선(`placeBars`)은 메타테이크 제목 수직 중심에 재계산(리사이즈·회전마다). 회전 시 선이 **매번 다시 그어지도록** 유지.
- **구현:** `.fvia{-webkit-line-clamp:2}` + `align-items:flex-start`. 선 위치는 `mtTitle` 기준 `getBoundingClientRect`로(이미 패턴 있음).

### R4 · 첫 페인트(above-the-fold)에서 히어로·카운터가 비어 보이지 않게
- **현재:** 페어/카운터가 클라이언트 JS로 채워짐.
- **문제:** 느린 연결에서 히어로가 잠깐 비거나 카운터 0이 보일 수 있다(첫인상 손해).
- **제안:** 최초 페어 1쌍과 카운터 목표값을 **서버 렌더(SSR)로 미리** 넣어두고 JS는 회전/애니메이션만. 백드롭은 onload 페이드(깨진 이미지 방지 + 컬러 유지).

---

## P2 — 다음 (마감 품질)

### R5 · 갤러리 ↔ 히어로 상호작용 확정
- **제안:** 갤러리 박스 클릭 → 히어로 전시 박스에 로드 + active 동기화 + 부드러운 스크롤. 썸네일(TMDB) 페이드, 누락 시 엔티티색 폴백. 현재 히어로에 떠 있는 쌍은 갤러리에서 살짝 표시.

### R6 · 살아있는 별자리(WANDER THE MAP): "click to travel" 실제 링크 + 라벨 가독성
- **현재:** 노드에 실제 영화/메타테이크 라벨(The Favourite, The Alienation Effect …).
- **제안:** 노드 클릭 시 **실제 페이지로 이동**(film/take), hover 포커스 시 라벨만 또렷하게(겹침 방지: 라벨 길이 제한·충돌 회피), 드래그/줌 유지. 저사양 대비 노드 수 상한 + 화면 밖이면 rAF 정지.

### R7 · "JUST ADDED" 티커: 여러 항목 순환 + 엔티티 색
- **현재:** 스냅샷에 1개만(META TAKE Recurring Personal Motifs…).
- **제안:** 최근 추가분 **여러 개를 7초 순환**, 각 항목에 엔티티 색 태그(메타=빨강/트로프=틸/리딩=자주/감독=보라/컨셉=청록). 실제 최신 데이터에서.

### R8 · 진입문(START ANYWHERE) 카운트 ↔ 카운터 일치
- **현재:** "935 HUBS", "439 FIGURE-TYPES" 등.
- **제안:** 이 숫자들이 §R2 카운터와 **같은 소스**를 쓰게(불일치 방지). 회전 샘플은 실제 최신/대표값.

### R9 · 용어·하이픈 표기 통일
- **현재:** 네비 "Meta takes" / 카운터 "META-TAKES" / 사슬 "Meta-take" 혼재.
- **제안:** 표기 1개로 통일 — **하이픈 없는 "Meta takes"를 기본**(네비 기준), 문장 중 형용사일 때만 "meta-take". 라벨/카운터/사슬 모두 정렬.

---

## P3 — 마지막 손질 (접근성·성능·반응형)

### R10 · prefers-reduced-motion 존중
- 카운트업·페어 자동회전·별자리 애니메이션·선 그리기를 **reduced-motion에서 정지/축소**. 정적이어도 내용은 그대로 보이게.

### R11 · 접근성
- 백드롭 이미지 의미 있는 `alt`(또는 장식이면 빈 alt), `:focus-visible` 링·버튼 포커스 링, "Another" 버튼 `aria-label`, 뮤트 텍스트 대비(작은 11~12px 회색의 명도 점검).

### R12 · 반응형(모바일)
- 히어로: 두 카드 세로 스택 + 빨강 선은 **세로 커넥터**로 전환. 갤러리 2열, 카운터 3열. 긴 메타테이크 제목 줄바꿈 확인.

### R13 · 성능
- 별자리 캔버스는 **뷰포트 진입 시 init**, 화면 밖이면 rAF 정지. 백드롭 이미지 `loading="lazy"`/적정 사이즈(w342/w500). 폰트 `display=swap`(이미 적용).

### R14 · 정체성 가드(절대 깨지 말 것)
- 빨강은 하나(+트로프 틸)만. 이미지 컬러 유지. **첫 줄 "Built on AI embeddings — not AI-generated content"** 와 "retrieved, not generated" 메시지 유지. 페어는 **실제로 같은 메타테이크를 공유하는 두 영화**만(아무 둘이나 잇지 말 것).

---

## 한 줄 요약 (다른 AI에게)
구조는 그대로. **(P1)** 갤러리 중복 제거 · 카운터 큰 수 안정화 · via 클램프/선 정렬 · 첫 페인트 SSR → **(P2)** 갤러리·맵·티커·진입문 마감 → **(P3)** reduced-motion·접근성·반응형·성능. 정체성(빨강 하나·근거 기반)은 절대 유지.
