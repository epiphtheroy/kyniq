# ASK 메인서비스화 — 마스터 작업계획

> 작성 근거: 2026-06-19 설계 대화. ASK(Grounded RAG)를 Metatake의 **메인 서비스**로 끌어올리기 위한 품질·구조 작업계획.
> 이 문서는 여러 AI 에이전트가 **병렬로** 작업하고 결과를 합치기 위한 단일 기준점(single source of truth)이다. 모든 변경은 이 문서의 레인 규칙·완료기준을 따른다.

---

## 0. 목적과 제1원칙

**목적.** "자연어 영화/개념 질문 → 우리 코퍼스에서 검색 → 근거·인용 있는 비평적 답변"을 플래그십 품질로 만든다. 자체 모델 학습은 없다. 검색·재랭킹·생성·인용 품질을 구조적으로 끌어올린다.

**제1원칙(모든 결정의 판단 기준).** Metatake의 해자는 **검증 가능한 큐레이션 코퍼스에 그라운딩된 답변**이다. 모든 결정은 단 하나의 질문으로 판단한다 — *"이것이 그라운딩 약속을 강화하는가, 희석하는가?"*
- 강화: 더 나은 검색/재랭킹, 검색결과 1급 노출, 학술 API 보강, 라벨 분리된 매체 인용.
- 희석(금지): 열린 웹 검색을 해석 소스로 사용, 코퍼스 밖 사실 주입, 출처 미표기 합성.

**품질 공식.** `체감 품질 = 검색(retrieval) × 재랭킹(rerank) × 생성(generation)`, 그리고 이 곱을 **평가(eval)** 가 묶는다. 레버 크기 순서: 검색·코퍼스 > 재랭킹 > 질의이해 > 생성모델 > 임베딩모델 차원. **측정 없이는 개선도 없다 → eval이 1순위.**

---

## 1. 현재 상태 스냅샷 (검증된 사실 — 2026-06-19, 라이브 DB `kyniq`/jvgarcqrtsmgfimdcwgo)

작업 에이전트는 아래를 ground truth로 공유한다.

**파이프라인 (`app/api/ask/route.ts`)**
- 질문 → `embed(q)`: `api.openai.com/v1/embeddings`, `text-embedding-3-small`, 1536차원, 직접 fetch. in-memory 캐시 1h, 레이트리밋 12/min/IP(in-memory).
- → `supabase.rpc('ask_retrieve', {p_qvec(text), p_q, p_k:40})`: 벡터 60 + FTS 60을 **RRF(k=60)** 융합. `status='published'` 필터, FTS는 `'english'` 고정. meta join은 `status='published' AND kind='reading'`.
- → `diversify()`: 형상당 1개, 영화당 max 2, 14개 유지(후보 부족 시 backfill).
- → `openaiAdapter.call(gpt-4o-mini)`: 그라운딩 시스템 프롬프트, temp 0.2, maxTokens 750. `[n]` 인용 + `USED:` 라인.
- → `/ask` 페이지가 `[n]`을 실제 figure/take 링크로 렌더, Sources 목록 표시.
- 코퍼스 임베딩은 오프라인 `worker/mt-embed.py`(동일 모델, `bulk_set_embeddings` RPC).

**DB 실측**
| 테이블 | total | embedded | 비고 |
|---|---|---|---|
| takes | 26,297 | 18,004 (68%) | 전부 published. **8,293개(32%) 임베딩 누락**(최신분) |
| figures | 7,410 | 4,559 | HNSW 인덱스 보유 |
| meta_takes | 4,820 | 4,142 | published 709, HNSW 보유 |

**인덱스 불일치(중요).** 검색 주 대상인 `takes`는 `IVFFlat(lists=100, probes=10)`뿐. 정작 figures/meta_takes만 HNSW. → 벡터 레이어 창구에서 takes도 HNSW로.

**현재 진행 중(다른 에이전트).** 신규 영화 ~1500편 임포트 + 임베딩 작업 진행 중(3-small/1536). **이 작업이 끝나고 DB 스냅샷을 뜬 뒤에야 벡터 레이어를 만진다.**

---

## 2. 3-레인 모델 + 절대 규칙

작업은 서로 충돌하지 않는 3개 레인으로 분리된다.

### 레인 A — 로직 레인 (지금 진행 ✅)
검색된 행 "위에서" 도는 모든 것. 임베딩 차원·인덱스·코퍼스 크기와 무관하게 작동. → **W1, W2, W3, W4, W5.**

### 레인 B — 벡터 레인 (동결 🔒, 임포트 완료 후)
임베딩 모델·차원·인덱스·커버리지 등 벡터 표현을 바꾸는 모든 것. → **W6.**

### 레인 C — 외부 소스 레인 (설계는 지금, 빌드는 코어 로직 후, 법률 게이트 🔶)
학술 API + 매거진 allowlist + 인용 엔진. → **W7, W8.**

### 🚨 절대 규칙
1. **벡터 레이어 동결.** 임포트+임베딩이 끝나고 DB 스냅샷을 뜨기 전엔 임베딩 모델/차원/인덱스/backfill을 **건드리지 않는다.** (지금 바꾸면 1536/3072 혼재로 검색이 깨진다.)
2. **"모델 업그레이드" 용어 분리.** 생성 모델(gpt-4o-mini→프런티어)=레인 A, 지금 가능. 임베딩 모델(3-small→3-large)=레인 B, 나중.
3. **eval이 모든 변경의 회귀 게이트.** W1 없이 W2~W8 머지 금지.
4. **그라운딩 무결성.** 코퍼스 답변과 외부(학술/매거진) 결과는 **인용 스트림을 절대 섞지 않는다.** 시각·의미적으로 라벨 분리.
5. **파일 소유권 분리.** 임포트 에이전트=`worker/*`,`data/*`. 우리=`app/*`,`lib/*`,`components/*`,`supabase/migrations/*`(신규 번호만). 겹치는 파일 금지.
6. **임포트 중 라이브 품질로 최종 판단 금지.** IVFFlat 인덱스가 신규/전체에 recall 저하 상태 → 이 구간 수동 QA에 과튜닝하지 않는다.

---

## 3. 워크스트림 (병렬 단위)

각 워크스트림은 독립 에이전트/담당자에게 배정 가능. 형식: 목표 / 작업 / 의존 / 완료기준(Acceptance) / 레인·병렬.

### W1 — 평가 하니스 (최우선, 모든 것을 게이트)
- **목표.** 변경마다 "정말 좋아졌는가"를 숫자로 증명하는 측정자.
- **작업.**
  - 골드셋 50~100문항: 유형별 균형 — ① 넓은 개념("영화는 감시를 어떻게 그리나"), ② 특정 작품("X의 결말 의미"), ③ 다국어(한/영 동일 의도 쌍), ④ 코퍼스 밖(거절이 정답인 질문). 각 문항에 기대 읽기/관련성 판정 부여.
  - 지표: 검색 `recall@k`·`nDCG`; 답변 `faithfulness`(모든 주장 인용+근거 일치), `citation accuracy`(오귀속 0), `groundedness`(코퍼스 밖 사실 0), `refusal correctness`(없으면 없다고), `latency`, `cost`.
  - 하니스 스크립트: 골드셋에 파이프라인 실행→채점→리포트. **DB read-only.**
- **의존.** 없음(지금 시작).
- **완료기준.** end-to-end 1커맨드 실행 → 재현 가능한 스코어카드 산출. 단 **비교 baseline freeze는 임포트+임베딩 안정 후**(이전엔 코퍼스가 움직여 절대 수치 비교 불가).
- **레인·병렬.** A. 골드셋 문항 초안은 **리서치 에이전트 위임 가능**(에디토리얼 검수 필수).

### W2 — 질의 이해 레이어
- **목표.** 질문을 분류·정규화해 다운스트림 정책을 동적으로 결정. 외국어 문제 해결.
- **작업.**
  - 의도 분류: 넓은개념 / 특정작품 / 다국어 / 범위밖.
  - 다국어 정규화: 언어 감지 → FTS축용 번역/정규화(현 `'english'` 고정 문제 해결), 벡터축은 원문 유지.
  - (선택) 질의 확장으로 recall 보강.
  - 출력이 W3의 다양성 정책(per-film 캡)·레지스터 가중·FTS 언어를 구동.
- **의존.** W1(검증용).
- **완료기준.** 라벨링된 질의셋에서 분류 정확도 목표 달성, 다국어 질의 recall 유의미 상승.
- **레인·병렬.** A. W3/W4와 병렬 개발.

### W3 — 리랭커 (지금 도입하는 최대 단일 품질 레버)
- **목표.** RRF 융합 결과를 질문-문서 동시 판독으로 재정렬.
- **작업.**
  - RRF 융합 → **cross-encoder 재랭킹** → 관련성 하한선(threshold 미달 drop) → **동적 다양성 캡**(W2 의도가 구동: 넓은개념=캡 유지, 특정작품=캡 완화).
  - 후보 확대: 40 → 60~80(리랭커가 정렬하므로). 기존 휴리스틱 `diversify` 대체/보강.
  - 제공자 결정: 호스티드(Cohere Rerank / Voyage / Jina) vs 자체호스팅. **시작은 호스티드 권장.**
- **의존.** W1(baseline), W2(동적 캡 입력).
- **완료기준.** W1 골드셋에서 RRF-only 대비 `nDCG`/`recall` 유의미 상승, 레이턴시 예산 내.
- **레인·병렬.** A.

### W4 — 생성 모델 + 프롬프트
- **목표.** 합성 품질(그라운딩 준수·인용 정확·비교/긴장·산문 결) 상향.
- **작업.**
  - 후보 생성 모델 A/B(gpt-4o-mini → 프런티어)를 W1 faithfulness/품질로 비교.
  - 그라운딩 프롬프트 v2: 인용 계약 강화, 거절 행동, **인용(quotation) 규칙**(W8 대비: 짧게·출처표기·패러프레이즈 우선).
- **의존.** W1.
- **완료기준.** faithfulness ≥ baseline, 품질(LLM-judge/휴먼) 상승, 쿼리당 비용 예산 내.
- **레인·병렬.** A.

### W5 — 검색결과 1급 노출 (UI)
- **목표.** "retrieved, not generated" 브랜드를 시각화. 합성이 약해도 원본 읽기 목록 자체가 가치.
- **작업.** "답변 / 읽기" 모드 토글, 출처 전면 노출(Perplexity식). 기존 `citations` API 형태 위에서 동작.
- **의존.** 없음(현 API로 충분).
- **완료기준.** 디자인+배포, 기존 API 계약 유지.
- **레인·병렬.** A. 완전 독립 — 아무 때나.

### W6 — 재임베딩 & 인덱스 업그레이드 🔒 (임포트 완료 + DB 스냅샷 후)
- **목표.** 커버리지 100% + 검색 인프라 정합.
- **작업.**
  1. **DB 스냅샷/캡처 먼저.**
  2. 임베딩 모델 결정: 3-small(1536) vs 3-large(3072) vs 3-large@축소차원(MRL). **어차피 전수 재임베딩하므로 3-large 전환 한계비용≈0.** 단 쿼리 경로도 동일 모델로, 벡터 컬럼 차원 변경 동반. W1 검색지표로 결정.
  3. 커버리지 backfill: 모든 published 행 임베딩(8,293 누락 + 신규 1500편).
  4. 인덱스: `takes` IVFFlat→**HNSW**(figures/meta_takes와 정합), `ef_search`/`m` 튜닝.
  5. **다축 검색**: figures+meta_takes 임베딩을 `ask_retrieve`에 합류(코드는 레인 A에서 플래그 뒤로 미리 작성, 여기서 활성화).
- **의존.** 임포트+임베딩 완료, DB 스냅샷. W1(이후 baseline freeze).
- **완료기준.** published 임베딩 커버리지 100%; HNSW recall/레이턴시 벤치; 다축 합류 시 W1 상승.
- **레인.** B.

### W7 — 학술 보강 레이어 (외부 중 법적 리스크 최저 → 먼저)
- **목표.** 검증 가능한 학술 메타데이터로 "더 읽을거리/사실확인" 보강.
- **작업.** OpenAlex/Crossref/Semantic Scholar API 통합(제목·초록·저자·인용·DOI = 자유 이용). **스캐폴딩/“더 읽을거리”로만**, 코퍼스 답변과 라벨 분리·링크아웃.
- **의존.** W1(groundedness 회귀 감시).
- **완료기준.** 학술 결과 라벨 분리 + 링크아웃, 그라운딩 인용 스트림 비혼합, eval에서 groundedness 무회귀.
- **레인·병렬.** C. 지금 설계·착수 가능(법적 리스크 낮음).

### W8 — 매거진 allowlist + 인제스트 + 인용 엔진 (최고 가치, 최고 법적 주의)
- **목표.** 사전 등록한 비평지에서 **짧은 인용 + 링크아웃**으로 답변 권위 강화. 응답 본문에 기사 문구를 합법적으로 녹임(인용/제28조 비평 목적).
- **작업.**
  - **W8a 등록 레지스트리 + 연락처 DB.** 관계형 2-테이블: ① outlets(매체 메타데이터 + 크롤봇 입력 필드: sitemap_url·render_mode·robots_crawl_delay 등) ② contacts(매체당 다수 제휴/마케팅 이메일 + 인물 정보). → **목표 ~200곳(품질 우선, 억지 패딩 금지) 조사를 리서치 에이전트에 위임**(별도 브리프 + 2 CSV 템플릿). **전수 전 5행 파일럿으로 크롤봇 입력에 스키마 1회 검증 후 확정**(포맷 재작업 0). 크롤 대상은 법률 게이트 통과한 부분집합만.
  - **W8b 인제스트 파이프라인.** robots.txt/옵트아웃 준수, **적법 접근만**(페이월 우회 금지), 신규분 RSS 증분, **전문 대신 임베딩 + 짧은 스니펫만 저장**, dedup, 신선도.
  - **W8c 인용 엔진 + 가드레일.** ① 출처당 인용 길이 리미터(1~2문장) ② verbatim n-gram 중첩 가드(초과 시 컷/재생성) ③ 인용 결속(오귀속 0) ④ 누적 재현 방지 dedup ⑤ 프롬프트 계약(짧게·출처표기·패러프레이즈 우선) ⑥ 라벨 분리 렌더 + 출처·링크.
  - **W8d 법률 게이트.** 한국 IP 변호사가 임계치(인용 길이, 답변 내 인용 비율, 접근 규칙) 확정. 핵심 매체엔 제휴/이용허락 타진.
- **의존.** W1, W4(인용 프롬프트), **W8d 변호사 사인오프**(go-live 게이트), 코어 로직 레인 완료 후 빌드.
- **완료기준.** 변호사 임계치 사인오프; 가드레일 적대적 테스트 통과(장문 재현 유도 불가); 모든 인용 = 출처표기+링크+적법 수집.
- **레인·병렬.** C. **W8a(매거진 조사)는 코드/법률 블로커 없이 지금 즉시 위임 병렬 착수 가능**(단 인제스트 W8b는 게이트 후).

---

## 4. 병렬화 & 의존성 맵

```
지금 즉시 (병렬):
  ├─ W1 평가 하니스 ........... [코어 게이트, 먼저]
  ├─ W2 질의 이해 ............. (W1로 검증)
  ├─ W3 리랭커 ................ (W1 baseline 후 검증, 개발은 병렬)
  ├─ W4 생성모델/프롬프트 ...... (W1로 검증)
  ├─ W5 검색결과 UI ........... [완전 독립]
  ├─ W7 학술 API .............. [리스크 낮음, 설계·착수]
  └─ W8a 매거진 조사 .......... [리서치 에이전트 위임 — 즉시 병렬]
        + 변호사 자문 착수(W8d 준비)

── Gate 1: 임포트+임베딩 완료 → DB 스냅샷 ──
  └─ W6 재임베딩/HNSW/다축/커버리지100% → W1 baseline freeze

── Gate 2: 변호사 임계치 사인오프 ──
  └─ W8b 인제스트 → W8c 인용엔진 go-live

상시: W1 eval = 모든 머지의 회귀 게이트
```

**핵심 병렬 통찰.** 코어 품질 작업(W1~W5)과 매거진 조사(W8a)·학술(W7)은 서로 막지 않는다. 매거진 100개 조사는 *지금 바로* 다른 에이전트에 위임해 코어 개발과 동시에 진행 → Gate 2가 열릴 때 인제스트 즉시 시작 가능.

---

## 5. 마일스톤 & 게이트

| 마일스톤 | 내용 | 게이트 조건 |
|---|---|---|
| M1 | W1 하니스 + 잠정 baseline | 1커맨드 스코어카드 |
| M2 | W2+W3+W4 머지 (검색·재랭킹·생성 v2) | W1에서 baseline 대비 상승 |
| M3 | W5 UI 배포 | 기존 API 무파손 |
| M4 | W7 학술 보강 | groundedness 무회귀 |
| M5 | W6 벡터 레인 (커버리지100%+HNSW+다축) | 임포트 완료+스냅샷 |
| M6 | W8 매거진 인용 go-live | 변호사 사인오프 + 가드레일 통과 |

---

## 6. 리스크 레지스터

| 리스크 | 영향 | 완화 |
|---|---|---|
| 벡터 레이어를 임포트 중 변경 | 1536/3072 혼재로 검색 붕괴 | 절대규칙 #1: 동결, Gate 1 후에만 |
| eval 없이 튜닝 | 좋아졌는지 모름, 회귀 누락 | W1 최우선, 모든 머지의 게이트 |
| 외부 결과와 코퍼스 혼합 | 환각·보이스 희석·신뢰 훼손 | 인용 스트림 분리·라벨링(규칙 #4) |
| 매거진 전문 저장/장문 재현 | 저작권 침해(한국 TDM 면책 부재) | 임베딩+짧은스니펫만, 인용 가드레일, 변호사 게이트 |
| LLM 오귀속/과다 인용 | 신뢰·법적 노출 | 인용 결속 + n-gram 중첩 가드 + 길이 리미터 |
| 임포트 중 라이브 QA 과튜닝 | 사라질 상태에 최적화 | 규칙 #6: 안정화 후 최종 판단 |
| 파일 충돌(임포트 에이전트와) | 머지 충돌·롤백 | 규칙 #5: 소유권 분리 |
| 리랭커 레이턴시 | 응답 지연 | 후보 수·모델 선택으로 레이턴시 예산 관리 |

---

## 7. 사용자(wonwoo) 결정 필요 항목

작업 전/중 확정 필요. 권장안 병기.

1. **생성 모델 타깃·예산** — 어떤 프런티어 모델, 쿼리당 비용 상한? (권장: W1에서 mini vs 프런티어 A/B 후 결정)
2. **재임베딩 모델** — 3-small 유지 vs 3-large 전환? (권장: Gate 1에서 3-large를 W1 지표로 평가 후 전환; 한계비용≈0)
3. **리랭커 제공자** — 호스티드(Cohere/Voyage) vs 자체호스팅? (권장: 호스티드로 시작)
4. **골드셋 소유** — 에디토리얼 직접 vs 리서치 에이전트 초안+검수? (권장: 에이전트 초안 + 당신 검수)
5. **변호사 자문 착수 시점** — W8 go-live 게이트. (권장: 매거진 조사와 동시 착수)
6. **매거진 조사 위임** — 지금 리서치 에이전트에 발주? (권장: 즉시, 첨부 브리프+CSV로)

---

## 8. 파일 소유권 맵 (충돌 방지)

| 영역 | 소유 | 비고 |
|---|---|---|
| `worker/*`, `data/*` | 임포트 에이전트 | 우리 편집 금지 |
| `app/api/ask/*`, `app/ask/*` | 로직 레인(W2~W5) | |
| `lib/*`(리랭커·질의이해 모듈) | 로직 레인 | 신규 파일 위주 |
| `components/*`(ASK UI) | W5 | |
| `supabase/migrations/00XX_*`(신규 번호) | W6, W8a | `ask_retrieve`는 CREATE OR REPLACE만. **동결 기간 중엔 1536 계약 유지**; 차원 변경(3-large/3072)은 W6에서 컬럼·함수 캐스트(`::vector(N)`)·쿼리 경로를 **한 번에** 바꿀 때만 |
| 외부소스 모듈(신규 `lib/sources/*`) | W7, W8 | |
| eval 하니스(신규 `eval/*`) | W1 | DB read-only |

---

## 부록 A — 위임 산출물
- `매거진-리서치-에이전트-브리프.md` — ~200개 매거진 조사 태스크 명세 + 2-테이블 스키마 + 5행 파일럿 게이트.
- `magazine-allowlist-template.csv` — 매체(outlet) 테이블 템플릿(크롤봇 입력 필드 포함).
- `magazine-contacts-template.csv` — 연락처(제휴/마케팅 이메일 + 인물) 테이블 템플릿.
