# HANDOFF · 한국어 앱 — 구독 토큰 번역 실행 런북

> **정본.** 모바일 완전 한국어화를 위한 프로즈 번역을 **API 키 없이 구독 토큰(`claude -p` 헤드리스 + 세션 내 에이전트)**으로
> 수행하는 실행 지시서. 상위 정본: `HANDOFF-한국어화-i18n-마스터.md`(전략·용어집·자율 루프 설계) ·
> `HANDOFF-다국어프로젝션.md`(프로젝션 구조). 오너 지침 근거: "자동화 LLM은 API 키 금지·구독 CLI"(2026-08-03).
> 작성 2026-08-05.
>
> **선례**: `content_i18n`의 기존 21,561행(~230만 자)이 정확히 이 방식(에이전트 세션 내 번역 → JSON 배치 →
> `scripts/load-content-i18n.mjs` 적재)으로 2026-07-17에 만들어졌다. 검증된 공정이다.

---

## 0. 판단 요약 (2026-08-05 실측)

- **물량**: 모바일 화면이 렌더하는 미번역 프로즈 ≈ **290만~320만 자(영문)** ≈ 입력 ~0.75M 토큰, 한국어 출력 ~1.4M 토큰.
  QA·재번역 여유 포함 **총 ~2.5M 출력 토큰 + ~1M 입력 토큰** 예상.
- **최근 소비 실측**(로컬 JSONL 집계, `scratchpad/usage_scan.py` 방식): 최근 주간 출력 5.5~7.5M 토큰/주 수준으로 상시 운영 중.
  이번 주(08-03~05) 이미 출력 ~5.4M. **번역 전체 = 현재 기준 "하루치 강도" 수준** → 구독 한도 내 커버 가능 판단.
  단, 주간 한도의 절대값은 비공개(% 게이지만 존재) → **런 시작 전 오너가 `/usage` 확인이 캘리브레이션 게이트.**
- **분산 원칙**: 이번 주 잔여일 저녁 + 필요 시 다음 주 초까지 3~5회 런으로 분산. 대화형 작업과 한도 공유하므로
  주간 작업 시간대 회피.

### 하드 룰
1. **API 키·Batch API 금지** — 전량 `claude -p`(구독) 또는 세션 내.
2. **모델 = Opus** (`--model claude-opus-5`). Fable 금지(오너 자동화 지침). effort는 최저로(번역에 깊은 추론 불필요 — 출력비 레버).
3. **extra usage(초과분 재과금) OFF 확인** — 켜져 있으면 한도 초과분이 재과금된다(오너가 콘솔에서 확인). 월 지출한도에 걸리면 중단·리셋 후 재개.
4. **원본 테이블 무변경** — 쓰기는 `content_i18n` upsert만. **이번에는 `source_sha256` 반드시 채운다**(기존 21,561행의 결함 — 0행 채워짐 → staleness 감지 불능).
5. **서비스롤 적재는 오너 `!` 실행** (에이전트 프로덕션 쓰기 차단 — 기정사실). 에이전트는 JSON 배치 생성까지.
6. iOS·Android 공통화의 열쇠 = **번역을 앱 번들이 아니라 DB에** 둔다. 두 플랫폼 모두 같은 BFF/RPC를 읽으므로 데이터만 채우면 동시 적용. 앱측 배선 변경은 `eas update` OTA(앱 2회 재실행 함정 유의)로 양 플랫폼 동시 배포.

---

## 1. 번역 대상 — 실측 확정 (2026-08-06 추출 완료, `data/i18n/src2/`)

| 코퍼스 | entity_type / field | 행수 | 영문 분량 | 비고 |
|---|---|---:|---:|---|
| `tow_segments` | `tow_segment` / text | **1,507** | **129,172** | ⭐ §1.1 — 6,837행이 1,507조각으로 접힘 |
| `portrait` | `director_portrait` / body | 219 | 267,357 | |
| `dfacts_items` | `director_fact` / fact | 3,762 | 804,527 | `entity_key = <slug>#<n>` |
| `dfacts_intro` | `director_fact` / intro | 211 | 57,318 | |
| `dfacts_meaning` | `director_fact` / name_meaning | 211 | 57,801 | 한자 병기 최적층 |
| `repolish_invitation` | `invitation` / rationale | 1,898 | 2,336,644 | R1 재집필 |
| `repolish_laconic` | `trope` / laconic | 4,710 | 298,572 | R1 재집필 |
| `repolish_trope_title` | `trope` / title | 4,710 | 177,656 | R1 재집필 |
| **합계** | | **17,228** | **4.13M 자** | 조립 후 실제 렌더 행 = 22,558 |
| — | Tonight 추천 사유 칩 | — | **0** | 생성기 i18n(LLM 0) |

영화 제목은 **번역 금지** — `title_ko` 오버레이/영어 폴백. 브랜드 고유명 영문 유지.
to.W `verdict_label`(5종)·`authority_label`(4종)은 닫힌 enum → DB 아닌 **앱 사전**(`mobile/src/i18n/tokens.ts`).

### 1.1 ⭐ to.W는 프로즈가 아니라 템플릿 조립물 (파일럿에서 발견)

행 단위로 번역하면 **같은 영어가 매번 다른 한국어로 렌더된다** — 파일럿에서 동일 원문
`Optional viewing (…)`이 "보고 싶으면 봐도 좋다" / "굳이 챙겨 볼 필요는 없다" / "선택 관람"
3가지로 갈렸다. 다양성이 아니라 **결함**이다.

실측: 6,837행 → **고유 조각 1,507개**(한 조각 최대 2,426회 재사용). 마스터 문서 계층 B의
"생성기 국제화"로 처리한다 — `scripts/i18n-tow-segments.mjs`:
- `split` — 무손실 토크나이저(` · ` + 문장 경계 보존)로 조각 추출 + 조립맵. **분량 89% 절감**.
- `assemble` — 번역 조각을 원 구분자로 재조립 → 6,837행. 동일 영어 = 동일 한국어가 **구조적 보장**.

⚠️ `tow_segment`는 **적재 대상 아님**(빌드 재료). DB에는 조립본 `tow_comment`만 넣는다.
⚠️ 다른 코퍼스는 중복률 0~2%라 이 기법이 불필요하다(감사 완료) — to.W만 템플릿이다.

## 2. 공정 — 구축 완료 (에이전트 = 공정관리자 + 품질관리자)

| Phase | 도구 | 상태 |
|---|---|---|
| A 추출 | `worker/i18n-extract.py --corpus all` (Management API·페이지네이션 400행) | ✅ |
| A' to.W 분해 | `scripts/i18n-tow-segments.mjs split` (무손실 검증 내장) | ✅ |
| B 번역 | `scripts/i18n-translate-run.mjs` → `claude -p --model claude-opus-5 --allowed-tools ""` | ✅ 실행중 |
| C 검증 | 드라이버 인라인 lint(전행) + 실패 시 **문제 목록을 붙여 최대 3회 재시도** | ✅ |
| C' 조립 | `scripts/i18n-tow-segments.mjs assemble` | ✅ |
| D 감사 | `scripts/i18n-audit-ko.mjs` — 번역투·종결분포·첫머리 n-gram·문장길이 변동계수·병기/대시 | ✅ |
| E 적재 | **오너 `!`**: `zsh scripts/i18n-load-all.sh` → `node scripts/i18n-verify.mjs` | 대기 |
| F 배선 | BFF locale 관통·모바일 봉인해제·토큰 사전·폰트(§2.1)·검색 자모 가드 | ✅ (배포는 iOS 심사 후) |

### ⚠️ 적재 전 필수 — 원장을 믿지 마라

```
node scripts/i18n-completeness.mjs
```

원장(`ko-run-ledger.jsonl`)은 "배치가 끝났다"를 기록할 뿐, **출력 파일에 그 키가 실제로 있는지는 모른다.** 둘은 갈린다:

- **파일명 충돌(2026-08-06 실사고)** — 배치 인덱스는 재개할 때마다 0부터 다시 매겨졌고 파일명이 인덱스뿐이었다.
  두 번째 런이 첫 번째 런의 `00000.json`을 덮어썼고, 원장은 그 키들을 done으로 남겨 **재개가 영원히 건너뛰었다.**
  실측 소실: 인바이테이션 **586**, 감독 팩트 **120**. 진행률은 그동안 "70%"라고 보고하고 있었다(실제 39%).
  ✅ 수정: 파일명에 `RUN_ID`(커밋 `7255e4ca`). 로더가 PK로 접으므로 파일이 늘어나는 건 무해하고, 충돌은 치명적이다.
- 모델이 항목을 누락한 배치, 빈 문자열 출력 등도 같은 방식으로 갈린다.

**소실분 복구는 일반 재개로 안 된다**(원장이 done으로 안다). 누락 키를 `data/i18n/requeue/<corpus>.json`에
`[{"entity_key": "..."}]` 형태로 넣고 `--requeue`로 돌려야 한다.

**운영 교훈**(2026-08-06 실런):
- 러너는 **`nohup` 필수**. Bash 백그라운드 작업으로 띄우면 수명이 끝날 때 killed 된다(실제 발생, 01:18).
  원장 덕에 손실 0 — 재개 시 `resume: N already done`으로 건너뛴다.
- 감사 임계 교훈 3가지: ① 템플릿 코퍼스(to.W)는 첫머리 반복이 **정상**이므로 예외 ② 단조 판정은 절대값이 아니라
  **원문 CV 대비 85%** (제목 EN cv 0.15 < KO 0.22 — 한국어가 더 다양) ③ 메타발화 규칙은 콜론을 요구할 것
  (트로프 "번역의 실패는…"이 오탐).

**실측 파라미터**(2026-08-06):
- `--allowed-tools ""`가 결정적 — 툴 정의를 빼면 호출당 **캐시 쓰기 0**(31k 캐시 읽기만). 안 빼면 매 호출 19k 캐시 쓰기.
- 처리량 **221건/분**(conc 4). tow_segments 1,507건 = 6.1분·출력 97k 토큰.
- 청크: 짧은 항목 60~100 / 중간 40 / 장문 8. 호출 지연이 지배적이라 소형 코퍼스는 청크를 키우는 게 이득.

- **원장**: `data/i18n/ko-run-ledger.jsonl` — 배치 단위 상태(`extracted→translated→linted→qa_ok→loaded`),
  키 단위 완료 집합. **모든 런은 원장 기준 멱등 재개**(중단·한도 도달·세션 교체 무관).
- **드라이버**: `scripts/i18n-translate-run.mjs`. 킬스위치 = `touch data/i18n/.stop`. 전량 재개형.
- **시스템 프롬프트**: `data/i18n/prompts/style-ko.md`(문체 헌장) + 드라이버 내 `GOLDEN`(교정 예시) +
  `VOICE[corpus]`(코퍼스별 보이스). 세 조각이 호출마다 동일 프리픽스라 캐시에 얹힌다.

### 페이싱 규칙 (구독 한도 관리)
- 런 단위 = **~50만 자(약 60~80배치, 출력 ~40만 토큰)**. 런 사이에 오너 `/usage` 확인.
- 게이지 주간 80% 도달 시 중단 → 다음 주 리셋 후 재개(원장 재개).
- 대화형 세션과 동시 사용 회피(저녁/야간 런 권장). hourly 파이프라인도 같은 구독 소비 중임을 계산에 포함.

### 2.1 🚨 폰트 — 안드로이드 두부(□) 리스크 (2026-08-06 발견·조치)

번들 폰트 **Inter·PT Serif 둘 다 한글 글리프가 없다**(TTF cmap 직접 검증: U+AC00·U+D55C 부재).
- **iOS**: CoreText가 시스템 서체로 자동 대체 → 렌더됨(서체 혼용은 발생).
- **Android**: 커스텀 Typeface가 지정되면 대체가 신뢰할 수 없다 — RN의 고전적 **두부(□)** 사례.

조치: `mobile/src/theme.ts`의 `font.*`를 **로케일 인식 getter**로 전환. ko/ja/zh에서는 `fontFamily`를
`undefined`로 반환해 플랫폼 기본 서체(Apple SD Gothic Neo / Noto Sans CJK)가 잡도록 한다.
모듈 스코프 `StyleSheet.create` 캡처가 0건이라 getter로 안전(전 사용처 21곳 렌더 시점 평가).

⚠️ **실기기 미검증** — 에이전트가 기기에서 확인할 수 없다. 안드로이드 실기기 한국어 확인이 출시 전 필수.
⚠️ **대가**: 한국어 사용자는 브랜드 서체(PT Serif)를 잃는다. 둘 다 지키려면 한글 지원 서체
(Pretendard·Noto Serif KR)를 번들해 `theme.ts`에 이름만 넣으면 된다 — **양 바이너리에 수 MB 추가라 오너 결정**.

## 3. DB 관리 계약
- `content_i18n`에 **가산적 upsert만**. 신규 entity_type 4종은 기존 PK·인덱스·RLS 그대로 사용(마이그 불필요).
- `source_sha256` 전행 기록 → 마스터 §6 리컨실러 최소판(주기 크론: sha 불일치·신규 감지 → 재번역 큐)이 이 데이터로 가동 가능해짐.
- 적재 후 검증: 엔티티별 행수 = 원장 대비 100%, 무작위 10행 원문 대조, anon select 200 확인.
- 롤백 = entity_type 단위 delete(가산 테이블이라 안전). 원본 테이블은 어떤 경우에도 무변경.

## 4. 오너 결정 — ✅ 4건 전부 확정 (2026-08-06)
1. ✅ `UI_LOCALE` 봉인 해제 — **기기 언어 자동 + 설정 토글**(기본값=기기 언어, 설정>언어에서 변경 가능).
2. ✅ 내비게이터 영어 불변식 **앱 내 해제**(T4 번역 진행). 웹 /room/navigator는 불변식 유지.
3. ✅ T1 범위 = **앱 카탈로그 교집합**(~7천 행).
4. ✅ 다음 주 분산 **허용**.

잔여 오너 게이트는 §5.4 파일럿 30건 검수 하나뿐.

## 5. 문체 전략 — "번역이 아니라 한국어 산문" (2026-08-06 추가)

> 배경: 기존 content_i18n 번역(07-17, 21,561행)에 대한 오너 불만 — "정이 없이 딱딱하다." 실측 진단 결과
> 원인은 모델 성능이 아니라 **지시의 결함**: "정확히 번역하라"고만 했지 "한국어 산문으로 쓰라"고 하지 않았고,
> 표본·오너 게이트 없이 대량 런을 돌렸다.

### 5.1 실측 진단 — 번역투 4대 증상 (재발 방지 블랙리스트의 근거)
| 증상 | 실례 (현행 프로덕션) |
|---|---|
| ① 영어 피동·명사화 직역 | "**살아진 삶** 곁에 촬영된 살지 않은 삶" · "존속이 … **매입된다**" · "낡음, **귀먹음**, 자기기만" |
| ② 영어 통사 보존 — 거대 관형절·대시·콤마열 복제 | "~로 자리매김한, ~ 고바야시 마사키(1916년생)**는,** 여기서…" (한 문장 130자 선행수식) |
| ③ 문체 혼재 | 같은 invitation 코퍼스 안에 평어체(samurai-rebellion "~응축한다")와 합쇼체(saltburn "~심화합니다") 공존 |
| ④ 리듬 부재 | ~다 단조 반복, 문장 길이 무변주 |

### 5.2 문체 정본
- **원칙: 트랜스크리에이션** — 의미·사실 충실 + 문장은 한국어로 다시 쓴다. 문장 분할·병합·어순 재배열 자유.
  "영어 문장 구조가 비치면 실패"가 판정 기준. 소리 내어 읽어 걸리면 다시 쓴다.
- **기본 문체 = 평어체(~다)**, 한국 영화비평 산문 관례(씨네21 지면 기준). 종결 변주 필수(~다/~는 것이다/명사 종결/드물게 의문).
- **콘텐츠별 보이스**: laconic=아포리즘(짧고 칼같이, 명사 종결 허용) · invitation/포트레이트=에세이(호흡 길되 관형절은 분할) ·
  **to.W 코멘트=큐레이터의 온기**(사람이 말을 거는 톤 — "정" 불만의 핵심 수신처) · UI 라벨=기존 간결체 유지.
- **블랙리스트(기계 검사 병행)**: ~되어진다 · ~에 의해 · 피동 과잉(살아진) · 대명사 직역(그것/이것 남발) ·
  선행 관형절 40자 초과 · 대시(—) 남용 · 명사화 3연발 콤마열 · 합쇼체 혼입(~습니다 검출) · 번역문 티 나는 "혹은".
- 브랜드 고유명 영문 유지·영화 제목 title_ko 오버레이 원칙은 §1과 동일.

### 5.3 공정 반영 (Phase B·D 개정)
- **황금 표본 few-shot**: 유형별 3~5쌍(나쁜 직역→좋은 산문 대조)을 시스템 프롬프트에 고정. §5.1 실례를 교정한 쌍으로 시작.
- **2패스**: 고노출 프로즈(T1 to.W·T2·T3·invitation)는 번역→**윤문 패스**(원문 없이 한국어만 보고 산문으로 다듬기) 2단.
  라벨류(T4)는 1패스. 구독 토큰이라 실비 증가 없음(출력 ~1.6배).
- **QA 루브릭 v2**: 정확성 + **자연스러움·문장미 축 신설** — "씨네21 지면에 실려도 어색하지 않은가" 기준, 블랙리스트
  기계검사는 전행, LLM 채점은 표본 8%. 문체 혼재는 기계검사로 전행 탐지.
- **R1 재윤문 웨이브(신규)**: 불만 나온 기존 코퍼스도 새 문체로 재작업 — laconic 14.4만 자 + invitation 117만 자
  우선(합쇼체 혼입분 최우선), taxonomy definition 34만 자 후순위. content_i18n upsert라 안전. 총 +130만~165만 자.

### 5.4 파일럿 게이트 — ✅ 완료 (오너 위임, 2026-08-06)

오너가 파일럿 검수와 이후 지시까지 에이전트에 위임했다. 30건(to.W 10·invitation 10·laconic 5·portrait 5)을
번역해 원문과 대조 검토했고, **결과로 3가지를 고쳤다**:

1. **to.W가 템플릿임을 발견** → §1.1 세그먼트 파이프라인으로 전환(같은 영어가 3가지 한국어로 갈리던 결함 제거).
2. **헌장에 사실 추가 금지 규칙 신설** — 모델이 `Oscar-nominated author`를 "오스카 **각본상** 후보"로 확장했다.
   사실이지만 원문에 없다. §1의 두 번째 불릿이 이 사례에서 나왔다.
3. **laconic 보이스 조정** — "정의문이지 주장문이 아니다" 추가(원문이 명사구면 명사구 종결 우선).

**to.W 보이스 = 담백 확정(오너).** 단 담백 ≠ 건조 — 딱딱함 불만의 재발 금지, "덜 꾸미되 온기는 남긴다."
**병기 정책 = 오너 추가 지시** → 헌장 §5(핵심 용어 한정·항목당 2회·첫 등장 1회·일상어 금지, 기계 검사).

### 5.4.1 ✅ 결과 검증 — 구본 vs 신본 A/B 채점 (2026-08-06)

같은 항목 14쌍을 동일 루브릭으로 채점(`scripts/i18n-qa.mjs` 기준):

| 축 | 구본(07-17) | 신본 | 변화 |
|---|---:|---:|---|
| 정확성 | 4.50 | 4.50 | — |
| 용어 | 4.93 | 4.86 | −0.07(오차범위) |
| **산문미** | **3.07** | **4.36** | **+1.29** |

**진단이 옳았음이 수치로 확인된다.** 7월 번역은 **틀린 게 아니라 딱딱했다** — 정확성 4.50은 그대로인데
산문미만 3.07("읽히지만 번역 티")이었고, 그것이 오너 불만의 정체다. 신본은 4.36("자연스럽다"~"지면에 실려도 좋다")
이면서 정확성을 하나도 잃지 않았다 — 의미를 희생해 얻은 유창함이 아니다.
장문(portrait) 독립 채점도 정확 4.45 · 용어 4.73 · 산문 4.45로 동일 수준.

### 5.5 반복 편향(mode collapse) 방어 — 오너 우려(08-06) 반영
고정 few-shot이 수만 행에 같은 문형·리듬·틱을 찍어내는 위험은 실재한다. 단 번역은 자유 생성과 달리 **행마다 다른
원문에 앵커**되므로 내용 수렴 위험은 낮고, 위험은 **종결어미·문장 첫머리·연결어·상투구**에 집중된다. 4중 방어:
1. **표본의 역할 = 변환 규칙 교육, 문체 템플릿 아님.** 대조쌍은 "무엇을 고치는가"(피동 제거·관형절 분할)를 가르치게
   구성하고, 표본 자체를 구조적으로 이질하게 섞는다(긴 호흡/짧은 호흡·명사 종결/동사 종결·장르·시대 분산).
   파일럿 30건 전부를 표본화하지 않는다 — 결함 커버리지 기준으로 12~20건만 선별.
2. **표본 로테이션**: 승인 표본 풀(~20쌍)을 3개 서브셋으로 나눠 배치마다 교대 주입. 서브셋 내 프리픽스는 고정해
   프롬프트 캐시 이득 유지(런 단위 캐시, 호출 단위 파괴 금지).
3. **코퍼스 레벨 다양성 감사(기계, 전행)**: 런 종료마다 종결어미 분포·문장 첫머리 n-gram 빈도·문장 길이 분산·
   상투구 카운트("~에 다름 아니다"류 틱)를 집계. 임계 초과 패턴(예: 동일 첫머리 >3%)은 해당 행만 "변주" 지시로
   재번역 큐. 특히 **laconic은 리스트 표면에 여럿이 나란히 렌더**되므로 단조가 바로 보인다 — 감사 1순위.
4. **윤문 패스에 앵티-수렴 지시**: 인접 배치를 함께 보여주고 "같은 종결 3연속 금지·리듬 변주"를 명시.

주의: 보이스의 **일관성 자체는 자산**이다(한 큐레이터가 쓴 것처럼 읽히는 것). 방어 대상은 기계적 틱이지
일관된 목소리가 아니다 — 다양성 감사의 임계값을 과하게 조이지 말 것.

---

## §6 앱 QA로 드러난 것 (2026-08-06 저녁)

번역은 다 있었다. **앱이 잘못된 키로 물어보고 있었다.**

### 6.1 코퍼스마다 다른 `entity_key` 모양 — 실패가 완벽한 화면으로 위장한다

`content_i18n.entity_key`는 코퍼스마다 다르다. **`lineage_list`와 `frame`만 영어 라벨**이고 나머지 15개는 슬러그다.
앱은 전부 슬러그로 물었고, 조회는 아무것도 못 찾고 **폴백을 반환**했다. 폴백은 정확한 영어다. 그래서 화면이 멀쩡하게
렌더됐고, 몇 시간 동안 아무도 몰랐다.

앱에 `LABEL_KEYED` 집합(`mobile/src/lib/dbLabels.ts`)으로 못 박아 뒀다. 다음에 배선하는 사람이 먼저 읽는다.

### 6.2 memo 식별자 버그
`useDbLabels`가 돌려주는 lookup의 memo deps에 `tick`이 없어서, 행이 도착해도 **그 lookup 아래에서 memo하는
호출자는 영원히 영어**였다. Explore 검색 예시가 바로 옆 카드와 언어가 달랐던 이유.

### 6.3 프로젝션의 자리 = 서버 (⚠️ 배포 순서 의존)
앱 산문(Invitation·감독 초상/이름뜻/intro/facts·Tonight 리드)은 **BFF가 서버에서 프로젝션**한다
(`lib/i18n/appProjection.ts`). 화면당 배치 1회 + CDN 캐시(s-maxage 900) → 영화당 15분에 1회.
엣지 조회는 독자당·오픈당 PostgREST 직격이라 어제 두 번 쓰러진 DB에 놓을 수 없다.

앱의 엣지 조회는 **BFF가 안 싣는 것만** 남겼다: lineage_list 라벨(5화면), to.W 코멘트(별도 RPC),
director_pick 이유.

🚨**따라서 앱 OTA는 웹 배포보다 먼저 나가면 안 된다.** 순서: `main` 배포 → 확인 → `eas update`.
웹이 먼저 안 나가면 그 화면들이 영어로 되돌아간다.

### 6.4 `director_pick/reason` 1,033건 (신규)
"어디서 시작할까"의 추천 이유. 위반 0 · QA 정확 4.61 / 용어 4.80 / 산문 4.60 · 미달 0 · 적재 1,033/1,033.

🚨**키는 `<감독>#<영화슬러그>`다. `#<pos>`가 아니다.** 오종·키에슬로프스키·알모도바르 **세 감독이 픽 세트를
두 벌씩** 갖고 있어(같은 pos, 다른 영화) 26행이 서로를 덮어썼다. 《귀향》의 이유가 《신경쇠약 직전의 여자들》 밑에
붙을 뻔했다. 적재 전 검증: 1,033행 / (director_slug, film_slug) 고유 1,033 / 충돌 0.

⚠️**중복 픽 세트 자체는 데이터 문제다** — 이 세 감독은 지금 앱에서 픽이 두 번씩 보인다. 모든 언어에서.

### 6.5 남은 영어 (배선 아님 = 번역/데이터가 없음)
| 대상 | 규모 | 성격 |
|---|---:|---|
| TV 플레이리스트 제목 | 5,565 | 코퍼스 없음. 번역 런 필요 |
| TV 프로그램 제목 | 1,808 | 상동 |
| `films.title_ko` 결번 | — | 《동경 이야기》 등. TMDB 프로젝션 갭 |
| 감독 이름 | 3,000+ | `directors.name_ko` 컬럼 자체가 없음. 산문 안에서는 이미 한글(번역가가 씀) |
| 촬영지 주소 | — | 로마자 주소. 국가명만 한국어화함 |

### 6.6 코너명 DB화 (오너 지시 08-06)
`entity_type='ui'` 413키 × 4언어 = 1,652행. 번들 사전은 즉시·오프라인 층으로 남고 **DB가 오버라이드**다.
증거: ko 사전에서 "An Invitation"→"초대"로 고쳤는데 앱은 계속 영어였다 — **DB가 옛 문자열을 서빙하고 있었기
때문**. 오버라이드가 정확히 설계대로 이겼다. 사전 수정 시 **반드시 재시딩**:
```
node scripts/i18n-seed-ui-strings.mjs
node scripts/load-content-i18n.mjs --locale ko --dir data/i18n/out/ui_strings --gentle
```

---

## §7 포스터 현지화 · ko 제목 결번 (2026-08-07)

### 7.1 🚨 ko 제목 백필이 알파벳 중간에서 죽어 있었다 — 7개월치 결번

《동경 이야기》·《잔 다르크의 수난》이 영어로 남은 이유를 추적한 결과:

```
ko : 조회됨 4,875 / 7,158   ← 2,283편 미조회
es : 조회됨 6,976
ja : 조회됨 6,977
zh : 조회됨 6,977
fr : 조회됨 6,977
hi : 조회됨 6,977
```

**ko만 미완주.** 미조회분의 알파벳 범위가 정확히 `the-attorney-2013` ~ `zulu-1964`였다.
`ko_fetched_at IS NULL` = 한 번도 물어본 적 없음. 07-17 런이 `the-a…`에서 죽고 재개되지 않았다.

TMDB에는 있었다: `?language=ko-KR` → "동경 이야기", "잔 다르크의 수난".
**번역 문제가 아니라 데이터 수집 중단이었다.** 조치=`worker/tmdb-i18n-backfill.py --locale ko --persist` 재실행.

⚠️**교훈**: `<loc>_fetched_at` 커서가 정직했기 때문에 갭이 보였다. 커서를 다른 용도로 오염시키면
이런 중단은 영원히 안 보인다(그래서 0137은 포스터에 별도 커서를 쓴다).
⚠️백필 스크립트는 **v3 API 키**를 쓴다(`TMDB_READ_TOKEN`이 32자면 v3). Bearer로 보내면 401.

### 7.2 포스터를 읽는 사람의 언어로 (마이그 0137·0138)

TMDB는 포스터마다 `iso_639_1`을 붙인다 — 《기생충》은 한국어 34장, 영어 85장.

**데이터**: `films.poster_path_<loc>` × 6 (0105 패턴 그대로) + `images_fetched_at` 커서 **1개**.
`/movie/{id}/images`가 전 언어를 한 응답에 주므로 호출 1회로 6컬럼이 다 찬다.
🚨**제목 백필의 `<loc>_fetched_at`을 재사용하지 마라** — 제목이 안 채워졌는데 채워졌다고 알리게 된다.

**읽기**: `locVal(film, "poster_path", locale)` — **새 코드 0줄**. 0105 패턴을 정확히 따랐기 때문에
`title_ko`를 푸는 그 세 줄이 포스터도 푼다. 언어 추가 = 컬럼 하나 + 목록 한 줄.

**선정**: 커뮤니티 투표 → 투표수 → 크기 순. 780px 미만 거부(2000px 영어판을 스캔본으로 바꾸면
독자는 버그로 읽는다). 영어와 같은 경로도 거부(정보가 0이고, "현지화 있음" 카운트를 거짓말로 만든다).

**배선**:
- 웹·BFF(film·director) = `locVal`, tonight = `cinecodex_ranked`가 영어를 주므로 **오버레이**
  (랭킹 RPC에 언어를 가르치는 건 과한 거래 — 그 블록이 이미 하는 films 조회에 얹어 추가 쿼리 0)
- 앱 리스트(덱·나·리스트·검색·주행) = `useLocalPosters`, `film_titles_for_slugs`에 `poster` 키 추가(0138)

🚨**포스터는 `contentLang`을 따른다. UI locale이 아니다.** 포스터는 그 개봉판 서체로 그 영화의 제목을
싣고 있는 물건이라 제목과 같은 축이다. 한국어 크롬 + 영어 제목 + 한국어 포스터 = 카드 한 장에서 세 언어가 싸운다.

⚠️`.select()` 문자열은 **반드시 단일 리터럴**. supabase-js가 그 텍스트에서 행 타입을 뽑기 때문에
런타임 조립하면 그 행의 모든 필드 타입이 죽는다(래칫 20→49를 이렇게 겪었다).

**적용 대기**: 0137·0138은 오너 `!` 실행 필요. 이후 `python3 worker/tmdb-poster-i18n.py --persist`.

### 7.3 ✅ 실행 결과 (2026-08-07)

**ko 제목 백필 재실행**: 2,283편 조회 · **한국어 제목 1,672개 신규** · TMDB 실패 1 · 2,282행 기록.
`title_ko` 3,536 → **5,208편**. 《동경 이야기》·《잔 다르크의 수난》·《고양》 전부 들어옴.

**포스터 수집 완주**: 7,153편 조회 · **현지화 포스터 12,956장** · TMDB 실패 1(404, 삭제 항목) · patch 오류 0 · 36분.

| | ko | fr | es | ja | zh | hi |
|---|---:|---:|---:|---:|---:|---:|
| 포스터 보유 | **1,945** | 3,739 | 3,307 | 1,874 | 1,943 | 164 |
| 비율 | 27.2% | 52.2% | 46.2% | 26.2% | 27.1% | 2.3% |

**프로덕션 검증**(main `53625f5d`): 기생충·화양연화·올드보이 한국어 포스터로 교체 · 《동경 이야기》는 TMDB에 없어 영어 유지(정상) · 봉준호 필모 전편 교체 확인 · 0138 RPC가 제목+포스터를 한 요청에 반환.

### 7.4 🚨 Tonight 덱이 하루 동안 영어였던 이유 — `pick()`에 null을 넘김

`08cf68a1` 배포 후 film·director는 한국어인데 **tonight만 영어**. 원인:

```ts
pick(inv, locale, "invitation", slug, "rationale", null)   // ✗
```

`pick()`은 `if (!map || !en) return en;` — **영어 원문이 falsy면 보지도 않고 반환**한다. 다른 호출부에서는
"렌더할 것 없음"과 "번역할 것 없음"이 같은 상태라 옳은 가드다. tonight은 영어 리드를 이미 `leadMap`에 갖고
있어 넘길 게 없었고 `null`을 넘겼다 → 전 항목 null → `if (ko)`가 한 번도 참이 안 됨.

```ts
const en = leadMap.get(slug) ?? null;                       // ✅ 투영의 출발점을 넘긴다
const ko = pick(inv, locale, "invitation", slug, "rationale", en);
if (ko && ko !== en) leadMap.set(slug, firstSentence(ko));
```

**교훈**: "여기 아무것도 없다"를 뜻하는 가드를 가진 헬퍼에게 "나는 이미 갖고 있다"는 뜻으로 자리표시자를
넘기면 안 된다 — 호출부에서 둘은 똑같이 생겼고 하나만 참이다. ✅수정 `a6b4adcd`, 프로덕션 검증 완료.

### 7.5 운영 함정 둘
- 🚨**`SUPABASE_ACCESS_TOKEN` 폐기 시 401** — `apply-sql.py`·`i18n-extract.py`가 동시에 죽는다.
  모양(`sbp_`·44자)은 멀쩡하니 토큰 자체를 Management API로 찔러봐야 안다. 재발급=대시보드 → `.env.local` 한 줄.
- 🚨**파이썬 stdout 버퍼링** — 리다이렉트하면 로그가 20분간 비어, **도는 런과 매달린 런이 구분되지 않는다.**
  진도는 산출물(DB 카운트)로 재라. 워커에 `reconfigure(line_buffering=True)` 넣음(`b080ec7b`).

### 7.6 감독 이름 (마이그 0139) — Wikidata를 **ID로** 조인

Tonight 덱이 한국어 제목 밑에 "Bong Joon Ho"를 찍고 있었다. `directors.name_ko` 컬럼이 아예 없었다.

🚨**TMDB `also_known_as`는 두 번 틀린 답이다**: ① 가장 필요한 사람에게 없다(봉준호·오즈·큐브릭 전부 한글 aka 없음)
② 있는 곳엔 경쟁 표기가 여럿이라 고를 근거가 없다("잉마르 베리히만" vs "잉그마르 베르히만").

✅**Wikidata**: `tmdb_person_id` → `/person/{id}/external_ids` → `wikidata_id` → `wbgetentities` 레이블.
표준 표기 하나로 나온다(잉마르 베리만·오즈 야스지로·스탠리 큐브릭).

🚨**이름으로 매칭하지 마라.** `public.theorists`가 이름매칭으로 22.5% 오염된 전례가 있다. QID 조인은 엉뚱한 사람을 못 붙인다.

**실측(871명 전수)**: Wikidata ID **862(99%)** · ko **748(86%)** · ja 821 · zh 817 · hi 186 · es/fr 각 57.
⚠️es/fr가 낮은 건 결함이 아니다 — 라틴문자권은 표기가 영어와 같아서 **영어와 동일한 레이블은 저장하지 않는다**(정보 0).

**덤**: `directors.wikidata_id`를 함께 저장. AI봇 맞이하기 문서가 **첫 격차로 꼽은 "엔티티 신원"**이 공짜로 확보됐다.

**배선**: `film_titles_for_slugs` 3차 확장(제목→포스터→감독). 한 카드가 셋을 함께 보여주므로 한 요청이 맞다.
시그니처 불변(`returns json`)이라 co-deploy 불필요. 감독 본인 페이지만 BFF `content` 파라미터.

🚨**축 규칙 확정**: **이름·제목·포스터 = `contentLang`**(그 영화/사람이 불리는 이름). **크롬·산문 = `locale`**.
영어 크롬 + 한국어 제목을 원하는 독자가 있고, 한 카드에서 축이 갈리면 세 언어가 싸운다.

### 7.7 앱이 여는 웹 페이지는 대부분 영어다 (앱 결함 아님)

| 앱에서 여는 곳 | 경로 | /ko |
|---|---|---|
| 메타테이크 전체 페이지 | `/film/[slug]` | ✅ 200 |
| TakeScore | `/takescore/film/[slug]` | ❌ 404 |
| 어디서 볼까 전체 | `/whereto/[slug]` | ❌ 404 |
| TV·편성 리스트 | `/tv/*` | ❌ 404 |
| For You · About · 검색 | `/journey` `/about` `/omni` | ❌ 404 |

웹의 ko 셸은 **`/ko/film/[slug]`·`/ko/director/[slug]`·`/ko/film/locations/[slug]` 셋뿐**이다.
`localizedPath` 허용목록이 이 셋만 `/ko/`로 보내는 건 **의도된 동작** — 없는 경로에 붙이면 404고 ISR이 1시간 캐시한다.
**해결은 웹에 ko 셸을 만드는 것**(다국어 프로젝션 §-2.2). 앱에서 눌리는 빈도로는 TakeScore·어디서볼까가 먼저.
