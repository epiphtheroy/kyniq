# HANDOFF — DB 백업 검증·PITR 검토·복구 플레이북 (정본)

*작성 2026-07-17. 상태: **검증 완료·PITR은 오너 결정 대기**. Management API 실측 + 공식 문서 리서치 기반.*

## §1 실측 결과 (2026-07-17, Management API)

- **일일 물리(WAL-G) 백업 정상 작동**: 최근 8일 연속 `COMPLETED` (07-10 ~ 07-17, 매일 ~21:25 UTC = **KST 06:25경**). `walg_enabled=true`, `pitr_enabled=false`.
- 플랜: Pro + Small 컴퓨트(2 cores/2GB, ~$15/월). 리전 ap-northeast-1(도쿄).
- 보관: **7일**(Pro). 즉, 사고 인지가 7일을 넘기면 복구 불가.

## §2 백업의 한계 (모르면 사고 나는 것)

1. **물리 백업은 다운로드 불가** — 대시보드 Download 버튼 없음. 오프플랫폼 사본을 원하면 직접 `pg_dump`/`supabase db dump`(로지컬)를 떠야 한다(§5).
2. **제자리 복원 = 프로젝트 전체 롤백** — DB·REST·Auth 전부 다운타임(크기 비례), 백업 시점 이후 **모든 쓰기 소실**. 단일 테이블 사고에 절대 쓰지 말 것.
3. Storage 객체는 백업에 미포함(메타데이터 행만). 현재 Storage 의존은 미미하나 향후 쓰게 되면 재검토.
4. RPO(최대 데이터 손실 창) = **최대 24시간**. 단, 심야 백필(02:00~06:00 KST) → 06:25 백업 순서라 **현행 심야백필 규칙과 우연히 정합** — 대량 적재는 같은 날 아침 백업에 담긴다.

## §3 단일 테이블 복구 플레이북 (무중단 경로) ⭐

사고 시나리오(예: content_i18n 오염·curation.* 삭제)에서 라이브를 세우지 않고 복구하는 절차:

1. 대시보드 → Database → Backups → **"Restore to a New Project" 탭** → 사고 직전 일일 백업 선택 → 새 프로젝트로 복원(비용 사전 표시, 유료플랜 전용). **라이브는 계속 운영됨.**
2. 클론 프로젝트에서 `pg_dump --table=schema.table` (또는 SQL로 추출).
3. 라이브에 해당 테이블만 복원(트랜잭션 + 사전 스냅샷 백업 후).
4. 클론 프로젝트 삭제(컴퓨트 과금 정지).
5. 주의: 클론에는 Edge Functions·Auth 설정·API 키·확장 설정이 안 넘어옴 — 데이터 추출 용도로만.

## §4 PITR 검토 — 권고: **당분간 보류**

| | 현행(일일 백업) | PITR 7일($100/월) |
|---|---|---|
| 복원 단위 | 하루 1스냅샷 | 2분 간격 WAL, 초 단위 복원 |
| RPO | ≤24h | ≤2분 |
| 일일 백업 | 유지 | **대체됨**(더 이상 안 뜸) |
| 요건 | — | ≥Small 컴퓨트(충족) |

권고 이유: ① 인프라 총비용(~$40/월) 대비 $100/월은 불균형. ② 데이터 대부분이 재생성 가능(TMDB·공장 파이프라인·마이그레이션 SQL이 저장소에 있음). ③ 재생성 불가 자산(큐레이션 산문·content_i18n·CRM 컨택·유저 데이터)은 쓰기 빈도가 낮아 24h RPO로 견딜 만하고, §3 무중단 경로가 있음. **재검토 트리거**: 유저 쓰기(계정·저장·기여)가 유의미해지거나, 시간당 원장성 쓰기(api_calls·mt_events)가 사업 데이터가 되는 시점.

## §5 남은 갭 — 오너 결정 1건

**오프플랫폼 로지컬 덤프 부재.** Supabase 계정 자체가 잠기는 최악 시나리오 대비가 0. 처방: 월 1회(또는 대형 적재 후) 재생성-불가 테이블만 `supabase db dump`로 로컬 보관. **DB 비밀번호가 필요해 오너만 실행 가능** — 원하면 덤프 스크립트+대상 테이블 목록을 다음 세션에서 준비.
