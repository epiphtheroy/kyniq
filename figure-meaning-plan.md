# FilmCurio — 형상-의미(Figure–Meaning) 엔진 설계 핸드오프

> 이 문서는 다른 AI/개발자가 **사전 맥락 없이** 이어받을 수 있도록 쓴 완결 명세다.
> "무엇을 / 왜 / 사용자 효용 / 기존 시스템과의 관계 / 스키마 / 어떻게 구축 /
> 후작업 / 핵심 알고리즘 / 제약 / 빌드 순서 / 미해결 결정"을 모두 담는다.
> 기존 설계 문서와 함께 읽을 것: `site-ia-plan.md`(IA), `film-features-plan.md`,
> `spoiler-guard-design.md`. 작업 언어는 한국어, 코드/식별자는 영어.
>
> **⚠ 용어/결정 갱신(2026-06-14).** 이 문서는 초기 "형상-의미" 메커니즘 문서다. 이후 척추가
> `meta-take-architecture.md`로 정리되며 **의미(meaning) → 메타테이크(meta_take)**, **형상-의미
> → take**로 명칭이 바뀌었다(개념은 동일). 또한 §9의 형상 URL `/figure/[id]`는 **영화 하위 중첩
> `/film/[film-slug]/figure/[figure-slug]`로 확정**되었고, 형상에 **로그인 사용자 기여 레이어**가
> 추가됐다. 형상 페이지/기여 최신 사양은 `figure-page-design.md`.

---

## 0. TL;DR (한 문단)

FilmCurio는 1,000편의 영화를 **작품 → 형상(figure) → 의미(meaning)**의 3층 그래프로
해체한다. *형상*은 작품에 고정된 구체적·관찰 가능한 요소(인물·사물·엔딩·대사·장면)이고,
*의미*는 작품을 가로지르는 추상적 비평 개념이며 **여러 작품의 형상을 잇는 조인 키**다.
하나의 형상은 여러 비평 렌즈(정신분석·정치·형식 등)에 따라 1~N개의 의미를 갖는다.
먼저 AI로 방대한 형상-의미 DB를 구축하되 **"의미를 수렴시키며" 구축**하고(이것이 성패를
가르는 단 하나의 원칙), 그 위에서 후작업(의미 수렴 → 간선 계산 → 랭킹 → 비평 에세이)을
돌려, 사용자가 한 작품의 인상적 요소에서 출발해 **표면은 멀고 심층은 같은** 다른 작품들로
빠져드는 "비평적 토끼굴"을 만든다. TV Tropes의 토끼굴 흡인력을 가지되, 민속분류·표면
패턴매칭이 아니라 **근거 있는 비평적 깊이와 의외의 연결**로 차별화한다.

---

## 1. 무엇을 만드는가 — 모델 정의

기호학의 기표(signifier)/기의(signified) 구조다. (사용자의 기존 프롬프트
`04. SIGNIFIER`가 이 원리의 직접 구현이다.)

- **작품(work)** = 영화. 기존 `films` 테이블.
- **형상(figure)** = 작품에 고정된, 구체적으로 묘사 가능한 요소.
  - 유형 3종(MVP): **인물(character) · 사물/상징(object) · 트로프/구조(trope)**.
    (확장 여지: 엔딩, 대사, 장면, 형식 선택.)
  - 한 작품에 다수. 관찰적·사실적 층위. 예: "성냥공장 소녀의 무표정한 얼굴".
- **의미(meaning)** = 작품을 가로지르는 추상적 비평 개념.
  - 정규화된 횡단 엔티티. **이것이 작품-작품을 잇는 조인 키.**
  - 예: "산업사회가 소모하는 처분 가능한 신체", "침묵이라는 저항".
- **형상-의미(figure_meaning)** = 형상에 의미를 부여하는 비평적 주장(M:N).
  - 하나의 형상에 의미가 1~10개. 각 주장은 **렌즈·근거·확신도**를 동반.

핵심 비대칭: **형상은 작품에 묶이고(구체·1:1), 의미는 작품을 초월한다(추상·N:M).**
토끼굴 경로 = 작품 → 그 형상들 → 각 형상의 의미들 → 같은(또는 인접한) 의미를 진
다른 작품의 형상들 → 그 작품들 → …

---

## 2. 왜 하는가 — 사용자 효용과 차별화

**진입 동기(감정 주도):** 어떤 영화에 *감동받은* 사람은 그 감정의 다른 육화를 찾는다.
"성냥공장 소녀의 그 소녀에게 마음이 움직였다면, 영화사 속 그 혈족(산업적 비극의 여인)을
만나보라." 이는 질문(머리)이 아니라 공명(가슴)에서 출발하는, 기존 frame 층과 **상보적인**
두 번째 입구다.

**효용 3겹:** (1) 다음 볼거리가 *진짜로* 관련 있음 — 장르·시대가 아니라 비평적 의미로
연결되어 회독·체류가 구조적으로 늘어남. (2) 발견 — "이 둘을 연결할 줄 몰랐는데 맞네"의
지적 쾌감. (3) 다른 곳에 없는 비교 콘텐츠 — 의미별 에세이는 검색·AI 인용의 유일 출처.

**TV Tropes와의 차별화(반드시 지킬 것):**
- TV Tropes = 상향식 민속분류, 농담조, **표면 패턴매칭**("이 트로프가 여기 나온다"),
  도파민 = 인식("맞아 저거").
- FilmCurio = **근거 있는 비평적 의미**, 도파민 = 발견("그렇게도 보이는구나").
- 차별화는 인터페이스가 아니라 **연결의 질**(의외이지만 변호 가능한 연결)에 있다.

---

## 3. 기존 시스템과의 관계 (중요)

현재 라이브 상태(이미 구현·배포됨):
- `frames` + `question_frames` + `frame_rankings` — **질문의 원형** 층(머리/호기심).
- `canonical_tags` + `tag_aliases` + `question_tags` — 모티프/테마 태그(평면적).
- `film_features` — 영화당 고정 4섹션(pitch/record/reception/experience).
- pgvector, OpenAI `text-embedding-3-small` 임베딩, 정합 파이프라인 패턴 확립됨.

형상-의미 모델의 위치:
- **`canonical_tags`의 상위·정밀 버전이며 이를 흡수·대체한다.** 기존 태그는 형상과
  의미를 납작하게 뭉갠 것("mirrors"). 형상-의미는 이를 "페르소나의 반복되는 거울"(형상)
  → "정체성의 용해"(의미)로 분리한다. (마이그레이션 시 기존 태그를 의미 후보로 시드 가능.)
- **frame 층과는 상보(대체 아님).** frame=질문 입구, figure-meaning=공명 입구.
  단, "이 형상은 무엇을 뜻하나?"류 질문은 자연히 형상-의미와 맞물리므로, 잎 질문 페이지에서
  형상 칩을 노출해 교차 연결할 수 있다.
- **재사용할 검증된 패턴:** (a) 임베딩 기반 정합(자동 alias ≥0.92 / LLM 판정 회색지대 /
  신규 등록), (b) 야간 통합 루프(클러스터→병합), (c) 무손실 병합(canonical_id 재지정),
  (d) 근거(evidence)·확신도(self_confidence) 강제 + 사후 감사(`content_events`),
  (e) 더블클릭 `.command` 워커 + 단계적 색인.

---

## 4. 스키마 (migration 0013 예정)

```sql
-- 형상: 작품에 고정된 구체적 요소
CREATE TABLE figures (
  id uuid PK,
  film_id uuid REFERENCES films(id),
  kind text CHECK (kind IN ('character','object','trope')),  -- MVP 3종
  label text,            -- 짧은 이름: "the match factory girl's face"
  description text,       -- 구체적·관찰적 묘사 (스포일러 가드 적용)
  provocation text,       -- 이 형상이 던지는 문제제기 한 줄
  embedding vector(1536),
  status text DEFAULT 'approved',
  source text, generated_by text, self_confidence numeric, claims_sourced bool,
  created_at, updated_at
);

-- 의미: 작품 횡단 추상 개념 (조인 키)
CREATE TABLE meanings (
  id uuid PK,
  slug text UNIQUE,
  label text,            -- "the disposable body of capitalism"
  definition text,        -- 2~3문장 비평적 정의
  lens text,              -- 렌즈/패싯 (§5): psychoanalytic|political|formal|...
  embedding vector(1536), -- 의미-의미 인접(soft edges)용
  status text DEFAULT 'active',
  merged_into uuid REFERENCES meanings(id),  -- 무손실 병합
  created_at
);
CREATE TABLE meaning_aliases (alias text PK, meaning_id uuid REFERENCES meanings(id));

-- 형상-의미: 비평적 주장 (M:N) — 하나의 형상에 1~10개
CREATE TABLE figure_meanings (
  figure_id uuid REFERENCES figures(id),
  meaning_id uuid REFERENCES meanings(id),
  lens text,             -- 이 주장이 선 렌즈
  rationale text,         -- 비평적 근거 (어느 장면/대사) — 환각 차단
  confidence numeric,
  created_at,
  PRIMARY KEY (figure_id, meaning_id)
);

-- 후작업 산출: 의미 안에서의 형상 랭킹 (물질화)
CREATE TABLE meaning_rankings (
  meaning_id uuid, figure_id uuid, rank int, rationale text, model text, updated_at,
  PRIMARY KEY (meaning_id, figure_id)
);

-- 게이트용 카운트 뷰 (의미당 형상 수 ≥ N 이어야 허브 발행)
CREATE VIEW meaning_figure_counts AS
  SELECT meaning_id, count(*) FROM figure_meanings GROUP BY meaning_id;
```
RLS: 익명은 published/active만. 워커는 service role. (0001 관례 준수.)

---

## 5. 의미의 유형론 — 렌즈(lens)

"하나의 형상에 의미 1~10개"인 이유는 **비평 렌즈가 여러 개**이기 때문. 같은 형상에:
정신분석(PSYCHO) → "죽음충동의 육화", 정치(POLITICAL) → "처분 가능한 신체",
형식(FACE/형태론) → "무표정, 감정의 거두어들임", 신화/원형 → "희생되는 처녀".

- 렌즈는 **의미 발생기이자 정리 패싯**이다. 토끼굴은 한 렌즈로 깊게, 렌즈를 갈아타며 옆으로.
- 렌즈 목록은 사용자의 30개 프롬프트에서 길어온다(PSYCHO, POLITICAL, PHILOSOPHY,
  SIGNIFIER, PHENOMENON/NOUMENON, FACE, AESTHETIC 등).
- **[결정 A 갱신(2026-06-14)] 고정 5렌즈는 폐기 → 비평 레지스터 2층 모델.** 5종 고정은 밋밋함·
  반복편향을 낳음. 대신 **상위 레지스터 팔레트 ~10종**(형식·기호·정신분석·이데올로기·정치경제·
  철학·실존·신화·영화사계보·수용비평사) + **열린 sub-angle**(사용자의 30 prompt가 여기 흡수).
  반복편향은 형상-적합 선택 + 코퍼스 IDF 균형 + 형상 내 강제 분산 + 근거우선 + 기본값 금지로 차단.
  **레지스터=길(다양), 메타테이크=도착지(수렴).** 상세·전략 = `figure-page-design.md` §6.

---

## 6. 어떻게 구축하는가 — 2단계 + 단 하나의 원칙

**원칙(가장 중요): "지어놓고 나중에 연결"이 아니라 "수렴시키며 짓는다".**
1,000편 × 3유형 × 형상~5 × 의미~3 ≈ 의미 주장 4.5만 개. 영화별로 자유 생성하면 같은
의미가 매번 다른 문자열로 태어나 **4.5만 개의 외딴 노드**가 되고 토끼굴이 0이 된다. 진짜
자산은 형상이 아니라 **의미 그래프**이며, 그 품질은 의미의 재사용(수렴)에 100% 달려 있다.

**Phase 1 — 추출(영화당 1콜, 배치).** 입력: 영화 메타+overview. 출력(JSON): 3유형 ×
형상 다수, 각 형상에 description+provocation, 각 형상에 렌즈별 의미 주장(label+rationale
+confidence). **수렴 장치 필수:** 생성 USER 블록에 "그 영화와 관련될 법한 기존 의미 후보
~30개"를 주입하고 "기존 의미 우선, 신규는 정말 없을 때만"이라 지시. 추출 직후 신규 의미는
정규화→임베딩→기존 의미와 cosine 비교(≥0.92 자동 alias / 0.80~0.92 LLM 판정 /
<0.80 신규). 콜드스타트는 기존 `canonical_tags` + 시드 온톨로지로 의미 어휘를 미리 깐다.

**Phase 2 — 후작업(§7).** Phase 1로 쌓인 그래프 위에서 수렴·간선·랭킹·에세이.

비용(참고): 추출 배치 $10~20, 의미 수렴은 로컬 연산(거의 공짜), 의미별 에세이 ~2천 편이
최대 비용 $30~80. 전체 1회성 수십 달러 규모.

---

## 7. 후작업 4단계 (= 제품의 실체)

1. **의미 수렴(consolidate).** 4.5만 주장 → 임베딩 클러스터링 + LLM 검토 → ~2천 개
   정규 의미로 응축. 비대해진 의미는 분할, 겹치는 의미는 병합(무손실). 야간 루프로 상시.
2. **간선 계산(edges).** 어떤 형상↔형상이 같은/인접 의미로 이어지는지 그래프 구축.
   의미-의미 임베딩 인접(soft edges)으로 동일 의미가 아니어도 점프 가능하게.
3. **랭킹(rank).** 한 의미 안에서 / 한 형상의 친족 중에서 무엇이 가장 강력한 사례인가.
   핵심 알고리즘은 §8. 결과는 `meaning_rankings`에 물질화.
4. **종합 글쓰기(synthesize).** 의미마다 "이 의미를 체현한 작품들과 각자의 방식"을 잇는
   비평 에세이(리스트가 아니라 에세이 — 저밀도를 깊이로 상쇄). 형상마다 "이 형상의 비평적
   하중 + 영화사 속 혈족". 의미당 형상 ≥N(예: 3) 게이트 통과 시에만 허브 발행.

---

## 8. 핵심 알고리즘 — "놀라움 점수"(차별화의 심장)

단순 유사도 랭킹은 평범해진다(누아르끼리). 토끼굴의 도파민은 **표면은 멀고 심층은 같은**
연결에서 나온다(성냥공장 소녀 ↔ 월스트리트 은행가가 "처분 가능한 신체"를 공유).

```
연결 점수 = (의미 근접도)  ×  (표면/장르 거리)
            ↑ 공유 의미 + 의미 임베딩 유사도   ↑ 장르·시대·형식의 거리(임베딩 거리)
```
즉 **의미는 가깝게, 외양은 멀게** 만드는 형상을 상위로. 사용자의 SIGNIFIER 프롬프트 기준
("unexpected, yet robustly defensible")을 수식화한 것. LLM 심판으로 "변호 가능성"을
한 번 더 거른다(억지 연결 차단).

---

## 9. 사용자 표면(UI) — 토끼굴 설계

- URL: **`/film/[film-slug]/figure/[figure-slug]`(형상, 확정)**, `/take/[slug]`(메타테이크 허브;
  옛 `/meaning/[slug]`), 영화 허브에 "이 영화의 형상" 섹션, 형상/메타테이크는 **전부 링크**(TV
  Tropes 핵심). 형상 페이지 상세 = `figure-page-design.md`.
- **의미 허브 = 에세이 + 랭킹 + 형상 목록**(저밀도여도 에세이로 깊이).
- 저밀도에서 굴 유지: 의미-의미 soft edges로 인접 의미 점프
  ("처분 가능한 신체" → "침묵하는 저항자" → "보이지 않는 노동").
- 정서적 다리: "이 인물에 마음이 움직였다면, 그 혈족을 만나보라."
- 약속 카운터: "N편이 이 의미를 공유". 개인 동선 빵부스러기: "5편을 가로질러 7개 형상을
  따라왔습니다"(자기만의 지도 감각).
- 색인 정책: 의미 허브는 게이트(형상 ≥N) 통과 후 index, 미만은 noindex. 단계적 색인.

---

## 10. 제약과 품질(반드시 지킬 것)

- **분포가 전부.** 일회성 의미는 토끼굴에 무용. 추출을 "명명 가능하고 재귀하는 비평
  개념"으로 편향. 결과는 지프 분포(상위 수백 의미가 굴의 대부분).
- **근거 강제.** 모든 figure_meaning에 rationale(장면/대사) 필수, 없으면 폐기.
  비평적 허세·환각 차단.
- **스포일러 가드 적용.** description/에세이는 기존 스포일러 정규식 백스톱·등급 체계를 탄다
  (특히 엔딩 관련 형상). `spoiler-guard-design.md` 준수.
- **사후 감사.** 인간 전수검수 불가 규모 → self_confidence + evidence + 사후
  spot-audit + `content_events` 기록(기존 패턴).

---

## 11. 빌드 순서 (권장)

0. **[미해결 결정] §5 렌즈 4~6개 확정, §12 평면/패싯 결정.**
1. **수직 슬라이스 먼저(핵심).** 15편으로 Phase 1 추출+수렴을 돌려 **분포 실측**:
   의미당 형상이 몇 개 모이는지 = *토끼굴이 실제로 파이는지*. (frame 발굴 때 26개 틀이
   지프 곡선으로 나온 것과 동일한 검증.) 15편에서 핵심 의미에 형상 3~4개가 모이면 1천 편엔
   수십 개 → 굴 확정.
2. migration 0013(§4) + pgvector.
3. 추출 워커(`figure-extract`, 영화당 1콜, 수렴 장치 포함) — 재실행 가능 `.command`.
4. 의미 정합 워커(`meaning-reconcile`, 임베딩+임계값) + 야간 통합 루프.
5. 랭킹 워커(`meaning-rank`, §8 놀라움 점수) → `meaning_rankings`.
6. 에세이 생성(`meaning-essay`, 게이트 통과 의미만).
7. UI: `/meaning/[slug]` 허브 → 영화 허브 "형상" 섹션 → `/figure/[id]` → 토끼굴 동선.
8. 검증·단계적 색인·배포(기존 `.command` + Vercel 패턴).

워커/배포 패턴은 기존 `worker/*.py` + `run-*.command` + `deploy-*.command`와 동일.

---

## 12. 미해결 결정 사항(다음 대화에서 확정)

- **A. 렌즈 개수/목록**(§5): 4~6개 권장. 어떤 렌즈가 가장 풍부한 의미를 길어 올리는가?
- **B. 의미의 구조**: 평면 정규 엔티티 vs 패싯/합성("산업적 비극의 여인" = 하나인가,
  [비극적 여성성]+[계급 희생]+[침묵]의 합성인가). 권장 절충: **의미는 평면, 단 lens 축으로
  분류 + 의미-의미 임베딩 인접 그래프로 soft edges**(평면의 단순함 + 패싯의 유연성).
- **C. 기존 `canonical_tags` 마이그레이션 범위**: 시드로만 쓸지, 완전 흡수·폐기할지.
- **D. 형상 유형 확장 시점**: MVP 3종(인물·사물·트로프) 이후 엔딩·대사·장면 추가 시점.

---

## 부록: 이 문서를 이어받는 AI에게

- 가장 흔한 실패 모드는 "의미를 자유 생성해 4.5만 외딴 노드를 만드는 것"이다. §6 원칙을
  최우선으로 지켜라.
- 이 프로젝트의 검증된 자산: pgvector + OpenAI 임베딩, 정합/통합 루프, 무손실 병합,
  evidence·confidence 게이트, `.command` 워커, 단계적 색인, 스포일러 가드. 새로 발명하지
  말고 재사용하라.
- 시작은 항상 **수직 슬라이스(15편)로 분포 실측** — 토끼굴이 파이는지부터 확인한 뒤 1천 편.
