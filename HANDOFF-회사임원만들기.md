# HANDOFF: 회사임원만들기 프로젝트 — AI 임원(직원) 조직 구축 지시서

> **단일 정본.** 다른 AI가 이 문서 하나로 이어서 구축할 수 있도록 작성했다.
> 작성 2026-07-15 (Opus·4관점 병렬 조사 종합: 인프라 실사·런타임 아키텍처·자율권/컴플라이언스·성장 플레이북).
> 상태: **기획 확정 · 구현 대기 (P0부터 착수)**.
> 선행 시스템(전부 라이브): `/crm` Touchpoint Engine — 정본 `HANDOFF-CRM-비즈니스접점엔진.md` (불변식 §10 필독).
> 컴플라이언스 캐논: `Metatake_아웃리치_운영설계.md`. 세그먼트·존재이유 지도: `lib/admindocs/content/business-touchpoints.ts`.

---

## §0. 오너의 비전 (이 프로젝트의 존재이유)

오너(사장, 1인 운영)의 원문 의도:

> IR·세일즈마케팅·인사팀 **전담 직원을 각각 AI 에이전트로** 만들고 싶다. 이 에이전트가 마치 사람처럼 행동하길 원한다. 마케팅 직원은 각 타겟의 홈페이지를 방문하고 구글링도 할 것이다. 이런저런 정보를 바탕으로 **우리 회사의 핏을 맞춰 제안을 커스터마이징**하고, **그들 입장에서 그들의 존재이유에 가까운 제안**을 하고, 보낼 때는 **스팸 같지 않은 방법**으로 보낸다. **예산이 필요하거나 대표 컨펌이 필요한 일은 보고서로 올리고, 컨펌하면 또 알아서 일을 본다.** 각자 최대한 많은 접촉을 자연스럽게 해내면서 비즈니스 접점을 늘려나간다. 회사 목표 = **비용 최소화 + 트래픽 증가 + 근본적 비즈니스 접점 확대**.

### "직원 비유"의 올바른 번역 (구현 계약)

| 오너의 표현 | 정직한 구현 |
|---|---|
| "타겟 홈페이지 방문+구글링 리서치" | 완전자율 (robots·KR 게이트 안에서) |
| "핏에 맞춘 제안 커스터마이징" | 자율+로그 — 오퍼 41종 coupling 매칭 + personal_line 초안까지 |
| "스팸 같지 않게 전달" | §7의 5요소. **전달 클릭은 언제나 사람** |
| "예산/컨펌 필요한 건 보고서로" | §5 결재 루프 — 보고서 작성 자체는 완전자율 |
| "컨펌되면 알아서 계속 일함" | 승인된 룰·오퍼·세그먼트 **범위 안에서** 계속. 범위 확장은 다시 결재 |
| "자연스럽게 접점 최대한" | 상한은 캡과 관련성 게이트. 좋은 직원일수록 **덜 보내고 더 맞힌다** |
| "사람처럼 행동" | **판단력·리서치 깊이·꾸준함·정성은 사람처럼. 신원은 도구로서 투명하게.** 가공 인물 발신은 금지(§7·§10) |

---

## §1. 한 줄 아키텍처 + 핵심 발견

> **AI 임원 1명 = 역할 플레이북(md 문서) + 스케줄된 헤드리스 Claude 세션(`claude -p`) + CRM DB(장기기억) + 일일 보고서(결재 채널)**

**핵심 발견 (조사로 검증):**
1. `crm_drafts.created_by` enum에 **`'ai'`가 이미 예약**돼 있다 (`0101_crm_core.sql`의 check 제약). AI 직원이 초안을 올리는 데 **스키마 변경 0**.
2. CRM이 이미 "보고→컨펌→재개" 머신이다: 후보는 `crm_candidates`→`/crm/research` 심사→`crm_promote_candidate`, 초안은 `crm_drafts(proposed)`→`/crm/outbox` 승인→Gmail 초안. **발송·승격 경로는 단 1줄도 새로 만들지 않는다.**
3. 이 저장소에는 검증된 하우스 패턴이 이미 5종 있다(§3). 이 프로젝트는 신규 플랫폼 구축이 아니라 **조립 문제**다: 신설 코드는 워커 2파일 + 플레이북 문서들뿐.

---

## §2. 조직도 — 3명 채용

| 직원 | 담당 클러스터 | 주간 발송예산* | 핵심 무기 | 성격 |
|---|---|---|---|---|
| **BD/마케팅** "그로스 오피서" | C 매체 · D 크리에이터 · F 관광 · K 산업 · M 언론 | **5통** | 임베드 위젯 맞춤 데모(`/api/v1/embed.js`), 촬영지 GeoJSON 선물, 인용-후-알림 | 가장 넓은 레인. 매거진 allowlist 150+컨택 다수로 "실행만 남음" |
| **IR** "캐피털 오피서" | A 자본 · B AI기업 | **2통** | **mt_ai_referrals 계기판**("당신 유저가 이미 우리 답을 받고 있다"), MCP 실사용 통계, 데이터룸 원페이저 | B 클러스터가 전사 최우선(이미 라이브·계기화됨) |
| **HR** "피플 오피서" | H 인재 · N 영화관계자 | **3통** | 크루 인용문 팩("당신을 이렇게 읽었다"), 기여자 인프라 데모 | N1(below-the-line 크루)부터 — 회신률 최고·게이트키퍼 없음. **N은 수동 레인 전용**(CRM §5-8-D) |

\* 주 10통 실효 캡(`crm_settings.weekly_send_cap`, 이미 하드코딩)의 직원별 분배. **캡 상향 금지** — 도메인·계정 평판 문제(CRM §10-11). 초과 수요는 "발송 없는 접점"(기브퍼스트 실물)으로 전환.

**권한은 부서가 아니라 행위에 붙는다** — 세 직원 모두 §6 매트릭스 하나를 공유. 직원별로 권한을 달리 주면 감사가 불가능해진다.

---

## §3. 재사용할 기존 인프라 (딛고 설 것 — 재발명 금지)

| 프리미티브 | 위치 | AI 직원이 쓰는 법 |
|---|---|---|
| 초안 상태머신 (proposed→approved→queued→sent) | `supabase/migrations/0101_crm_core.sql` crm_drafts | `created_by='ai'`로 insert → 기존 outbox 결재에 무수정 합류 |
| 후보 큐 + 원자 승격 RPC | crm_candidates (source: scout/radar/**research**/manual) + `crm_promote_candidate` | 리서치 산출 슬롯. evidence_url 필수. 승격은 사람만 |
| 룰 하드필터 (opt-out 불가) | `lib/crm/rules.ts` — suppression·HARD_EXCLUDE_STAGES·LIA 3필드·KR/CA 동의·E*/G* 비상업·팔로업 상한 | AI 초안 워커도 **같은 필터 셋을 반드시 재검사** (draft-propose에 내장) |
| 정중한 크롤 규율 | `worker/crm-scout.py` — robots.txt·KR 요청시점 하드스킵·depth-1·호스트당 2초·≤4요청 | 리서치 워커가 상속 |
| Gmail 동기화·회신 분류·stage 전이 | `app/api/crm/cron/route.ts` 잡②③ + `lib/crm/classify.ts` | 직원은 replied 이후 손 뗌 — 회신 관리는 기존 크론 몫 |
| 워커 공용 배관 | `hourly/pipeline/common.py` — load_env·sb_get/insert/rpc(서비스롤+SB_UA)·anthropic_call·usage.jsonl 실비 기록 | `sys.path.insert` import 선례: crm-scout.py |
| `claude -p` 헤드리스 선례 | `worker/factory-sentinel.py` L187~224 (`--output-format json`) | 직원 세션 호출의 검증된 원형 |
| Mac 상주 워처 패턴 | `hourly/now-playing-watch.sh`, `worker/factory-watch.sh` — nohup+PIDFILE+HOLD 킬스위치+매시 정렬 | ⚠️ **launchd/cron은 TCC가 ~/Documents 차단(2026-07-09 실측) — Terminal 컨텍스트 nohup이 유일 검증 경로** |
| 원자적 잡 클레임 | `worker/factory-watch.sh` L37~42 (`UPDATE ... WHERE status='queued' ... RETURNING`) | "컨펌 = status 플립 → 워커가 클레임해 완주" |
| 체인지오더 결재 원형 | `worker/factory-sentinel.py` — open→proposed→approved→applied·risk 3등급·CO별 md 보고서 | §5 결재 루프의 원형 |
| 발송 규율 데이터화 + 자기정지 | crm_settings 싱글턴 + 잡⑤ 서킷브레이커(반송률 초과 시 스스로 send off) | 직원별 캡·정지 스위치를 같은 싱글턴에 키 추가 |
| 세그먼트×오퍼 라이브러리 | crm_segments 66 + crm_offers 41(coupling 필드) + `business-touchpoints.ts` | "존재이유 결합형 제안"의 시드 문장 |
| 측정 인프라 | mt_events(퍼스트파티)·mt_ai_referrals(AI 유입)·api_calls(/admin/usage) | KPI 귀속 (§8 UTM 규약) |
| append-only 저널·실비 회계 | `hourly/ledger.md`·usage.jsonl 포맷 | 직원 저널·비용 귀속의 하우스 포맷 |

**빠진 것 = 신설 목록 (P0):** `worker/agent-crm.py`(유일 DB 통로), `worker/agent-run.sh`(래퍼+검증), `agents/<role>/PLAYBOOK.md`(직원 정체성), `agents/DECISIONS.md`(결재 채널), `Outputs/agents/<role>/reports/`(보고서), `agents/<role>/journal.md`(저널).

---

## §4. 런타임 아키텍처 — P0 권장안

**3안 비교 결론** (상세 조사 완료): (a) **Mac 워처 + `claude -p` 헤드리스 세션 = P0 채택**. (b) Agent SDK 상주 워커는 운영 부채 순증으로 P3 전까지 채택 이유 없음. (c) Vercel 크론+API 파이프라인은 판단 업무에 부적합 — P2에서 **리서치 팬아웃 전용**(Message Batches 50% 할인)으로만 차용.

### 구성

```
Mac 워처 (Terminal nohup — hourly 워처 패턴, 평일 이른 아침 롤별 시차)
  └─ worker/agent-run.sh <role>        # flock + 런 원장 + 종료 후 검증
       └─ claude -p "$(cat agents/<role>/PLAYBOOK.md)" \
            --model opus --max-turns 80 --output-format json
            도구: WebSearch, WebFetch, Read,
                  Bash(worker/agent-crm.py * 만 allowlist)
```

- **모델: Opus** (오너 지침: Fable 금지·Opus 사용 — hourly 지침을 에이전트 레인에도 적용).
- **비용: 한계비용 ~$0** (Claude 구독 소진). 오너의 대화형 사용과 5시간 윈도우를 공유하므로 **오너 비활동 시간대(예: 05:30~07:00 KST)에 스케줄**. `--dangerously-skip-permissions` 금지 — 필요한 도구만 settings allowlist.
- **직원 추가 = 플레이북 1개 + 워처 엔트리 1개.** 코드가 아니라 역할 문서의 차이일 뿐.

### 보안 원칙: 권한 = 도구 표면 축소

에이전트 프롬프트에 service-role 키를 **절대 노출하지 않는다**. `worker/agent-crm.py`가 유일한 DB 통로이고 서브커맨드는 5개뿐:

| 커맨드 | 역할 | 내장 게이트 |
|---|---|---|
| `candidate-add` | 리서치 산출 → crm_candidates | evidence_url 필수·KR 호스트 거부·dedup 검사 |
| `draft-propose` | 제안 초안 → crm_drafts | `status='proposed'`·`created_by='ai'` 하드코딩 + suppression·stage·LIA·E*/G* 게이트를 rules.ts와 동일하게 재검사 |
| `dedup-check` | 중복 리서치 방지 | — |
| `journal-append` | 저널 기록 | append-only |
| `proposal-add` | 결재 필요 사항 → DECISIONS.md | — |

**발송·승격·suppression 조작·contacts 직접 insert 커맨드는 존재하지 않는다** — LLM이 어긋나도 물리적으로 불가.

추가 가드: **PreToolUse 훅**으로 `.kr/.co.kr/.or.kr` 호스트 WebFetch를 차단(정보통신망법 §50-2, CRM §10-3 계승 — WebFetch는 Anthropic 인프라 fetch라 scout의 UA/robots 규약이 자동 적용되지 않으므로 훅으로 보강).

### 메모리 설계 — 무엇을 DB에, 무엇을 파일에

원칙: **드래프트·게이트에 관여하는 데이터는 전부 DB(단일 진실), 에이전트의 자기 컨텍스트는 파일(싸고 git-감사 가능).**

| 데이터 | 저장처 |
|---|---|
| 상대방 사실(후보·증거·조사 요약) | DB — crm_candidates(evidence_*, meta) → 승격 후 contacts.owner_notes/tags. 스니펫 ≤300자(§10-15) |
| 접촉 이력 | DB — crm_touches (재발명 금지) |
| 런 원장(몇 건 했나·실패) | DB — P0는 content_events(entityType='crm_agent_run') 재사용, P1에 crm_agent_runs 승격 |
| 플랜 결재 | P0: `agents/DECISIONS.md` → P1: crm_agent_proposals 테이블+/crm 패널 |
| 세션 내러티브 저널 | 파일 — `agents/<role>/journal.md` (append-only·최근 40엔트리 롤링·매 세션 tail 30줄만 읽음) |
| 절차적 교훈 | 파일 — 플레이북 `## 교훈` 섹션에 에이전트가 append (오너가 주 1회 git diff 감사) |
| 일일 보고서 | 파일 — `Outputs/agents/<role>/reports/YYYY-MM-DD.md` (커밋 = 감사 추적) |

---

## §5. "보고 → 컨펌 → 알아서 계속" 루프의 기계적 구현

1. **세션 시작**: 플레이북 로드 → journal tail 읽기 → `DECISIONS.md`에서 `상태: APPROVED`·`소비: -` 항목 파싱(=재개 신호) → 소비 마킹 후 착수.
2. **일상 산출**: 리서치→`candidate-add` / 초안→`draft-propose`. 이 단계 컨펌은 **기존 /crm/research·/crm/outbox UI가 그대로 담당 — 새 UI 0**.
3. **판단 유보 사항**(예산·새 세그먼트 착수·툴 구독 등 CRM §11류): `proposal-add` → DECISIONS.md에 블록 append:
   ```
   ### P-2026-07-16-01 [bd] Substack 영화비평가 30인 신규 레인 착수 제안
   근거: … / 예상 비용: $0 / 리스크: …
   상태: PENDING   ← 오너가 APPROVED | REJECTED(사유) 로 수정
   소비: -         ← 다음 세션이 읽으면 세션ID 기입
   ```
4. **일일 보고서** (표준 4섹션 — 3명 공통): ①이번 주 성과(KPI 4개만) ②승인 대기 초안(근거 URL+오퍼 코드+결합점 1줄) ③결정 요청(예산·가격·법적) ④다음 계획. 산출량 수치(초안 수·후보 수)는 부록으로 격리 — vanity metric 방지.
5. **오너 (하루 2분·주 15분)**: /crm 대시보드 4숫자 확인 → outbox/research 심사 → DECISIONS.md PENDING에 한 줄 답.
6. **다음 세션이 1번으로 순환.** REJECTED 사유는 플레이북 `## 금지·교훈`에 반영 — **컨펌이 곧 학습 데이터**.

**완료 위조 방지 (필수·저장소 실사례 있음)**: `agent-run.sh`가 세션 종료 후 **DB 실측(오늘 생성된 candidates/drafts/journal 행 수)과 세션의 자기 주장을 대조**, 불일치 시 런 실패 마킹. "알림은 디스크로 검증" 원칙.

---

## §6. 자율권 매트릭스 (불변 — 3직원 공통)

| 등급 | 행위 |
|---|---|
| **완전자율** | 공개 웹 리서치·구글링(robots·KR 게이트 내), Metatake 내부 데이터 조회, 핏 분석 메모, 보고서 작성 |
| **자율+로그** | 후보 발굴(→candidates, evidence_url 필수), 초안 작성(→proposed), 회신 분류, 팔로업 스케줄 계산(followup_max 클램프 내), 신규 스카우트 소스 '제안', 새 오퍼 '제안서'(draft) |
| **사전결재** (오너 명시 액션 전 실행 불가) | **초안 발송**(이메일·폼 제출·DM 전부 — 채널 불문 전송 클릭은 사람), 후보→컨택 승격, 새 세그먼트 개척(룰 on), 톤/템플릿/오퍼 변경, **가격·조건 제시**(레브셰어율·라이선스 단가·보상 — 기계는 플레이스홀더로 남김), 예산 지출, KR/CA 발송(동의 게이트+결재), parked 재접촉, 캡 변경, send_enabled 토글 |
| **금지** (어떤 승인으로도 불가 — 코드 레벨 차단이 원칙) | **가공 인물 명의 발신**("IR 매니저 김OO"), **AI냐는 질문에 사람이라고 답하기**, LinkedIn/IMDbPro 자동 추출, KR 도메인 이메일 자동수집, suppression 무시·삭제, 거절자(negative_reply) 재접촉, 비공개 이메일 역추적·구매, 트래킹 픽셀, Resend 콜드 발송, 봇→컨택 직행, 수신 메일 전문 저장, **자동응답의 실시간 대화형 진화**(자동응답도 초안까지만 — EU AI Act 50조 고지 의무 원천 회피의 유일 구조) |

---

## §7. "스팸 같지 않게"의 정직한 구현 (불변)

스팸 여부는 발신자가 아니라 **수신자와 규제가 판정**한다. 목표는 "필터를 통과하는 기술"이 아니라 "필터가 통과시키는 게 맞는 메일"이다.

1. **관련성 게이트 — 보낼 자격의 정의.** 초안 생성 전제 = 다음 중 하나: (a) `metatake_url` 존재(그 사람/작품을 다룬 우리 페이지 — "당신을 이렇게 읽었다") (b) 상대의 존재이유와 결합된 오퍼 매칭(coupling) (c) 상대가 먼저 낸 신호(레이더 웜 리드·인바운드). **셋 다 없으면 아직 보낼 때가 아니다** — 리서치를 더 하거나 스킵.
2. **기브퍼스트 시퀀스 — 첫 메일은 요청이 아니라 선물.** ①선물(그의 작업을 다룬 심층 리딩·인용문 팩·데이터) → ②알림("이렇게 썼다, 사실 오류 있으면 정정하겠다" — 이 문장이 콜드 메일을 저널리즘 관행으로 바꾼다) → ③제안(CTA 1개). 첫 메일에서 ③을 빼는 변형 허용.
3. **볼륨 규율 = 경쟁력.** 주10/일20/크론당5·팔로업≤2·반송률 5% 서킷브레이커 (전부 이미 하드코딩). 적게 보내는 것이 병목이 아니라 시스템의 강점.
4. **실명 발신.** 언제나 **Wonwoo Yoon / Metatake** (공개 필명 — 회신 가능·책임 소재 명확·검증 가능). 렌더러가 서명 블록을 강제하는 방향으로 보강 권장.
5. **AI 관여 입장.** (a) 사람이 검토·발송하는 1:1 이메일에 AI 표시 의무는 없다 (b) **거짓말은 금지** — 물으면 "파이프라인이 초안을 만들고 제가 검토해 보냅니다" (c) About/미디어킷에 운영 방식을 공개해 두면 물어보기 전에 답이 공개된 상태 (d) M1·H1·B2 오퍼 메일에는 이 사실을 **오히려 훅으로 명시** — Metatake의 공개 서사가 "1인+AI"이므로.

**가짜 페르소나가 금지인 이유 (요약):** CAN-SPAM 헤더 기만(건당 최대 ~$53,088)·EU AI Act 50조·캘리포니아 B.O.T. Act·GDPR 14조 + Gmail/LinkedIn ToS. 결정타는 신뢰: **수신자가 기자·평론가·학자** — 검증이 직업이고 발각하면 쓰는 사람들. 1인 회사의 "직원"은 LinkedIn 검색 한 번이면 들통난다. Metatake의 판매 자산이 "human-curated 비평 권위"인데 가짜 직원 발각 한 번이면 "비평도 가짜인가"로 번진다. 반대로 "AI가 초안, 사람이 발송"은 M1(테크 언론)·H1·B2·L2 오퍼의 **살아있는 증거물** — 숨길 게 아니라 팔 것.

---

## §8. 부서별 플레이북 (플레이북 작성 시 이 절이 시드)

### 전사 공통 운영 계약
- 발송예산 BD5/IR2/HR3. 초과분은 기브퍼스트로 전환(이월 금지).
- **모든 초안의 마지막 상태는 proposed.** 어느 직원도 발송하지 않는다.
- 개인화 헌법: 모든 초안은 컨택의 `metatake_url` 1줄 포함. 없으면 페이지부터 확인/보강 요청.
- **UTM 규약**: 기브퍼스트 산출물의 모든 링크에 `utm_source=crm&utm_campaign=bd-<play>|ir-<play>|hr-<play>` — 이것 없이 직원별 KPI 귀속 불가.
- **KPI 4개만** (발송수·초안수 금지): 회신률(crm_touches) · stage 전이(negotiating/won=미팅·계약) · 유입(mt_events/mt_ai_referrals) · 백링크 신규 루트도메인.
- 주간 리듬: 월 리서치(+LIA 백필) → 화 기브퍼스트 실물 제작 → 수 초안 → 목 보고서 → 금 측정·루프 관리.
- ⚠️ **P4 재대사(Gmail 소급 동기화+OUTREACH 원장 흡수) 완료 전까지 룰 전부 off·수동 초안 레인만**(CRM §10-6). 파일럿은 **미접촉 신규 세그먼트 한정**으로 시작 — 기발송 상대 중복 1차 메일 방지.

### 직원 1 — BD/마케팅 (C·D·F·K·M)
플레이: ①**인용-후-알림**(상대 필자의 글을 reception/misreadings 페이지에 공정인용≤60단어 후 "당신 글을 인용했다" 통보형 선물 — 상대는 확인하러 방문하고 대개 공유) ②**임베드 위젯 맞춤 데모**(상대 매체의 실제 기사 1건에 TakeScore pill+촬영지 지도가 얹힌 목업 1장 — 스크립트 한 줄=도팔로우 백링크) ③**지역 촬영지 GeoJSON 선물**(비KR 필름커미션 — F2 연간 라이선스로 연결) ④**데이터 리빌 패키지**(크리에이터에게 영상 1편치 독점 소재 무상) ⑤**TV 플레이리스트 co-branded 큐레이션**(시네마테크 상영 시즌 맞춤) ⑥**뉴스레터 데이터 훅**(주간 영화 데이터 1건 무상). ⚠️ **KR 기관(KTO 등)은 이메일 밖** — 공식 문의폼+조달 공고 레인 별도 섹션, 제출은 오너.

### 직원 2 — IR (A·B)
표준 산출물 = **계기판 차트**(mt_ai_referrals 주간 소스별 + api_calls 추이 — 매주 자동 갱신되는 유일무이한 세일즈 자산). 플레이: ①B1 "당신 유저가 이미 우리 답을 받고 있다"(계기판+인용 스크린샷 → 30일 무상 파일럿→사용량 과금) ②MCP 쇼케이스(B3 공동 마케팅) ③랩 조달 프로세스("소송 리스크 0 문화 도메인 데이터" — 컨텍스트 팩이 미리보기) ④VC 20분 데이터룸 리뷰(A1 — 1인 운영 실증 원페이저) ⑤KR 그랜트 레인(A5 — 공고 응모, 이메일 밖). 컨텍스트 팩 샘플 반출은 건별 오너 승인(service_role 전용).

### 직원 3 — HR (H·N)
⚠️ **N은 수동 레인 전용** — scout 금지·IMDbPro/LinkedIn 자동추출 금지. 리서치는 "오너의 수동 승격을 위한 완성된 도시에"까지. 연락처는 공개 포트폴리오 문의처·길드 공개 디렉토리만, 건별 수동, evidence_url 필수. 플레이: ①**"당신을 이렇게 읽었다"**(N1 크루에게 심층 리딩 페이지+인용문 팩 — 부탁 제로, 경력 자산 선물) ②**차기작 타이밍 프레스 킷**(N2 — radar/뉴스룸 신호 감지 시점에 전작 리딩+계보 페이지 제공) ③**퍼블리시스트 커버리지 팩**(N3 — IMDbPro 구독은 N1/N2 회신률 검증 후 오너 결정) ④**기여자 인프라 시연**(H2 프리랜스 비평가) ⑤빌드인퍼블릭 소재 준비(H4 — 발행·등판은 오너).

---

## §9. 구축 순서

### P0 — BD 직원 1명 파일럿 (코드 ~2파일 + 문서)
빌드 목록:
1. `worker/agent-crm.py` — common.py 재사용, 서브커맨드 5개(§4 표), 게이트 내장
2. `worker/agent-run.sh` — flock·런 원장·**종료 후 DB 실측 대조**(완료 위조 방지)
3. `agents/bd/PLAYBOOK.md` — §8 BD 절 + §6 매트릭스 + §7 5요소를 정체성으로
4. `agents/DECISIONS.md` — §5 규약으로 초기화
5. Mac 워처 엔트리(hourly 워처 패턴·Terminal nohup — launchd 금지)
6. PreToolUse 훅(KR 호스트 WebFetch 차단)

첫 4주는 발송보다 **기브퍼스트 실물 재고** 중심: 위젯 데모 목업 4장·GeoJSON 2건·인용-후-알림용 인용 6건·(HR 대비) 크루 인용문 팩 5건. 이 재고가 이후 모든 초안의 회신률을 결정한다.

**성공 기준**: 오너 아침 심사 10분 내 · 후보 승인율 ≥50% · 초안 무수정 승인율 ≥30%.

### P1 — 결재 시스템 승격 + IR 직원
- 마이그레이션: `crm_agent_proposals`·`crm_agent_runs` (번호 = supabase/·worker/·radar/ 3곳 최대+1 재확인 — CRM §10-10)
- /crm 대시보드에 "직원 제안" 패널 1개 (모바일 결재 가능)
- IR 플레이북 추가(런타임 공유), 반려사유→플레이북 자동 반영 루프 정착

### P2 — HR 직원 + 리서치 팬아웃 분리
- HR 플레이북(N1부터). 대량 리서치만 Message Batches API 야간 일괄(50% 할인, magazine research agent 선례)로 분리 — 아침 세션은 판단·커스터마이징만. 비용: 판단=구독 $0 + 조사=배치 $3~6/일.

### P3 — 발송 자동화 옵트인 (조건부·오너 결정)
전제 해석 고정: **승인의 자동화가 아니라 "승인된 것의 집행 시각" 자동화**(proposed→queued 클릭은 영원히 사람). 선결 게이트 전부 충족 시에만:
재대사 완료 · 물리주소+LIA 등록(완료됨) · unsub 엔드포인트 라이브(완료됨) · **수동 발송 ≥50건에서 반송률<2%·스팸 신고 0** · 분류기 인바운드 ≥20건에서 unsubscribe/bounce 오분류 0 · **초안 무수정 승인률 ≥80%**. 켠 후 4주는 캡 불변·주1 hygiene 리뷰. 자동 롤백: 반송률>5%(기존 서킷브레이커)·신고 1건 즉시.

---

## §10. 불변식 (위반 시 프로젝트 존재이유가 무너진다)

CRM §10 불변식 17조 **전부 상속**. 이 프로젝트 고유 추가:

1. **사람 승인 없는 발송 제로** — 채널 불문(이메일·폼·DM). 자동화되는 것은 집행 시각뿐(P3 이후에도).
2. **가공 인물 발신 금지.** 발신 명의는 언제나 Wonwoo Yoon / Metatake. AI냐는 질문에 거짓말 금지.
3. **캡 불변** — 주10(BD5/IR2/HR3). 상향은 P6 전용 도메인+워밍업 이후에만, 그것도 사전결재.
4. **agent-crm.py가 유일한 DB 통로** — 에이전트 프롬프트에 service-role 키 노출 금지. 발송·승격·suppression 커맨드는 만들지 않는다.
5. **KPI에 발송수·초안수 금지** — 회신률·stage 전이·유입·백링크 4개만. 산출량은 보고서 부록으로 격리.
6. **재대사 전 룰 금지** (CRM §10-6 상속) — 그때까지 수동 초안 레인·미접촉 세그먼트만.
7. **완료 주장은 DB/디스크 실측으로 검증** — 래퍼가 대조, 불일치 시 런 실패.
8. **N 클러스터 수동 레인·LinkedIn/IMDbPro 자동추출 금지·KR 이메일 자동수집 금지.**
9. **가격·조건 수치는 기계가 채우지 않는다** — 플레이스홀더, 오너가 승인 시 기입.
10. **자동응답의 실시간 대화형 진화 금지** — 자동응답도 초안까지만.
11. **플레이북 '교훈' 자가-append는 허용하되 행동 규칙 변경은 PENDING proposal 경유** — 주 1회 오너 git diff 감사.
12. **/updates 발표 제약 준수** — 성과 포스트는 데이터 수치형만, 산문 편수·비용·내부도구 발표 금지.

---

## §11. 함정·리스크 (조사에서 실측 발견)

- **오너 승인 병목**: 3직원이 주당 초안 10+후보 수십+보고서 3장 → 보고서 표준화+/crm/research 카드 심사로 오너 시간 주 1~2시간 내 설계. 초과 시 직원 산출 쿼터 축소(캡 증가 방향 금지).
- **LIA 3필드 갭**: 임포트 2,318행 중 3필드 미비 컨택은 초안 생성이 차단됨(§10-4) — 월요일 리서치에 LIA 백필 포함 안 하면 발송 가능 풀이 예상보다 훨씬 작다.
- **Gmail 단일 장애점**: 발송·수신·재대사 전부 개인 Gmail 계정 1개+토큰 1개 — 캡 준수가 곧 계정 보호. 볼륨 확대 전 P6 전용 도메인 분리 검토.
- **TCC 함정**: launchd/cron이 ~/Documents 접근 차단(실측) — Terminal nohup 워처만. Mac 꺼짐=hourly 폴러와 동일하게 수용된 리스크.
- **플레이북 드리프트**: 에이전트 자가-append가 몇 주 뒤 플레이북 오염 가능 — 주 1회 git diff 감사(§10-11).
- **DECISIONS.md 동시성**: 오너 편집과 에이전트 소비 마킹이 같은 파일 — append-only+"소비:" 줄만 수정 규약으로 P0는 버티되, 충돌 1회라도 나면 즉시 P1 테이블 승격.
- **기브퍼스트 소진**: 무상 실물을 회신 없는 상대에게 계속 만들면 리소스 소진 — **클러스터당 회신률 검증 전 최대 2회** 손절 규칙.
- **KPI 게이밍**: 발송수를 금지해도 초안 수·후보 수가 대체 vanity metric이 될 수 있음 — §5-4 보고서 규약이 방어.
- **관할권 오분류**: 한국 거주자의 gmail.com은 도메인 판정 불가 — 애매하면 jurisdiction='KR' 보수 분류, 하향은 오너만.
- **분류기 오분류 = 법적 사고**: unsubscribe를 question으로 오분류하면 재발송=CAN-SPAM 위반 — 수신거부 키워드는 과탐 방향으로 보수 튜닝, unmatched 큐 24~48h 내 비우기.
- **비용 추정 불확실**: 리서치 타겟당 토큰(80~150K)은 ±2배 분산 — P2 배치 전환 전 파일럿 10건 실측.

---

## §12. 착수 가이드 (다음 AI에게)

**시작 전 읽을 것 (순서대로):** ① 이 문서 전체 ② `HANDOFF-CRM-비즈니스접점엔진.md` §4(DDL)·§5-5(룰)·§10(불변식) ③ `Metatake_아웃리치_운영설계.md` ④ `crm-handoff/README.md`(현재 라이브 상태) ⑤ `worker/factory-sentinel.py`(claude -p 선례)·`hourly/pipeline/common.py`(배관).

**현재 라이브 상태 (2026-07-15):** /crm 프로덕션 라이브 · 컨택 2,318 · Gmail OAuth 토큰 발급·검증 완료(초안 파이프라인 실증) · 물리주소 등록 · send off·룰 off·드래프트 0. 어드민 계정: thinkartist1·channel.wonwoo·contact.kyniq.

**P0 첫 행동:** §9 P0 빌드 목록 1번(`worker/agent-crm.py`)부터. 게이트를 rules.ts와 동일하게 재검사하는 것이 핵심 — 코드 리뷰 시 §6 매트릭스와 대조.

**오너 결정 대기 항목:**
| # | 결정 | 기본값(미결정 시) |
|---|---|---|
| 1 | P0 직원 가동 시간대 | 평일 05:30~07:00 KST |
| 2 | 보고서 수신 채널 | `Outputs/agents/*/reports/` 파일 (P1에서 /crm 패널) |
| 3 | IMDbPro 구독(월 ~$20, N3용) | 보류 — N1/N2 회신률 검증 후 |
| 4 | LIA 문서 확정(EU 콜드 전 필요) | crm-handoff/LIA.md 빈칸 대기 |
| 5 | 발송예산 분배(BD5/IR2/HR3) | 제안값 그대로 |

*이 문서는 4개 관점 병렬 조사(기존 인프라 실사·런타임 3안 비교·자율권/컴플라이언스·부서별 성장 플레이북, 각 에이전트가 실제 파일 전문을 읽고 작성)를 종합한 것이다. 조사 원문이 필요하면 오너에게 요청.*
