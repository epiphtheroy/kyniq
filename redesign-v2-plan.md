# FilmCurio 디자인 개편 기획서 — v2 "읽기 도구(Reading Instrument)"

> 동반 산출물: `redesign-v2-home.html`, `redesign-v2-question.html` (열어서 바로 확인).
> 방향: **거의 무채색 · 전면 텍스트 우선 · 홈 + 질문 페이지**. 기존 `redesign-home-and-question.md`(이미지 중심 "dwell engine")의 시각 방향을 **대체**합니다.

---

## 1. 한 문장 철학

**텍스트가 곧 디자인이다.** FilmCurio의 무기는 깊은 해석 텍스트이고, 목적은 (a) AI 답변 엔진에 인용되고 (b) 사람에게 끝까지 읽히는 것. 그러면 디자인이 할 일은 단 하나 — **텍스트 앞에서 비켜서는 것**. 포스터·배너·영상·카드·그림자는 전부 텍스트와 인용을 가리는 무게다. v2는 그 무게를 걷어내고 **타이포그래피 · 여백 · 헤어라인 · 구조**만으로 위계를 만든다.

이건 단순한 미니멀리즘 취향이 아니라 **제품 전략과 일치**한다: zero-click 시대의 해자는 *깊이*이고, 깊이는 곧 본문이다. 본문을 페이지 맨 앞에, 가장 읽기 좋은 형태로 놓는 것이 인용 확률을 직접 끌어올린다.

---

## 2. 지금 무엇이 잘못됐나 (실제 방문 결과)

**홈** — 다크 네이비 배경에 포스터 썸네일 + 질문이 섞인 단조로운 피드. 텍스트가 서로 붙어 위계가 무너짐("The Green Mile (1999)Frank Darabont", "Continue reading →Open full page"). 카드마다 포스터가 시선을 잡아먹지만 정작 *읽을 이유*(해석의 한 줄)는 안 보임. 우측 여백 낭비.

**질문 페이지 — 치명적.** 위에서부터: 무거운 네이비 그라데이션 **배너 + 포스터**(Persona인데 *The Maltese Falcon* 포스터가 붙은 버그) → **빈/깨진 스틸 박스 3개** → **YouTube 영상 모듈**(주황 재생 버튼) → 그리고 **그 한참 아래에야 "THE READING"(=인용 대상인 정식 답변)**. 즉 **제품의 핵심 자산이 장식 미디어 밑에 매장**돼 있다. AI·독자·크롤러 모두 본문에 도달하기 전에 무게부터 만난다. 우측 컬럼은 통째로 비어 있음.

---

## 3. 새 디자인 시스템

### 3.1 컬러 — 거의 무채색 (네이비+마리골드 폐기)

따뜻한 거의-흰 종이 위, 따뜻한 거의-검정 잉크. **액센트는 절제된 단 한 색**(머티드 옥스블러드). 네이비·마리골드·틸 전부 제거.

| 토큰 | Light | Dark | 용도 |
|---|---|---|---|
| `--paper` | `#FBFAF7` | `#141310` | 페이지 배경 |
| `--surface` | `#FFFFFF` | `#1A1916` | (드물게) 떠 있는 면 |
| `--ink` | `#1B1A17` | `#ECE9E1` | 본문 텍스트 |
| `--ink-soft` | `#34322D` | `#D6D3CA` | 부제·리드 |
| `--muted` | `#6E6C66` | `#9B988F` | 메타·보조 |
| `--faint` | `#9B998F` | `#6F6C64` | 최약 메타·플레이스홀더 |
| `--hairline` | `rgba(27,26,23,.12)` | `rgba(236,233,225,.13)` | 구분선 |
| `--accent` | `#8A3B2F` | `#C2604E` | **주요 액션 + 활성 상태 전용** |

**액센트 사용 규칙(엄수):** `--accent`는 화면에서 거의 안 보여야 한다. 등장 허용 위치 = ① 단 하나의 주요 버튼("Share your reading"), ② 활성 업보트(▲), ③ 섹션 라벨의 짧은 틱(tick), ④ "아직 해석 없음 ▸" 같은 초대 신호. 그 외 **모든 링크는 잉크색 + 헤어라인 밑줄**로 처리(색 링크 금지). 다크모드 자동 전환(`prefers-color-scheme`).

### 3.2 타이포그래피 — 웹폰트 0KB

가장 가벼우면서 장문 가독성을 극대화하는 조합: **읽기 본문은 시스템 세리프, UI는 시스템 산세리프.** 다운로드 폰트 0 — 즉 Reddit Sans 웹폰트 로딩 제거(가장 큰 무게 절감 중 하나). 세리프는 긴 해석문을 신문/문예지처럼 읽히게 하고 "Read films closely"의 정체성과도 맞는다.

- 읽기 본문: `--serif` `Iowan Old Style, Charter, "Source Serif 4", Georgia, serif` — **19px / 행간 1.72 / 측정폭 ~64ch**.
- 디스플레이(질문 제목·피드 질문): 세리프 700, 질문 페이지 H1 ~33px, 피드 질문 ~21px.
- 리드/스탠드퍼스트(인용용 한 줄): 세리프 ~22px / 1.5.
- UI·메타·내비·라벨·버튼: `--sans`(system-ui). 섹션 라벨 11.5px / 대문자 / 자간 .13em / `--muted`.

### 3.3 형태 규칙

분리는 **헤어라인 + 여백**으로만. **카드·박스·그림자·그라데이션 전면 금지.** 라디우스 최소(4px, 입력·버튼 한정). 모션은 없거나 hover 밑줄 정도. 한 화면에 주요 액션 1개.

---

## 4. 홈 페이지 구조 (위 → 아래)

포스터를 전부 제거하고, **질문이 주인공인 텍스트 인덱스**로 재구성. ( `redesign-v2-home.html` )

1. **얇은 헤더** — 워드마크(텍스트) · 내비(Films · Directors · Collections) · **작동하는 검색 입력**(기존의 죽은 검색 링크 대체) · Sign in. 하단 헤어라인 하나.
2. **마스트헤드** — "Read films closely."(세리프 대형) + 한 줄 포지셔닝 + 통계(`150 interpretations across 15 films · updated daily`).
3. **Latest readings (피드)** — 행마다: **질문(세리프 21px, 히어로)** → `영화 (연도) · 감독`(작은 메타, 링크) → **해석 한 줄 리드**(이게 스크롤을 멈추게 함) → `n일 전 · read by N` / 답변 없으면 "be the first ▸"(유일한 액센트). 행 사이 헤어라인. **포스터 없음.**
4. **Browse 레일(우측, 조용한 텍스트 인덱스)** — 영화별(질문 수 포함) · 감독별 · 컬렉션 · About 한 줄. 발견성(특히 감독 허브 도달)을 텍스트로 해결.
5. **푸터** — 텍스트만.

요지: 첫 화면이 "인기 포스터 모음"이 아니라 **"여기는 영화가 *해석*되는 곳"**이라고 한눈에 말한다. 전부 텍스트라 빠르고, 스캔되고, 크롤된다.

---

## 5. 질문 페이지 구조 — "답변 우선(answer-first)"

가장 중요한 변경. 무거운 배너·포스터·스틸·영상 모듈을 **전부 제거**하고, **정식 답변(The Reading)을 페이지 상단 히어로로** 끌어올린다. ( `redesign-v2-question.html` )

1. **영화 컨텍스트 = 텍스트 서브헤드** — 그라데이션 배너·포스터 대신, `**Persona** (1966) · dir. **Ingmar Bergman** · ▸ 10 questions` 한 줄(전부 실제 링크). 가볍지만 **SEO/AI의 영화-엔티티 결속을 그대로 충족**(§6 참조).
2. **브레드크럼** — Films › Persona › (현재 질문). 크롤 링크 + 방향성.
3. **질문 H1**(세리프 대형) + 메타(`FilmCurio Editorial · updated · read by N`).
4. **질문 본문**(asker의 변) — 짧게, 좌측 헤어라인 인용 형태.
5. **THE READING — 즉시, 히어로.** 인용용 **리드 한 줄**(굵게/세리프) → 정식 답변 본문(세리프 19px, 모듈 단락) → 크레딧 라인 + **정직한 공시**("AI-written and fact-checked … human oversight by sampling") → 액션(주요 버튼 "Share your reading" / "Suggest an edit" / 활성 업보트). **스틸·영상 없음.**
6. **Readings from the community** (Top · Newest) — 비어 있어도 우아한 빈 상태.
7. **Dwell engine = 순수 텍스트 링크 리스트** — "More questions about Persona" / "More from Ingmar Bergman" / "Continue reading". 이미지 없이도 **막다른 길 방지 + 내부 링크 그래프**를 만든다.
8. **우측 스티키 텍스트 레일** — On this film(제목·감독·질문 수) · More on this film(질문 링크) · More from director. 읽는 동안 관련 질문이 따라옴.
9. **푸터.**

핵심 효과: 인용 대상 텍스트가 **첫 스크롤 안에** 들어오고, 페이지가 가볍고 빠르게 SSR 되며, 막다른 길 없이 내부 링크가 유지된다.

---

## 6. 무게는 뺐는데 GEO는 어떻게 지키나 (중요)

미디어 제거가 GEO를 해치지 않는다 — 오히려 강화한다. SPEC §8.2의 **영화-엔티티 3중 결속**은 이미지가 아니라 **텍스트·마크업**으로 작동하기 때문이다.

- **보이는 링크 텍스트 서브헤드** (5-①) = "real, crawlable HTML text + links" 요건 그대로 충족. 무거운 배너는 *연출*이었을 뿐, 결속의 본질이 아니었다.
- **`<title>` = "{질문} — {영화}({연도}) | FilmCurio"** + **`QAPage.about → Movie` + `sameAs`(IMDb/Wikidata)** JSON-LD 유지(코드 레이어, 시각 개편과 무관).
- **본문 내 영화 1회 자연 멘션 → linkifier 자동 내부링크** 유지.
- **Dwell engine 텍스트 링크** = 내부 링크 그래프·체류·크롤 깊이를 이미지 없이 그대로 제공.
- 본문이 미디어 밑이 아니라 **맨 위**에 오므로, AI가 읽는 prose 우선순위가 올라가 **인용 확률 상승**. 페이지 경량화로 **SSR/LCP 개선**(SEO 보너스).

---

## 7. 제거 목록 (v2에서 사라지는 것)

네이비 그라데이션 배너 · 홈 피드 포스터 · 질문 페이지 포스터 · 스틸 박스 3종 · "Related on YouTube" 영상 모듈 · 모든 카드/박스/그림자 · 네이비/마리골드/틸 팔레트 · Reddit Sans 웹폰트 로딩. (영상이 꼭 필요하면 본문 하단 **텍스트 링크 한 줄**로 대체 가능 — 임베드 아님.)

---

## 8. 적용 가이드 (구현 시)

1. **`app/globals.css`** — §3의 토큰으로 `:root` / 다크모드 블록 교체. 두 목업의 `<style>` 블록이 그대로 출발점(토큰 + 컴포넌트 클래스 동일).
2. **컴포넌트 클래스** — 목업의 `.site-head / .masthead / .feed .item / .reading / .dwell / .rail` 를 컴포넌트(`Header`, `InfiniteScrollFeed`, 질문 페이지)에 매핑.
3. **`app/page.tsx`** — 포스터 제거, 피드 행을 질문-주도 텍스트 행으로. 검색 입력 실연결.
4. **질문 페이지** — DOM 순서를 5번 구조로 재배치(**The Reading를 미디어 위로**). `MediaGallery`/`YouTubeFacade` 렌더 제거(또는 텍스트 링크 폴백). Curiobot 루프 자체는 그대로 두고 *프론트 렌더만* 끔.
5. **충돌 플래그(승인 필요)** — 이 개편은 의도적으로 `AGENTS.md`/SPEC의 두 규칙과 어긋난다: **(a) "Reddit Sans 단일 폰트"** → 시스템 세리프/산세리프로 대체, **(b) Curiobot 미디어를 질문 페이지에 표시** → 텍스트 우선 정책상 미표시. 진행 전 SPEC/AGENTS 문구도 함께 갱신할지 확인 필요.

---

## 9. 같이 발견한 버그 (별건)

- **포스터 미스매치:** Persona(1966) 질문 페이지에 *The Maltese Falcon* 포스터가 붙어 있음 — TMDB 매칭 오류. v2에선 포스터를 안 쓰므로 노출은 사라지지만, `films` 데이터 정합성 차원에서 별도 수정 권장.
- 기존 질문 페이지의 빈 스틸 박스 3개(미디어 미부착 시 깨진 자리) — v2에서 자연 해소.

---

## 10. 결정이 필요한 것

1. 이 방향으로 **실제 코드(globals.css + 두 페이지) 반영**까지 진행할까요, 아니면 목업 단계에서 더 손볼까요?
2. 액센트 옥스블러드(`#8A3B2F`)가 "거의 무채색" 취지에 맞나요, 아니면 **완전 무채색(액센트도 잉크색)**으로 더 밀까요?
3. 영화 페이지·감독 허브·프로필 등 **나머지 페이지로 확장**할까요?
