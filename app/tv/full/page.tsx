"use client";

import MetatakeTV from "@/components/MetatakeTV";

// Full-screen METATAKE TV kiosk. The embedded (large-in-page) version is /tv.
export default function TVFullPage() {
  return <MetatakeTV />;
}
