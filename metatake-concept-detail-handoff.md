# Metatake — 컨셉(개념) 상세 페이지 인수인계 문서 (Concept Detail Spec)

> 대상 목업: **`metatake-concept-detail-mockup.html`** (예시 데이터: *repetition compulsion* — 실데이터, Supabase)
> 이 문서 + 목업 HTML만 보고 `/concept/{slug}` 페이지를 그대로 구현할 수 있도록 작성.
> 홈·영화 상세와 **공유하는 디자인 토큰·네비·푸터**는 `metatake-home-handoff.md` / `metatake-film-detail-handoff.md`를 기준으로 하고, 여기서는 **컨셉 상세 고유의 구조·데이터·메커니즘**을 다룬다.
> 작성: 2026-06-26 · 프로젝트: Supabase `kyniq`(ref `jvgarcqrtsmgfimdcwgo`) / Vercel.

---

## 0. 한눈에 보기 (TL;DR)

- **무엇:** 한 편의 영화가 아니라 **하나의 비평적 개념**(이론에서 온 아이디어)을 종착지로 삼는 페이지. "이 개념이 무엇인가 → 그것이 낳은 트로프 → 그것을 보여주는 영화들 → 그 출처가 된 이론가 → 지도 → 이웃 개념"의 순서로 읽힌다.
- **개념 = 영화 위에 겹쳐진 이론.** 영화 상세가 "한 편을 깊게 읽기"라면, 컨셉 상세는 "한 아이디어가 어떤 영화들에서 작동하는지" 가로지른다.
- **본문 데이터는 거의 전부 실제 테이블에서 온다** (아래 §5 매핑). 목업의 포스터/아바타만 placeholder. **READINGS/TROPES 헤더 카운트는 쿼리 전까지 일부 예시**.
- **6개 섹션** 순서·앵커는 스티키 서브네비 + 스크롤스파이로 이동. 영화 상세보다 짧다(12 → 6 섹션).
- **라이브 확인:** 리스트 페이지 `/concept` 는 라이브. 상세 `/concept/[slug]` 는 이 목업 기준으로 신규 구현(SSG/ISR).

---

## 1. 파일 링크

작업 폴더 `/Users/jerryje/Documents/MetaTake/`

| 파일 | 역할 |
|---|---|
| **`metatake-concept-detail-mockup.html`** | 이 문서의 구현 기준 (컨셉 상세) |
| `metatake-concept-detail-handoff.md` | 이 문서 |
| `metatake-film-detail-mockup.html` / `…-handoff.md` | 영화 상세 — 마스트헤드·서브네비·graphbox·`.rcard`/`.troperow` 패턴의 출처 |
| `metatake-home-handoff.md` / `metatake-home-mockup-v7.html` | 공유 디자인 토큰/네비/푸터의 단일 출처 |

---

## 2. 의도 (왜 이렇게)

1. **개념이 종착지다.** 홈의 "Popular concepts" 타일, 영화 상세의 "Strong Misreadings"·"Tropes", 트로프 페이지 — 모두 결국 비평 개념으로 수렴한다. 이 페이지는 "한 아이디어를 영화들 위에 펼친 지도"의 정점이다.
2. **이론 → 트로프 → 영화의 사다리.** 위에서 아래로 추상이 구체화된다: 개념(이론) → 트로프(반복되는 형상-유형) → 영화(개별 사례). 독자는 어느 층위에서든 진입·하강할 수 있다.
3. **권위는 이론가 + 구조 지표로.** 유저 활동이 적으므로 "After Sigmund Freud"(출처) + 248 films / 12 tropes(구조)로 신뢰를 만든다.
4. **에디토리얼 보이스가 본문.** Definition은 metatake의 목소리로 개념을 짧게 설명하되(잡지 드롭캡), "비평이 실제로 쓰는 어휘를 영화에 매핑한다"는 자기-규정을 명시한다.
5. **짧고 밀도 높게.** 영화 상세(12섹션)보다 가볍게. 6섹션으로 한 번에 스크롤되도록.

---

## 3. 정보구조 (섹션 순서 · 앵커 id)

마스트헤드(dark) → **스티키 서브네비**(dark) → 본문(밝음/paper-2/다크 교차).

| # | 섹션 | 앵커 id | 밴드 |
|---|---|---|---|
| — | 상단 네비 (홈과 동일) | — | dark |
| — | **마스트헤드** (breadcrumb·label·title·native·gloss·stats·theorist·CTA) | — | dark |
| — | **스티키 서브네비** (6 앵커 + 스크롤스파이) | — | dark |
| 1 | Definition / essay (개념 설명, metatake 보이스) | `#definition` | paper |
| 2 | Tropes built on it (트로프 행 리스트 + 카운트 배지) | `#tropes` | paper-2 |
| 3 | Films that embody it (영화 카드 그리드) | `#films` | paper |
| 4 | The theorist (Freud 카드) | `#theorist` | paper-2 |
| 5 | Connection map (라이브 노드 그래프) | `#map` | dark |
| 6 | Related concepts (이웃 개념 칩) | `#related` | paper |
| — | 푸터 (홈과 동일) | — | dark |

> 서브네비 앵커 6개 = 본문 섹션 6개(`#definition #tropes #films #theorist #map #related`). 순서를 바꾸면 스크롤스파이 매핑도 함께 유지할 것.

---

## 4. 디자인 시스템

### 4.1 공유 토큰 (홈·영화 상세와 동일)
색 변수(`--paper #FCFBF7`, `--paper-2 #F4F1E9`, `--ink #1A1714`, `--red #C8102E`, `--gold #E8A100`, `--dark #100D0A` …), 폰트(Newsreader/Spectral/Inter, Google Fonts only), 섹션 헤더(`.shead` + 크림슨 4px 세로바), 밴드(`.band`/`.p2`/`.dark`), 다크 스티키 네비·푸터(TMDB 고지 포함), 상단 `.mockbar` — **100% 재사용**. 단일 self-contained HTML.

### 4.2 이 페이지 고유 컴포넌트 & 크기
| 요소 | 사양 |
|---|---|
| 컨테이너 | max 1280px / 좌우 26px (공통) |
| 마스트헤드 | 풀블리드 그라데이션 오버레이(`.mast-bg`, 좌→우 어둡게). 포스터 없음(개념엔 이미지 없음) → 텍스트 중심 |
| 라벨 | `.clabel` "CONCEPT · THE THEORY" 10.5px/700, red-soft, letter-spacing .18em |
| 제목 | `.ctitle` 58px / 600 세리프 소문자(개념명은 소문자 표기 관례) (≤1020:42px, ≤680:34px) |
| 네이티브 텀 | `.cnative` 이탤릭 22px, 예: *Wiederholungszwang* |
| 글로스 | `.cgloss` 18px 한 문장, max 760px |
| 스탯 스트립 | 3칸(Films·Readings·Tropes), 값 26px, max-width 520px |
| 이론가 라인 | `.theorline` 이탤릭 14px red-soft, "After {theorist}" → `/theorist/{slug}` |
| CTA | `＋ Save concept`(크림슨 primary) · `⌖ Open the map`(ghost, `#map` 스크롤) |
| 스티키 서브네비 | `position:sticky; top:60px`(네비 높이), 높이 48px, 가로 스크롤, 활성=흰색+크림슨 밑줄 |
| Definition | `.essay` max 820px, 본문 18px/1.62, **드롭캡 62px 크림슨**, 하단 `.note`(개념=도구임을 명시) |
| **Tropes** | `.tropelist` 2열 행 리스트. `.troperow`(이름 + 한 줄 설명 `.tv`) + 우측 카운트 배지(`.cnt` 크림슨 pill = film_count). `.troperow.primary`(keystone)는 ink 배지 + 약간 큰 글자 |
| **Films** | `.filmgrid` 5열(≤1020:3열, ≤680:2열). `.fcard`: 포스터 placeholder(2/3, ＋버튼) + 제목/연도 + 감독 + `via {figure}`(이탤릭, 2줄 clamp) + `→ reading` |
| **Theorist** | `.theorist` 그리드 120px/1fr(≤1020:1열). 원형 아바타(이니셜) + 라벨 + 이름 28px + 생몰/소속 이탤릭 + 본문 + key text 출처 + "Open {name} →" 버튼 |
| Connection map | `.graphbox` 높이 420px, viewBox 1180×420. 중심=개념(크림슨 r14), 링=영화(빈 원 + red-soft 링 r7), 외곽=트로프(크림슨 r5). 4.2초마다 재배치 + `.gfade` |
| Related | `.archchips` 칩(개념명 + 네이티브 이탤릭 + film 카운트 배지). hover 시 크림슨 |
| 출처 | `.prov` 이탤릭 — "Generated by the metatake editorial method (AI-drafted) · concept slug … · created … · editor …" |
| 반응형 | 1020px(필름 3열, tropes/theorist 1열화), 680px(필름 2열) |

---

## 5. 섹션별 상세 스펙 (데이터 매핑 · 라우트 · 메커니즘)

> 표기: **소스** = Supabase 테이블.컬럼. 기준은 `sm_concepts` 한 행(개념) 또는 `takes.concept` 문자열 매칭.
> ⚠️ **개념 식별의 미묘함:** `sm_concepts`에는 정확히 `repetition compulsion`(slug `repetitionecompulsion`, n=2)가 있지만, `takes.concept`에는 같은 아이디어가 수십 가지 변형 문자열로 흩어져 있다(`repetition compulsion (Wiederholungszwang)`, `death drive (Todestrieb) / repetition compulsion`, `fort-da (repetition compulsion)` 등). 헤더의 **248 Films** 는 `takes.concept ILIKE '%repetition compulsion%'` 의 distinct 영화수로, 의미상 정확하지만 `sm_concepts.n`(=2)과는 다르다. 실구현은 **정규화 매핑(canonical concept → 변형 문자열 집합)** 또는 임베딩 클러스터로 통일할 것. 자세히 §5.0.

### 5.0 마스트헤드 (헤더)
- **소스:** `sm_concepts`(slug, name, name_l = native term, n = film count). 라벨/글로스는 에디토리얼.
  - title = `sm_concepts.name` (예: "repetition compulsion")
  - native = `sm_concepts.name_l` 의 표기형 (예: "Wiederholungszwang")
  - gloss = 에디토리얼 한 문장 (저장 위치 §10 확인 필요)
- **스탯 스트립 3칸:**
  - **Films** = 이 개념을 읽은 distinct 영화수. 목업은 **248**(실측, `takes.concept ILIKE '%repetition compulsion%'`). `sm_concepts.n`(=2)은 정확 문자열만 세므로 사용하지 않음 — 정규화 매핑 권장.
  - **Readings** = 같은 조건의 `takes` 수 (목업 248 = takes 수와 동일하게 측정됨).
  - **Tropes** = 이 개념의 takes가 연결된 distinct `meta_takes`(via `takes.trope_id`). ⚠️ 목업의 **12** 는 예시(상위 트로프 12개 노출 기준) — 쿼리로 확정.
- **theorist line** = 이 개념의 대표 이론가. 목업 "After Sigmund Freud" → `/theorist/sigmund-freud` (`theorists.slug`).
- **CTA:** `＋ Save concept`(유저 셸프), `⌖ Open the map`(`#map` 스크롤).
- **라우트:** breadcrumb "Concepts" → `/concept` (라이브 리스트).

```sql
-- 헤더 개념 + 실측 film/reading 수 (변형 문자열 포함)
select c.slug, c.name, c.name_l, c.n as exact_n,
       (select count(distinct fig.film_id)
          from takes t join figures fig on fig.id=t.figure_id
          where t.concept ilike '%repetition compulsion%')        as films_inclusive,
       (select count(*) from takes t
          where t.concept ilike '%repetition compulsion%')         as readings_inclusive
from sm_concepts c where c.slug = :slug;
```

### 5.1 Definition / essay
- **의도:** 개념을 metatake 보이스로 ~2단락 설명. Freud의 *Beyond the Pleasure Principle*(1920), 죽음충동의 서명으로서의 반복, 영화가 왜 반복으로 가득한가(루프·귀환·재연). 드롭캡으로 잡지 느낌.
- **소스:** 에디토리얼 텍스트(생성). ⚠️ **저장 위치 확인 필요** — `sm_concepts`에 essay/definition 컬럼이 없음(현재 슬림 스키마: id/slug/name/name_l/n). 별도 `concept_asset`(또는 `sm_concepts`에 컬럼 추가)이 필요. 목업은 작성 카피 사용.
- **카피:** 하단 `.note` "A critical concept — a tool for reading, not a verdict." + 본문 안에 "The vocabulary critics actually use, mapped onto the movies that show it."

### 5.2 Tropes built on it ✅소스 확정
- **의도:** 이 개념을 instantiate 하는 반복 트로프(형상-유형). keystone 트로프 1개를 강조 + 관련 트로프.
- **소스:** 이 개념의 `takes` → `meta_takes`(title, slug, **film_count**), `takes.trope_id = meta_takes.id` 로 조인, distinct. 배지 = `meta_takes.film_count`(전 영화 공유 수).
- **실데이터(검증됨):** The Compulsion To Repeat An Unmastered Wound (**52**, keystone), The Object That Will Not Be Mourned (**43**), The Held Shot As Ethical Witness (**37**), The Death Drive Wearing A Disguise (21), The Return Of The Repressed Wears A Mask (8), The Compulsive Repetition That Wards Off Mourning (7), Trauma Compelled To Restage Its Own Wound (5), The Dance As Compulsion To Repeat (5).
- **라우트:** 트로프 상세 → `meta_takes.slug`. ⚠️ 경로 미확정(트로프 라우트, 홈 문서 §9.3 / 영화 상세 §5.6) — 코드에서 확정.

```sql
select mt.title, mt.slug, mt.film_count, count(*) as takes_here
from takes t join meta_takes mt on mt.id = t.trope_id
where t.concept ilike '%repetition compulsion%'
group by mt.title, mt.slug, mt.film_count
order by takes_here desc, mt.film_count desc nulls last;
```

### 5.3 Films that embody it ✅소스 확정
- **의도:** 이 개념이 읽힌 영화들의 그리드. 각 카드 = 포스터 + 제목/연도/감독 + "via {figure}"(개념을 담아낸 형상) + → reading.
- **소스:** `takes`(concept, theorist_name, take_title) JOIN `figures`(label → "via …", film_id) JOIN `films`(title, year, director, slug, poster_path). `takes.concept ILIKE '%repetition compulsion%'` 필터, 영화별 1행(대표 take).
- **실데이터(검증됨, 슬러그·포스터 실재):** Aftersun(2022, Wells / 카메라 footage), The Iron Claw(2023, Durkin / Von Erich curse), Black Swan(2010, Aronofsky / Nina), The Shining(1980, Kubrick / Room 237), Vertigo(1958, Hitchcock / Herrmann score), Mulholland Drive(2001, Lynch / blue box), Caché(2005, Haneke / surveillance tape), The Favourite(2018, Lanthimos / the rabbits), Marnie(1964, Hitchcock / compulsive theft), The Fabelmans(2022, Spielberg / train-set crash). **"See all 248 films →"**.
- **이미지:** 포스터 `https://image.tmdb.org/t/p/w500{poster_path}` (목업 placeholder). poster_path 실재 확인됨(예: Aftersun `/evKz85EKouVbIr51zy5fOtpNRPg.jpg`).
- **라우트:** 카드 → `/film/{films.slug}`. → reading → 해당 take 상세(있다면) 또는 영화의 #readings.

```sql
select distinct on (f.id)
  f.title, f.year, f.director, f.slug, f.poster_path,
  fig.label as via_figure, t.take_title, t.framework
from takes t
join figures fig on fig.id = t.figure_id
join films f on f.id = fig.film_id
where t.concept ilike '%repetition compulsion%'
order by f.id, t.strength desc nulls last, f.year desc;
```

### 5.4 The theorist ✅소스 확정
- **의도:** 개념의 출처가 된 이론가 카드. key text + 죽음충동으로의 계보.
- **소스:** `theorists`(slug, name, blurb) — 개념 대표 이론가. 연결: `meta_takes.theorist_id` 또는 `takes.theorist_id`/`takes.theorist_name`. 이 개념의 takes는 압도적으로 `theorist_name = 'Sigmund Freud'`.
- **실데이터:** Sigmund Freud, slug `sigmund-freud`(검증됨). ⚠️ `theorists.blurb = NULL` — 목업의 바이오/생몰("1856–1939")·key text("*Beyond the Pleasure Principle* 1920")는 **에디토리얼/외부 지식(Combined Intellectuals database)** 으로 보충해야 함. 목업은 그 출처를 명시("bio note illustrative — to be sourced via the Combined Intellectuals database").
- **라우트:** "Open {name} →" → `/theorist/{theorists.slug}`. 본문 내 "death drive" 링크 → `/concept/{slug}`.

### 5.5 Connection map
- **의도:** "이 개념 = 중심, 영화 = 링, 트로프 = 외곽"의 라이브 지도. (영화 상세의 graphbox 재사용, 단 중심/링/외곽의 의미가 다름.)
- **소스:** 이 개념의 영화들(§5.3) + 트로프들(§5.2) + `meta_take_edges`(a, b, similarity)로 인접. 임베딩 기반.
- **메커니즘:** 중심 노드 1(개념) + 영화 링(원형 배치) + 트로프 외곽. `setInterval 4200ms` 재배치 + `.gfade`. 노드 클릭 = 진입(영화 `/film/{slug}` · 트로프 라우트 · 개념 자기 자신).

### 5.6 Related concepts ✅소스 확정
- **의도:** 공유 영화가 많은 이웃 개념들. 칩 = 개념명 + 네이티브 텀 + film 카운트.
- **소스:** `sm_concepts` 이웃 — 같은 영화들을 공유하는 다른 고-n 개념. 단순 버전: `sm_concepts`에서 의미상 가까운 고-n 항목을 큐레이션/임베딩 인접. (또는 takes 공유 영화 교집합 카운트.)
- **실데이터(검증됨, `sm_concepts.name`/`n`):** death drive (Todestrieb) **134**, return of the repressed (Wiederkehr des Verdrängten) **72**, the uncanny (das Unheimliche) **50**, mourning and melancholia (Trauer und Melancholie) **31**, Nachträglichkeit (deferred action) **13**.
  - ⚠️ 브리프가 예시로 든 숫자(113/63/44/26/11)는 **illustrative**였고, 실DB 값은 위와 같다(목업은 실DB 값 사용).
- **라우트:** 칩 → `/concept/{sm_concepts.slug}`.

```sql
select name, name_l, slug, n from sm_concepts
where name ilike '%death drive%' or name ilike '%return of the repressed%'
   or name ilike '%uncanny%' or name ilike '%mourning and melancholia%'
   or name ilike '%nachtr%'
order by n desc;
```

---

## 6. 데이터 레이어 — 이 페이지가 쓰는 테이블

```
sm_concepts      id, slug, name, name_l(native term), n(exact film count)        (헤더 / 이웃)
takes            figure_id, trope_id, concept, theorist_name, theorist_id,
                 take_title, framework, leap, strength, status                    (Films / Tropes 조인 축)
figures          id, film_id, label(→ via), description, kind, slug               (Films / via 형상)
films            id, title, year, director, director_slug, slug, poster_path      (Films 카드)
meta_takes       id, slug, title, film_count, theorist_id, theory_family_id       (Tropes 배지)
meta_take_edges  a, b, similarity                                                 (Connection map)
theorists        id, slug, name, blurb                                            (The theorist)
```

> ⚠️ 스키마 메모: `sm_concepts`는 슬림하다(essay/definition/gloss/theorist 컬럼 없음). Definition 본문·gloss·대표 theorist 매핑은 **별도 자산 테이블/뷰 또는 컬럼 추가**가 필요(§10). 개념-영화 연결은 `sm_concepts`가 아니라 **`takes.concept` 문자열**을 통해 이뤄지므로, 정규화 매핑(canonical slug ↔ concept 문자열 집합)이 핵심 미해결 과제.

### 6.1 "진짜 vs placeholder"
| 항목 | 상태 |
|---|---|
| 개념명·네이티브 텀(`sm_concepts.name`/`name_l`) | **실데이터** |
| Films 카운트(248), Films 카드(제목/연도/감독/슬러그/포스터경로/via figure/take_title) | **실데이터** (검증됨) |
| Tropes(이름/슬러그/film_count: 52·43·37 …) | **실데이터** (검증됨) |
| Related concepts(이름/네이티브/카운트: 134·72·50·31·13) | **실데이터** (검증됨) |
| Theorist 식별(Sigmund Freud / slug) | **실데이터**; **바이오·생몰·key text = 보충 필요**(`blurb` null) |
| 포스터/이론가 아바타 이미지 | placeholder → **TMDB/외부 교체** |
| Definition essay 본문 · gloss · label 카피 | **에디토리얼(생성)** — 저장 필드 확인 필요 |
| 헤더 Tropes 카운트(12) | **예시** — 쿼리로 확정 |
| 개념-영화 연결의 정규화(canonical concept ↔ 변형 문자열) | **미해결 / 매핑 필요** |

---

## 7. 라우팅 & 연결 (Vercel/Next.js)

- **경로:** `/concept/[slug]` (App Router 동적 라우트). `sm_concepts.slug`로 fetch, 없으면 404. 리스트 `/concept` 는 라이브.
- **렌더:** SSG + ISR 권장(`generateStaticParams`로 n이 큰 개념 prebuild, `revalidate`). 페이지 데이터는 §5 쿼리들을 서버에서 1회 묶어 fetch.
- **이미지:** `next/image`, `image.tmdb.org` 허용. 포스터 w500.
- **내부 링크:** 영화 `/film/{slug}`, 이론가 `/theorist/{slug}`, 트로프 `meta_takes.slug`(경로 확인), 이웃 개념 `/concept/{slug}`, breadcrumb `/concept`.
- **env:** 홈과 동일(`NEXT_PUBLIC_SUPABASE_URL/ANON_KEY`, 서버용 service role).

---

## 8. 메커니즘 (인터랙션)

| 메커니즘 | 동작 |
|---|---|
| 스티키 서브네비 | `position:sticky; top:60px`(네비 높이). 가로 스크롤, 스크롤바 숨김. |
| 스크롤스파이 | `IntersectionObserver`(rootMargin `-120px 0 -65% 0`)로 현재 섹션 링크에 `.on` 토글. |
| Connection map | 중심 1(개념) + 영화 링 + 트로프 외곽, `setInterval(4200ms)` 재배치 + `.gfade`. 노드 `<a>` 클릭 진입. |
| 앵커 이동 | 서브네비 클릭 → `scroll-behavior:smooth`. 각 `section[id]`에 `scroll-margin-top:112px`(네비 60 + 서브네비 48 + 여유). |
| Open the map CTA | 헤더 ghost 버튼 → `#map` 스크롤. |

---

## 9. 구현 체크리스트

- [ ] `/concept/[slug]` 동적 라우트 + SSG/ISR, 404 처리.
- [ ] 마스트헤드: `sm_concepts`(name/name_l) + gloss(에디토리얼) + 스탯 3개(정규화 매핑된 카운트) + theorist line + CTA.
- [ ] 스티키 서브네비(top:60px, 6 앵커) + 스크롤스파이 + `scroll-margin-top`.
- [ ] Definition: 개념 essay(저장 필드 확정) + "tool for reading" 노트.
- [ ] Tropes: `takes.trope_id → meta_takes`, film_count 배지, keystone 강조.
- [ ] Films: `takes`+`figures`+`films`, distinct 영화, via figure + → reading, "See all N films".
- [ ] Theorist: `theorists` 식별 + 바이오/key text 보충(Combined Intellectuals).
- [ ] Connection map: 중심=개념, 링=영화, 외곽=트로프, 재배치 애니메이션, 클릭 라우팅.
- [ ] Related concepts: 이웃 `sm_concepts`(이름/네이티브/n).
- [ ] **개념 정규화 매핑**(canonical slug ↔ `takes.concept` 변형 문자열) 확정 — 카운트·조인의 전제.
- [ ] 공유 네비/푸터/토큰은 홈과 단일 컴포넌트로.
- [ ] 반응형(1020/680).

## 10. 미해결 (Open questions)
1. **개념 정규화:** `sm_concepts.slug`(정확 문자열) ↔ `takes.concept`(수십 변형) 매핑. 카운트·Films·Tropes·Theorist 전부가 여기에 의존. (임베딩 클러스터 or 수동 별칭 테이블.)
2. **Definition/gloss essay 저장 위치:** `sm_concepts`엔 컬럼 없음 → `concept_asset` 신설 or 컬럼 추가.
3. **헤더 Tropes 카운트** 정의(상위 N vs distinct trope 전체).
4. **이론가 바이오:** `theorists.blurb` 채우기(Combined Intellectuals database 연동) — 생몰·소속·key text.
5. **트로프 상세 라우트** 확정(홈 문서 §9.3 / 영화 상세 §5.6과 공유 이슈).
6. **Related concepts 선정 로직:** 의미 큐레이션 vs 공유-영화 교집합 vs 임베딩 인접.

---

## 11. 부록 — 이 페이지 CSS 클래스 글로서리
```
.masthead/.mast-bg                               마스트헤드(개념: 이미지 없음, 그라데이션만)
.crumb/.clabel/.ctitle/.cnative/.cgloss          breadcrumb·라벨·제목·네이티브·글로스
.statstrip/.s                                    개념 스탯(3: Films/Readings/Tropes)
.theorline                                       "After {theorist}" 라인
.mcta/.b(.primary/.ghost)                        CTA(Save concept / Open the map)
.subnav (a.on)                                   스티키 서브네비 + 활성
.band(.p2/.dark) / .shead / .kicker              섹션 프레임(홈과 공유)
.essay/.drop/.note                               Definition essay(드롭캡)
.tropelist/.troperow(.primary)/.tn/.tv/.cnt      Tropes built on it
.filmgrid/.fcard/.pp/.ph/.add/.ft/.fd/.via/.read Films that embody it
.theorist/.av/.tk/.tn/.tt/.tb/.tsrc/.topen       The theorist 카드
.graphbox/#graph/.gnode/.gedge/.gfade            Connection map(홈·영화 상세와 공유)
.archchips/.c/.nat/.b                            Related concepts 칩
.prov                                            편집 출처
footer/.fgrid/.fbrand/.fbar/.tmdb                푸터(홈과 공유)
```

---
*문서 끝. 디자인 의도 §2, 구조 §3, 크기 §4, 데이터·SQL §5/§6, 라우트 §7, 인터랙션 §8. 공유 토큰은 `metatake-home-handoff.md` 기준. 구현은 이 문서 + `metatake-concept-detail-mockup.html`을 1:1로 따라가면 됩니다. 핵심 미해결은 §10.1 개념 정규화 매핑.*
