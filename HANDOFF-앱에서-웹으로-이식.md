# HANDOFF — 앱에서 배운 것을 웹으로 (모션 완성도 · 선택 UI)

2026-07-30 하루 동안 모바일 앱을 다듬으며 만든 것 중 **웹에 옮길 가치가 있는 것**과,
**옮기면 안 되는 것**을 정리한다. 다른 AI에게 지침을 줄 때 이 문서의 §0을 먼저 읽히면
의도를 이해한 상태에서 작업할 수 있다.

작성 근거: 오늘 실제로 구현·배포한 코드(`mobile/`)와 라이브 실측. 추정 아님.

---

## §0. 먼저 읽을 것 — 웹과 앱은 다른 물건이다

이 구분을 놓치면 이 문서는 웹을 망가뜨리는 지침이 된다.

| | 웹 (metatake.net) | 앱 (Metatake iOS) |
|---|---|---|
| 정체성 | **에디토리얼 아카이브** — 읽는 곳 | **판단 도구** — 오늘 밤 뭘 볼지 정하는 곳 |
| 디자인 정본 | `DESIGN-SYSTEM.md` v4 — **직각 모서리 · 헤어라인 · 플랫 레드 `#E3120B`** | `mobile/src/theme.ts` "Lava" — 둥근 모서리 · 소프트 섀도 · 그라데이션 `#FF385C→#D70466` |
| 서체 | PT Serif 중심의 신문 조판 | Inter 중심 + 작품 제목만 세리프 |

🚨 **앱의 "Lava" 비주얼 언어를 웹으로 가져오지 말 것.**
둥근 모서리, 소프트 섀도, 분홍 그라데이션을 웹에 칠하면 아카이브의 정체성이 사라진다.
`DESIGN-SYSTEM.md` 8행에 이미 이 경계가 명시돼 있다(앱은 v4를 의도적으로 폐기했고,
웹 정본은 불변).

**이 문서가 옮기라고 하는 것은 "모션의 원리"와 "선택 UI의 구조"이지, 색과 모양이 아니다.**
웹에서 구현할 때는 v4 토큰(직각·헤어라인·`#E3120B`)으로 그린다.

오너 원칙(2026-07-30): **"내가 요청한다고 해서 사이트 전체의 정체성이나 디자인 컨셉을
해치는 것이면 듣지 않아도 된다."** 이 문서의 어떤 항목도 이 원칙보다 우선하지 않는다.

---

## §0.5 진행 상태 (2026-08-03)

**§1.4의 1~3번 웹 이식 완료.** 브랜치 `feat/discovery-feed`, 미커밋.

| 항목 | 상태 | 어디에 |
|---|---|---|
| 1. 스켈레톤 + 페이지 관통 스윕 | ✅ | `app/motion.css` `.mo-sk` + `components/Skeleton.tsx` |
| 2. 이미지 페이드인 | ✅ 전 사이트 | `components/MotionRuntime.tsx` (img 228개·파일 122개 무수정) |
| 3. 리스트 순차 등장(cap 7) | ✅ 도착하는 목록만 | `.mo-stagger` |
| 4. 진행바 스프링 | ✅ | `.mo-fill` — 커버리지(`/room`·렌즈)·`.tsf-bar`·`.ccx-bar` |
| 5. 링 그리기 | ✅ | `.mo-ring` — `components/ScoreDonut.tsx` (전 TakeScore 표면) |
| 5. TakeScore 카운트업 | ❌ **의도적 미적용** | 아래 4번 항목 참조 |
| 6. CTA 광택 | ✅ 홈 히어로 1곳 | `.mo-sheen` — `components/home2/DecisionHero.tsx` |

**웹에서 알게 된 것 4가지 (문서에 없던 것)**

1. **관통 스윕은 측정이 필요 없다.** `background-attachment: fixed`가 그라데이션을
   뷰포트에 고정하므로 모든 스켈레톤이 이미 하나의 좌표계에 있다. `:root`에 등록
   커스텀 속성(`@property --mo-sweep`) 하나를 애니메이트하고 각 스켈레톤이 그것을
   읽는다 → **페이지당 애니메이션 1개**(앱과 동일한 이점). `:root:has(.mo-sk)`로
   스켈레톤이 없으면 드라이버가 아예 안 돈다.
2. 🚨 **하이드레이션 전에 DOM을 건드리면 안 된다.** 처음엔 차단형 인라인 스크립트로
   `<img>`에 클래스를 붙였는데, React가 이미 렌더한 prop과 어긋나 **하이드레이션
   미스매치**가 났다. 클래스는 더 위험하다 — 이후 렌더가 className을 건드리면 표식이
   지워지고, 그 이미지는 이미 디코드돼서 `load`가 다시 안 뜨므로 **영구히 투명**해진다.
   → 표식은 `data-mo-in` **속성**으로, 작업은 전부 **하이드레이션 이후 effect**에서.
   순서도 중요: **이미 로드된 것 먼저 표시 → 그 다음 게이트 올리기**(안 그러면 깜빡임).
3. **CSS 순차 등장은 서버 렌더 목록에 무의미하다.** CSS 애니메이션은 화면에 있든
   없든 로드 시점에 실행되므로, 긴 색인 페이지는 스크롤해서 도착하기 전에 이미
   등장이 끝나 있다(앱은 스크롤 시 마운트되므로 공짜로 얻는 이점). 그래서 **도착하는
   목록**(검색 결과·필터 결과)에만 적용했다. 정적 색인에 적용하려면
   `animation-timeline: view()`가 필요 — 별건.

   ⚠️ 단, **진행바(4번)는 서버 렌더에도 적용했다.** 등장(3번)은 *감추는* 것이라
   안 보이면 손해지만, 바가 *자라는* 것은 안 보이면 그냥 채워진 상태로 읽힌다.
   감추면 비용, 자라면 공짜 — 이 구분이 판단 기준이다.
4. ❌ **TakeScore 카운트업은 웹에 옮기지 않았다(의도적).** 앱은 값이 JS와 함께
   도착하니까 0에서 세어 올라가는 게 맞다. 웹은 **서버 HTML에 이미 진짜 숫자가
   박혀 있다** — 페인트로 `73`을 보여준 뒤 하이드레이션에서 `0`으로 떨어뜨렸다가
   다시 올리는 꼴이라, 카운트업이 아니라 **버그처럼 읽힌다**. 대신 같은 의도
   ("이미 그만큼"이 아니라 "거기까지 갔다")를 **링 그리기**(5번)가 가져간다 —
   링은 숫자가 아니라 그림이라 서버 렌더와 충돌하지 않는다.
   `stroke-dashoffset`을 빈 링에서 시작해 **암묵적 `to` 키프레임**으로 컴포넌트가
   계산한 값에 착지시키므로, 정적 값이 여전히 단일 진실원이다.

**덤으로 고친 것**: 웹에 이미 손으로 만든 이미지 페이드가 **8벌** 있었는데
(`.idx-thumb`/`.idx-th`/`.idx-hero`/`.idx-rb-thumb`/`.lt-ph`/`.lt-cth`/`.lt-dph`/`.hm-bd`)
전부 무조건 `opacity:0` + 폴백 없음이었다 — §1.3 규칙 3이 경고하는 바로 그 사고가
웹에 이미 심겨 있었다. 전역 메커니즘으로 통합하고 그 `opacity:0`을 제거.

⚠️ **`loading.tsx`는 일부러 안 넣었다.** 색인 페이지들은 크롤 백본이고, 지금
[[gsc-traffic-drop-2026-07-18-diagnosis]] 대응 중이라 Suspense 경계를 크롤 경로에
새로 넣지 않았다. 스켈레톤은 전부 **클라이언트가 가져오는 영역**에만 붙였다.

---

## §1. 모션 — 무엇을 왜 만들었나

### 1.1 문제 정의

오너 지시는 "완성도를 극단적으로 높여라 · 모션 · 반짝이기 · 디테일 · 그라데이션"이었다.
이걸 화면마다 애니메이션을 손으로 붙이는 식으로 하면 **앱이 화면마다 다르게 튄다.**
그래서 **모션 어휘를 하나 만들고 전 화면이 그것만 쓰게** 했다.

정본: `mobile/src/components/motion.tsx` (토큰은 `mobile/src/theme.ts`의 `motion`)

### 1.2 만든 어휘와 그 의도

| 부품 | 무엇 | **왜 (의도)** |
|---|---|---|
| 스프링 3종 `gentle/snappy/bouncy` | 감속 곡선 프리셋 | 애니메이션마다 damping을 손으로 정하면 리듬이 어긋난다. 3개로 강제 |
| `Shimmer` + `Skeleton*` | 로딩 중 **콘텐츠의 형태**를 보여줌 | 스피너는 "무언가 돌고 있다"만 말한다. 스켈레톤은 "무엇이 올지"를 말한다. **스피너 14곳을 전부 교체** |
| **페이지 관통 스윕** | 모든 스켈레톤이 **드라이버 하나**를 공유하고, 각자 `measureInWindow`로 받은 화면 절대위치로 밴드를 놓음 | 타일마다 각자 반짝이면 산만하다. 빛 하나가 페이지를 가로지르며 포스터를 차례로 훑는 느낌. **덤으로 더 싸다** — 화면당 애니메이션 1개(타일당 1개가 아니라) |
| `Appear(index)` | 리스트가 순차적으로 올라오며 등장 | 벽처럼 한꺼번에 뜨는 대신 읽는 순서를 만든다. ⚠️ **stagger는 7개에서 끊는다** — 400행 리스트가 7행처럼 빨라야 한다 |
| `Pop` | 상태가 바뀔 때 튀어오름 | "눌렸다"가 아니라 **"판단이 안착했다"**를 보여준다 |
| `Sparkle` | 입자 버스트 | 긍정 행위(찜)에만. ⚠️ **마운트에는 터지지 않는다** — 스크롤로 들어온 카드가 축하하면 안 된다 |
| `Sheen` | CTA를 스치는 광택 | 화면의 유일한 긍정 행위를 조용히 가리킨다 |
| `ProgressBar` | 값까지 스프링 | 진행도가 "이미 그만큼"이 아니라 **"거기까지 갔다"**로 읽힌다 |
| `useCountUp` | 숫자가 올라감 | 화면의 **주인공 수치 하나**에만 (TakeScore) |
| `haptic.*` | 5종 진동 | 앱 전용. 웹 해당 없음 |

### 1.3 지킨 규칙 (웹에서도 지킬 것)

1. **그라데이션(웹에선 강조색)은 CTA와 활성 상태에만.** 멀티선택 칩을 전부 강조색으로
   칠하면 4개가 서로, 그리고 CTA와 싸운다. 앱에서는 활성 칩을 잉크색 크로스페이드로 처리
2. **무한 루프 애니메이션은 3곳만** — 로딩 · "이어가기" 펄스 · CTA 광택
3. **실패해도 안 깨진다** — 포스터 페이드인은 `onLoadEnd` + **2.5초 타이머 폴백**.
   이벤트가 유실되면 전 포스터가 투명해지는 사고가 나기 때문
4. **측정 실패는 동기화된 시머로 폴백** — 관통 스윕이 위치를 못 받아도 반짝이긴 한다

### 1.4 웹에 옮길 것 — 우선순위 순

| 순위 | 항목 | 웹에서의 값어치 | 난이도 |
|---|---|---|---|
| **1** | **스켈레톤 + 페이지 관통 스윕** | 웹은 ISR/스트리밍이라 대기가 잦다. 지금은 빈 화면이 뜬다 | 중 (CSS 애니 + 공유 드라이버) |
| **2** | **이미지 페이드인** | 포스터가 온 사이트에 있다. 체감품질 최대 레버. CSS `opacity` 전환 + `onload` | 하 |
| **3** | **리스트 순차 등장(cap 7)** | 색인·계보·리스트 페이지가 전부 목록이다 | 하 (CSS `animation-delay`) |
| **4** | **진행바 스프링** | `/room` 커버리지, 계보 진행도에 이미 진행바가 있다 | 하 |
| **5** | **TakeScore 카운트업 + 링 그리기** | `/takescore/film/[slug]`의 주인공 수치 | 하 |
| **6** | CTA 광택 | 웹 CTA는 적다. 효과 대비 낮음 | 하 |
| — | 햅틱 · 스와이프 강조 | **웹 해당 없음** (터치 제스처 아님) | — |

**웹 구현 시 주의**
- React Native Reanimated 코드를 그대로 못 옮긴다. **원리만 가져와 CSS/Web Animations로**
- 관통 스윕의 핵심은 "각 스켈레톤이 자기 화면 위치를 알고 공용 진행값으로 밴드를 놓는다".
  웹에서는 `getBoundingClientRect().x` + CSS 변수 하나로 같은 효과를 낸다
- `prefers-reduced-motion` 존중은 웹에서 필수 (앱보다 강하게 요구됨)

---

## §2. Tonight의 선택 UI — 무엇을 왜 바꿨나

### 2.1 이 앱의 심장 (오너 정의)

> **"내 구독 서비스 × 분위기(중복선택) × TakeScore 높은 순"**

이게 Tonight의 정의다. 웹의 대응 표면은 `/what-to-watch`(MarqueeExplorer)와
`/takescore` 스크리너(ScreenerExplorer)다.

### 2.2 바꾼 것과 의도

| 변경 | **의도 / 문제** | 웹 적용 |
|---|---|---|
| **칩 8개 → 시트형 PickerChip 2개** (정렬·연도) | 상호배타 옵션 8개가 가로로 늘어서면 (a) 세로 공간을 먹고 (b) **현재 설정을 "어느 칩이 칠해졌나"로 추론**해야 한다. 피커 칩은 `[⇅ TakeScore ▾]`처럼 **현재 값을 글자로** 보여준다 | ✅ 강력 추천. 웹은 공간이 넓으니 드롭다운/셀렉트로 |
| **연도 하한** (전체/1980+/2000+/2010+/2020+) | 오너: "너무 오래된 영화들이 계속 앞자리를 차지한다". TakeScore는 정전에 유리해서 흑백 걸작이 상단을 영구 점유한다. **연도 하한이 그걸 푸는 유일한 손잡이** | ✅ **서버는 이미 지원** — `p_year_min`. 웹 ScreenerExplorer도 이미 넘기고 있다. UI만 노출하면 됨 |
| **TakeScore 상위 100/500/1000** | 순서를 바꾸는 게 아니라 **덱을 자른다.** 그래서 다른 필터와 겹쳐 읽힌다: "내 서비스에서, 2000년 이후, 상위 100편" | ✅ 웹에서도 유용. 클라이언트 슬라이스로 충분 |
| **제작국 필터 (복수·편수 표기·많은 순)** | 국가별 시네마는 우리 아카이브의 강점인데 필터가 없었다 | ⚠️ **웹은 아직 `p_country`를 안 넘긴다.** RPC에는 있다 → 웹도 넘기기만 하면 됨 (§2.3) |
| **"내 취향" 영구 opt-in** | 개인화는 **끄고 켤 수 있어야** 한다. 앱은 켜면 `me_recommend_wwi`로 소스가 바뀌고, 그 선택을 기억한다 | ✅ 웹 `/room`에 λ 다이얼이 이미 있다. "개인화 끄기"를 명시적 토글로 |
| **보류 영화 고정 레일 삭제** | 일시적인 것(방금 패스한 영화)이 **영구적인 세로 공간**을 먹고 있었다. 되돌리기는 언두 알약과 상세 페이지에 이미 있다 | 웹 해당 없음 |
| **스와이프 시 카드 테두리가 반응** | 제스처에 화면이 응답해야 "먹혔다"를 안다 | 웹은 hover/focus로 번역 |

### 2.3 제작국 필터 — 서버 사실관계 (중요)

- 랭킹 RPC `cinecodex_ranked`에 **`p_country text`가 이미 있다** (`curation.film.country_code` 비교)
- **단일값**이라 복수 선택이 안 된다 → 앱 BFF는 **국가별 병렬 호출 후 병합**으로 해결
  (`app/api/v1/app/tonight/route.ts`). 핵심 RPC를 건드리지 않아 되돌리기 쉽다
- 가드: 국가 최대 5 · 국가당 300행 · 메모리 페이징 · `total`은 병합 크기
- ⚠️ **`takescore_for_slugs`는 500씩 청크** — 행 반환 RPC라 PostgREST가 1000행에서 자른다.
  5×300이면 초과 → 1000행 밖 영화가 `ts=-1`로 침몰해 "랭킹이 망가진 것처럼" 보인다
- 편수는 마이그레이션 `0117`의 `film_country_counts()` (신규 읽기전용 함수).
  `curation` 스키마는 PostgREST 미노출(406)이라 집계가 서버에 있어야 했다
- 실측 분포: `us 2150 · fr 605 · gb 448 · jp 362 · de 279 · kr 262` (국가정보 보유 6,641편)

**웹에 옮길 때**: 웹은 `cinecodex_ranked`를 직접 부르므로 병렬 병합조차 필요 없을 수도 있다.
단일 국가면 `p_country` 하나만 넘기면 끝. 복수를 원하면 앱과 같은 병합 전략을 쓴다.

### 2.4 Explore에서 배운 것

| 변경 | 의도 | 웹 적용 |
|---|---|---|
| 컬렉션 헤더에 **실제 개수**("리스트 114개") | 규모를 말하지 않으면 사용자는 몇 개인지 모른다. ⚠️ **114는 `film_count≥5` 게이트를 통과한 수**(전체 274 중). 숫자를 부풀리지 말 것 | ✅ `/lineage` 인덱스에 |
| **검색이 리스트에도 닿게** | 리스트를 광고하면서 검색되지 않으면 죽은 길이다 | ✅ 웹 통합검색에 lineage kind 추가 (⚠️ 프론트·RPC 동시 배포 필요) |
| 빈 검색창에 **실제 리스트명 순환** | 검색으로 무엇을 할 수 있는지 예시로 가르친다. **진짜 검색되는 이름만** 쓴다 | ✅ 그대로 이식 가능 |
| 장르·연대 칩 벽 → 가로 스크롤 한 줄 | 칩 5줄이 세로를 먹고 있었다 | 웹은 공간이 넓어 불필요 |

---

## §3. 밀도 — 공간을 되찾은 방법

오너: "프레임 공간 최대 활용". 한 것:

- 카드 간격 `24 → 12`
- 섹션 헤더 여백 `32 → 24` (화면당 5개 섹션이면 ~40pt 회수)
- 칩 벽 → 가로 스크롤
- 헤더를 리스트의 `ListHeader`로 넣어 **스크롤하면 사라지게**

**웹 적용**: 웹은 세로 여백이 훨씬 넉넉하고, 아카이브는 여백이 곧 품격이다.
**밀도 압축을 웹에 그대로 옮기지 말 것.** 다만 "헤더가 스크롤하면 비켜난다"는 원리는
`/film`·`/director` 색인 페이지에서 검토할 만하다.

---

## §4. 다른 AI에게 줄 지침 (복사해서 쓰기)

> `HANDOFF-앱에서-웹으로-이식.md`를 읽어라. **§0을 먼저 읽고 그 경계를 절대 넘지 마라** —
> 앱의 Lava 비주얼(둥근 모서리·소프트 섀도·분홍 그라데이션)을 웹에 가져오면 안 된다.
> 웹은 `DESIGN-SYSTEM.md` v4(직각·헤어라인·`#E3120B`)가 정본이다.
> 옮기는 것은 **모션의 원리와 선택 UI의 구조**뿐이다.
>
> 우선순위: §1.4 표의 1~3번(스켈레톤+관통 스윕, 이미지 페이드인, 리스트 순차 등장) →
> §2.2의 연도 하한과 피커 칩 → §2.3의 제작국 필터.
>
> 서버는 대부분 이미 준비돼 있다(§2.3). 새 RPC를 만들기 전에 기존 인자를 확인해라.
> `prefers-reduced-motion`을 반드시 존중해라.

---

## §5. 참고 — 앱 쪽 구현 위치

| 내용 | 파일 |
|---|---|
| 모션 어휘 정본 | `mobile/src/components/motion.tsx` |
| 모션 토큰 | `mobile/src/theme.ts` (`motion`, `shimmerTint`) |
| 공용 프리미티브(칩·피커시트·포스터) | `mobile/src/components/ui.tsx` |
| Tonight 선택 UI | `mobile/app/(tabs)/index.tsx` |
| 제작국 팬아웃 (서버) | `app/api/v1/app/tonight/route.ts` |
| 국가 편수 (서버) | `app/api/v1/app/countries/route.ts` + `supabase/migrations/0117_film_country_counts.sql` |
