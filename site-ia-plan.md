# FilmCurio — 정보 구조(IA) 설계서 v1

> 목적: "피드 + 잎 페이지"뿐인 단편적 구조를 **차원 → 틀 → 사례**의 3층 디렉토리로
> 재편한다. 이 문서는 디자인과 DB 사이의 계약서다 — 페이지 타입, URL, 내비게이션,
> 페이지별 블록 구성, 색인 정책을 고정한다. 비주얼 디자인은 수직 슬라이스(§9)에서
> 실데이터 위에 한다.
>
> 전제가 되는 결정들(전략 대화에서 확정):
> 태그 3층 정합 파이프라인(닫힌 enum / 시드 어휘 / 임베딩 정합) ·
> 틀(frame) ~150개 + 슬롯 세분화 · 분류는 별도 배치 루프 ·
> 틀 생성/병합만 어드민 게이트 · 허브는 사례 ≥5 게이트 ·
> 고정질문 10개는 별도 URL 없이 영화 허브의 섹션 ·
> 발행과 색인의 분리(단계적 색인) · 작가 렌즈는 같은 DB의 두 번째 렌더링.

---

## 1. 구조 원칙

1. **스키마 = 사이트 구조.** 모든 페이지 타입은 엔티티에 1:1 대응한다.
   새 페이지 타입이 필요하면 먼저 "어떤 엔티티의 렌더링인가"를 답해야 한다.
2. **잎은 좁고 깊게, 허브는 넓고 게이트 있게.** 잎(질문)은 하나의 크럭스만.
   허브(틀·태그·엔딩)는 사례가 충분히 모였을 때만 태어난다.
3. **모든 잎은 최소 하나의 허브에 속한다.** 고아 페이지 금지 — 분류 안 된
   질문은 발행은 되지만 "최근 글" 외의 입구를 갖지 못하므로, 분류 루프가
   따라잡는 것을 전제로 한다.
4. **URL은 계약.** 한 번 색인된 URL은 바꾸지 않는다. 기존 URL
   (`/film/[slug]`, `/film/[slug]/q/[qslug]`, `/director/[slug]`)은 전부 유지.

## 2. 엔티티 ↔ 페이지 타입 맵

| 엔티티 | 페이지 타입 | URL | 색인 |
|---|---|---|---|
| film | 영화 허브 | `/film/[slug]` (기존) | index |
| question (영화 고유) | 질문 페이지 (잎) | `/film/[slug]/q/[qslug]` (기존) | index |
| 고정질문 10종 | 영화 허브의 섹션 | `/film/[slug]#themes` 등 앵커 | (허브에 포함) |
| frame | 틀 허브 | `/frame/[frame-slug]` | 사례 ≥5 && 승인 시 index |
| dimension | 차원 인덱스 | `/frames`, `/endings`, `/motifs` | index |
| ending_type (닫힌 enum) | 엔딩 유형 허브 | `/endings/[type-slug]` | index (enum이라 게이트 불필요) |
| canonical_tag (모티프·테마) | 태그 허브 | `/motif/[slug]`, `/theme/[slug]` | 사례 ≥5 시 index, 이전엔 noindex |
| 슬롯 조합 | 탐색기 (패싯 필터) | `/browse?frame=…&motive=…` | **noindex 영구** (쿼리스트링) |
| director | 감독 허브 | `/director/[slug]` (기존) | index |
| 작가 렌즈 | 틀 허브의 섹션 | `/frame/[slug]#craft` | (허브에 포함) |

원칙: **닫힌 enum 차원은 전용 경로**(`/endings/…` — 수가 적고 안정적이므로 1급 시민),
**열린 태그는 종류별 경로**(`/motif/…`), **틀은 단일 경로**(`/frame/…`).
슬롯 세분화는 절대 URL을 만들지 않는다 — 틀 허브 안의 필터다(틀 분열 방지의 IA 버전).

## 3. 내비게이션 모델

```
[마스트헤드]
The latest · Films · Directors · Questions · Endings · About
                                    │          │
                              /frames 인덱스   /endings 인덱스
```

- **Questions** = `/frames`: 차원별로 묶인 틀 디렉토리. "영화를 보고 사람들이
  품는 큰 질문들"이라는 사이트의 정체성을 내비게이션이 직접 말하게 한다.
- **Endings** = `/endings`: 가장 강한 소비자 후크라 차원 중 유일하게 승격.
  (모티프·테마는 `/frames` 인덱스와 풋터에서 진입 — 상단 항목 비대화 방지.)
- 풋터: 차원 인덱스 전체 + 인기 틀 상위 N + 인기 태그 상위 N.

## 4. 페이지 타입별 블록 구성 (와이어프레임 수준)

### 4.1 영화 허브 `/film/[slug]` — "한 편의 영화에 대한 모든 질문" (개편)
1. 헤더: 제목·연도·감독·포스터 (기존)
2. **The big questions** — 고정질문 10종을 아코디언/섹션으로 (각각 H2 + 앵커,
   별도 URL 없음). 영화 허브가 "그 영화의 완결된 한 페이지"가 되는 핵심.
3. **This film's own questions** — 영화 고유 질문 10개 리스트 (기존 All questions,
   스포일러 칩·마스킹 제목 적용)
4. **Where this film sits** — 이 영화가 속한 틀·태그 칩 목록
   ("열린 결말 영화 87편 중 하나 →" 식으로 허브로 내보내는 링크)
5. 미디어 / 관련 영화 (기존)

### 4.2 질문 페이지 (잎) — 기존 + 모듈 1개
1. 기존 구조 전부 유지 (스포일러 실드 포함)
2. **"This is one of cinema's big questions"** — 본문 직후:
   소속 틀 이름 + 한 줄 정의 + 같은 틀 상위 사례 3개(마스킹 제목) + "전체 N편 →"
   = 모든 잎을 허브로 연결하는 단 하나의 모듈. 과하지 않게 1개 틀만(주 틀).

### 4.3 틀 허브 `/frame/[slug]` — 신설, 사이트의 새 중심
1. 헤더: 틀 이름 + 정의(2~3문장) + 차원 + 사례 수
2. **The ranking** — 편집 랭킹: 사례 리스트(영화·질문 링크·rationale 한 줄·
   스포일러 칩). 상위 10 펼침 + 나머지 접힘.
3. **Filters** — 슬롯 패싯 (동기 유형 · 결말 분기 등; URL 변화 없이 클라이언트 필터)
4. **For writers** (`#craft`) — 작가 렌즈 섹션: 이 비트의 선택지 분포
   (슬롯 통계), 작동 원리 요약(aha 집계), 대표 사례 3편의 craft 노트
5. **Related frames** — 인접 틀 3~5개
6. JSON-LD: `ItemList` (+ FAQ가 적절한 틀이면 `FAQPage`)

### 4.4 엔딩 유형 허브 `/endings/[type]` — 틀 허브의 특화판
틀 허브와 동일 골격 + 유형 간 가로 비교 모듈("열린 결말 vs 꿈 해석 — 갈리는 지점").

### 4.5 차원 인덱스 `/frames`, `/endings`, `/motifs`
차원 설명 한 단락 + 틀/유형 카드 그리드(이름·정의 한 줄·사례 수) + 인기순/최신순.

### 4.6 탐색기 `/browse`
패싯 필터(차원 → 틀 → 슬롯 → 영화 연대/감독) + 결과 리스트.
"미친듯이 뒤지는" 모드 — noindex, 순수 사용자 도구.

### 4.7 홈 (조정)
1. 리드 + 피드 (기존)
2. **Browse the big questions** — 인기 틀 카드 6~8개 (신설, In Brief 아래)
3. In Brief / Most-read films (기존)

## 5. 내부 링크 규칙 (허브-스포크 계약)

- 질문(잎) → 주 틀 허브 1 + 형제 질문 3 + 영화 허브 1
- 영화 허브 → 소속 질문 전부 + 소속 틀/태그 칩
- 틀 허브 → 사례 질문 전부 + 인접 틀 + 차원 인덱스
- 홈/풋터 → 차원 인덱스 + 인기 틀
- **규칙: 어떤 잎에서도 3클릭 안에 다른 차원의 잎에 도달 가능해야 한다.**

## 6. 색인 정책 (발행 ≠ 색인)

| 단계 | 대상 | 처리 |
|---|---|---|
| 즉시 index | 영화 허브, 질문 잎, 감독 허브, 차원 인덱스, 엔딩 유형 허브 | sitemap 포함 |
| 게이트 후 index | 틀 허브(사례 ≥5 + 어드민 승인), 태그 허브(사례 ≥5) | 게이트 전 noindex |
| 영구 noindex | `/browse`, 모든 쿼리스트링 변형, 고아 질문(주 틀 미배정) | robots meta |
| 웨이브 색인 | 신규 영화 대량 투입분 | 발행은 즉시, sitemap 등재는 검색 수요 상위 영화부터 주 단위 웨이브 |

## 7. DB에 거는 요구 (이 IA가 스키마에 요구하는 것)

- `frames(id, dimension, slug, label, definition, slot_schema, status[candidate|approved], embedding)`
- `question_frames(question_id, frame_id, is_primary, slots, confidence, evidence)` — 잎의 "주 틀" 모듈에 `is_primary` 필요
- `frame_rankings(frame_id, question_id, rank, rationale, updated_at)` — 틀 허브 §4.3-2가 물질화 랭킹을 요구
- `canonical_tags / tag_aliases / question_tags` (기확정)
- `films`에 고정질문 10종 답변 저장 위치: `film_features(film_id, feature_kind, body, …)` — §4.1-2가 요구
- 모든 허브 게이트 카운트는 뷰/물질화 뷰로 (`frame_instance_counts`)

## 8. 빌드 순서 (IA → 실물)

1. **0단계 검증**: 기존 발행 질문 임베딩 → 클러스터링 → 틀 후보 실측
   (이 결과가 §4.3·§4.5의 가정을 검증/수정)
2. 마이그레이션: §7 테이블 + pgvector
3. 분류 루프(Loop 5) + 정합 워커
4. **수직 슬라이스**: 엔딩 차원 하나를 끝까지 — enum 확정 → 백필 분류 →
   `/endings` + `/endings/[type]` 1종을 실데이터로 디자인·구현
5. 검증 후 틀 허브·차원 인덱스·잎 모듈로 확장
6. 영화 허브 개편(고정질문 섹션)은 신규 생성분부터 적용, 기존 영화는 백필
7. 작가 렌즈 섹션(#craft) → 탐색기 → (트래픽 후) Ask the archive

## 9. 이 문서가 정하지 않는 것

- 비주얼 디자인 디테일 — §8-4 수직 슬라이스에서 실데이터로
- 틀의 실제 목록 — §8-1 클러스터링이 결정
- Ask the archive / API·MCP — 트래픽 이후 별도 설계
